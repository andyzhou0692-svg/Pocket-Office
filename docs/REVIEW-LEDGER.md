# REVIEW-LEDGER — adjudicated whole-codebase-review findings

The institutional record of every adjudicated review finding (confirmed, refuted,
accepted-as-residual) from the whole-codebase reviews and review-shaped arcs.
Purpose: future reviews stop re-paying for the same refutations — and stop
trusting stale ones. A refutation is a claim about the code **as it stood at a
HEAD**; the code moves, so this ledger is premise-anchored: every row carries
the seam, the exact claim, the anchor paths/mechanism the verdict rested on,
and the HEAD it was rendered at.

## Usage protocol (for every future review run)

1. **Dedup stage**: match each candidate finding against this ledger by
   **seam + claim** (not seam alone — see Notable flips below).
2. **A match against a REFUTED row does NOT kill the candidate — it DEMOTES
   it.** Run `git diff <verdict-head>..HEAD -- <anchor paths>`. If the anchors
   changed, the entry is **EXPIRED** for this candidate: full verification,
   with the old entry attached as context only. If the anchors are unchanged,
   route the candidate to a single cheap checker agent asking exactly: *"does
   the cited mechanism still refute THIS claim?"* The June-9/June-10
   socket-steal case (below) is WHY: same seam, different claim — one refuted,
   one REAL.
3. **Tiering**: only verdicts citing a documented sharp edge or a merged PR
   get the demote fast-path (**tier A**). Judgment-call verdicts
   ("accepted residual", grouped-summary rows, reconstructed mechanisms) are
   **tier B** = always fully re-verify.
4. **A match against a CONFIRMED-fixed row becomes a regression check**:
   verify the fix PR's mechanism is still present at the anchor paths — not a
   re-derivation of the original bug.
5. **Every NEW design-intent refutation MUST cite an existing sharp edge**
   (workspace or nested `CLAUDE.md`, or an in-code WHY doc) **or add one in
   the same PR**. No undocumented refutations enter this ledger.
6. **Append-only.** A flipped verdict appends a superseding row that links the
   old one; the old row is never edited or deleted.
7. **After adjudication, append**: a new dated `##` section, one row per
   adjudicated finding (same 8 columns), ID prefix `RMMDD-NN` (date-based);
   grouped rows only where individual claims weren't preserved (those are
   tier B by definition).
8. **Calibration**: every Nth review runs ledger-blind. A finding the ledger
   would have demoted but the blind run confirms = a false suppression —
   record the rate in `docs/review-metrics/`.

Head notation: `(n/r)` = HEAD not recorded in the backfill source — treat
anchor expiry as tier B.

Verdict vocabulary: `CONFIRMED→PR#N` (optionally via a deferral issue),
`REFUTED-design` (matched documented intent), `REFUTED-wrong-premise`
(the claim's factual premise was false), `ACCEPTED-residual` (real, consciously
not fixed; WHY documented).

---

## Notable flips — premise drift in action

**The socket-steal seam (`crates/pixtuoid-core/src/source/hook/unix.rs`,
liveness arbitration) — the canonical example.** Same seam, three adjudications
in three days (plus the documented mixed-version residual), verdicts in both directions:

1. *2026-06-09 review @151e38d* — a socket-steal-shaped candidate at this seam
   fell into the refuted set (the then-current stale-socket reclaim was the
   documented design). Not individually preserved — it lives only in the
   grouped 26-refuted record (row R0609-17), which is exactly the
   record-keeping failure this ledger exists to fix.
2. *External review finding #11* — CONFIRMED: the bind path **silently stole a
   live daemon's transport**, and the alternative bail killed the whole CC
   source. Fixed in PR #232 (connect-errno probe → typed `SocketBusy` →
   transcript-only degradation). Merged 2026-06-11T02:31Z.
3. *2026-06-10 review @7bc2777 — hours after #232 merged* — CONFIRMED
   (MEDIUM): **the probe itself misclassifies**. The repo's own kernel note
   (`pixtuoid-hook/tests/shim.rs` stalled-listener test) documents that a
   backlog-saturated LIVE daemon yields `ECONNREFUSED` on macOS / `EAGAIN` on
   Linux — so the errno probe steals from a live-but-saturated owner. Fixed in
   PR #235: exclusive advisory lock on a sibling `<sock>.lock` held for the
   daemon's lifetime; connect errnos never decide liveness for a lock-holding
   owner (now a sharp edge in `crates/pixtuoid-core/CLAUDE.md`'s `hook/`
   entry, with the mixed-version lock-less residual honestly documented).

   Lesson: a refuted match must be **demoted, not killed** — and a *fix* is
   itself a premise that the next review may legitimately overturn.

**Two grounded sibling examples (same seam, different claim, opposite
verdicts):**

- *Install lock* (`crates/pixtuoid/src/install/io.rs`): June-9 REFUTED
  "advisory `try_lock` fail-on-contention is a bug" (it is documented design —
  io.rs doc: "FAIL on contention rather than block"); the external review
  CONFIRMED "the lock doesn't cover the whole read→merge→write round"
  (lost-update TOCTOU) → PR #229's `ConfigLock` RAII. Both verdicts correct.
- *Resurrect-in-place* (`state/fsm.rs` + reducer): June-9 REFUTED
  "resurrect-in-place active_tasks bypass" (matched a documented edge) while
  the SAME review CONFIRMED "resurrect drops the open Active span" → PR #210;
  then the external review CONFIRMED a third claim — "resurrect leaves the
  dead life's correlation state, suppressing the new life's hooks" → PR #232.
  Three claims, one seam, three different correct verdicts.

---

## 2026-06-07 — 0.6.0 sweep (v0.5.0..HEAD; 3 engines: bug / refactor / cargo-mutants + adversarial verify)

Source: maintainer memory (`project_codebase-sweep-060`). Engine totals:
bugs 3 MED + 12 low confirmed, 2 rejected; refactor 1 HIGH + 2 MED confirmed,
21 dropped; mutants 86/98 caught. Headline: 23 candidates rejected by
adversarial verify = "very clean".

| # | seam (file/mechanism) | claim (1 line) | verdict | anchor (paths + cited sharp edge / fix PR) | tier | head | date |
|---|---|---|---|---|---|---|---|
| S60-01 | logging `make_filter` (try_from_default_env) | empty `RUST_LOG` silently turns ALL logging off (errs only when UNSET; empty = zero directives) | CONFIRMED→PR#172 | PR #172 (5b6193c): pure `filter_directives` helper; empty = unset policy (reused by #235/#236 env contracts) | A | 0.6.0-dev (n/r) | 2026-06-07 |
| S60-02 | `Activity` enum + `render_to_rgb_buffer` monolith | dead `Activity` (Typing/Reading) feeds nothing; 735-line enqueue monolith | CONFIRMED→PR#173 + PR#174 | PR #173 (8421e73, ~90 sites, golden byte-identical), PR #174 (24495a6, 9 named phases, 60-frame byte-identity matrix) | A | 0.6.0-dev (n/r) | 2026-06-07 |
| S60-03 | `state/{reducer,scope,fsm}` mutation survivors + API hygiene | 12 mutants survive; tuning consts public; event enums not `#[non_exhaustive]` | CONFIRMED→PR#171; partial ACCEPTED-residual | PR #171 (082b32c): 6 survivors killed, 3 boundary mutants documented-equivalent; `AgentEvent`/`ToolDetail` non_exhaustive; `AgentSlot`/`SceneState` REFUSED (literal-constructed in ~12 binary files — disproportionate churn) | A | 0.6.0-dev (n/r) | 2026-06-07 |
| S60-04 | reducer `detail == ToolDetail::Task.display()` | "magic string" Delegating comparison should be a typed state | REFUTED-wrong-premise (+ROI) | one single-source comparison, both sides reference the same enum method — no drift risk; clean fix = 63 construction-site edits + touches the `detail: Arc<str>` cheap-clone invariant | B | 0.6.0-dev (n/r) | 2026-06-07 |
| S60-05 | (grouped) 23 rejected candidates across both engines | various behavior-risk / churn refactor and bug candidates | REFUTED-design (grouped) | adversarial-verify rejections (behavior-risk / intentional-design / not-worth-churn); individual claims not preserved | B | 0.6.0-dev (n/r) | 2026-06-07 |

## 2026-06-09 — 5-lens improvement audit → PR #208

Source: maintainer memory (`project_improvement-audit-pr208`). Architecture
lens returned zero findings (verified correct, not lazy).

| # | seam (file/mechanism) | claim (1 line) | verdict | anchor (paths + cited sharp edge / fix PR) | tier | head | date |
|---|---|---|---|---|---|---|---|
| A08-01 | workspace `rust-version` + fs4 lock sites | MSRV 1.78 is fiction (serde_spanned needs edition2024 ⇒ Cargo ≥1.85); fs4 redundant once std file locks exist | CONFIRMED→PR#208 (closes #194) | PR #208 (151e38d): MSRV→1.89, fs4 dropped for std `File::try_lock`, `just msrv` CI gate; contention parity proven by a 2-handle test | A | pre-#208 (n/r) | 2026-06-09 |
| A08-02 | `SceneLayout::compute_with_seed` per-frame call (tui renderer) | per-frame layout recompute is a perf win waiting on a cache | REFUTED-wrong-premise | profiled 12–42µs/call = 0.04–0.13% of a 33ms frame, visible floor only — cache adds stale-on-resize risk for nothing | B | pre-#208 (n/r) | 2026-06-09 |

## 2026-06-09 — whole-codebase review @151e38d (16-reviewer Workflow) → PR #210 (+ #218, #219)

Sources: maintainer memory (`project_codebase-review-2026-06-09`,
`project_review-followups-pr210`) + PR #210 body. 49 candidates → 23 confirmed
(2 MED + lows) → PR #210 (merged 77c5ef5, 2026-06-09) + 2 deferrals; 26
refuted, killed by the design-intent skeptic against documented sharp edges.

| # | seam (file/mechanism) | claim (1 line) | verdict | anchor (paths + cited sharp edge / fix PR) | tier | head | date |
|---|---|---|---|---|---|---|---|
| R0609-01 | `tui/widgets/tooltip.rs:76` disambig label | MED: `&session_id[..4]` byte-slice panics per-frame when a Reasonix cwd-id splits a UTF-8 codepoint at byte 4 | CONFIRMED→PR#210 | PR #210: `chars().take(4)`; mechanism later superseded by PR #232's 4-hex digest (see EXT-08) | A | 151e38d | 2026-06-09 |
| R0609-02 | `runtime/driver.rs:161` headless summary | MED: untrusted label/detail/Notification reason printed verbatim → ANSI/OSC escape injection in headless mode | CONFIRMED→PR#210 | PR #210: `sanitize_line` (strips `is_control()`) in `summarize` | A | 151e38d | 2026-06-09 |
| R0609-03 | `tui/widgets/hud.rs:285` footer | footer width/pad measured in bytes, not display columns (multi-byte ·/×/↑↓ over-counted; `[q]uit` not flush-right) | CONFIRMED→PR#210 (closes #211) | PR #210: `chars().count()`; first dropped for baseline churn, then RESTORED (0f5a588) + 24 baselines regenerated; tui/CLAUDE.md "byte width"→"column width" | A | 151e38d | 2026-06-09 |
| R0609-04 | `reducer.rs:374` resurrect | resurrect-in-place sets `state=Idle` directly, dropping the open Active span from `active_ms` | CONFIRMED→PR#210 | PR #210: `fsm::resurrect_in_place` folds the span; sharp edge now in core CLAUDE.md ("SessionStart on an EXITING root slot cancels the walkout") | A | 151e38d | 2026-06-09 |
| R0609-05 | `config.rs:147` + `install/claude.rs:112` (parallel-impl drift) | `update_config` skips the fsync `io.rs::write_config_atomic` does; `merge_install` silently coerces a non-object settings doc to `{}` | CONFIRMED→PR#210 | PR #210: fsync parity; bail on valid-JSON-but-non-object (codex immune — TOML doc is always a table). Write-path later fully consolidated by PR #229 | A | 151e38d | 2026-06-09 |
| R0609-06 | `tui_renderer/mod.rs:723/:657` cross-frame state | stale `last_popup_scale` on footer-only early-return; MotionState evicted on current floor only (leak) | CONFIRMED→PR#210 | PR #210: zero on Ok(None)/Err; evict across ALL floors (+ regression test). PoseHistory half resurfaced in the external review (EXT-17 → PR #233) | A | 151e38d | 2026-06-09 |
| R0609-07 | decode/format input-robustness lows | antigravity negative `step_index` mints unmatchable id; sprite hex accepts `+` prefix; final-frame parse error lacks line context; `RgbBuffer::get/put` unbounded | CONFIRMED→PR#210 | PR #210: `.filter(s>=0)`, `is_ascii_hexdigit` gate, error context, `debug_assert` bounds | A | 151e38d | 2026-06-09 |
| R0609-08 | `jsonl` oversized-skip path | >1MiB JSONL tail-skip can drop a buried CC SessionEnd terminator → slow stale-sweep only | CONFIRMED→PR#210 | PR #210 #8: known-file oversized skip tail-scans `check_session_ended` before EOF-seek | A | 151e38d | 2026-06-09 |
| R0609-09 | `source/mod.rs` `Transport` enum | `Transport` not `#[non_exhaustive]` — adding a variant is a needless semver break | CONFIRMED→#212→PR#218 | deferred for a HARD constraint (semver-checks baselines against crates.io 0.6.1 → would red every PR until release); shipped with the 0.7.0 bump (450888a) | A | 151e38d | 2026-06-09 |
| R0609-10 | desk-index / floor-index domain quantities | raw `usize` mixes global vs floor-local desk indices | CONFIRMED→#209→PR#219 | deferred (large cross-crate refactor); PR #219 (a509aa2) `GlobalDeskIndex`/`FloorLocalDeskIndex` — surfaced + fixed 2 real bugs | A | 151e38d | 2026-06-09 |
| R0609-11 | `reducer.rs` `sweep_stale` pass-2 | pass-2 already-cascaded skip is redundant | ACCEPTED-residual | OMITTED as un-pinnable: `mark_exiting` is already write-once, so removing the line changes no observable state — a regression test can't fail-first; documented in the PR #210 commit, not a vacuous test | B | 151e38d | 2026-06-09 |
| R0609-12 | reducer negative-branch test gaps | NEGATIVE/write-once/dedup-skip branches untested (subagent-resurrect gate, ghost-label-after-drop, capacity-dropped ordinal) | CONFIRMED→PR#210 | PR #210: +12 regression tests incl. `exiting-subagent-doesn't-resurrect`, `capacity-dropped-no-ghost-ordinal`, motion-eviction-on-non-current-floor | A | 151e38d | 2026-06-09 |
| R0609-13 | resurrect path vs `active_tasks` | resurrect-in-place bypasses `active_tasks` (subagent-leak suppression) | REFUTED-design | matched a documented sharp edge per the review record; the exact edge text cited then is not preserved and the seam was REWORKED by PR #232 (anchors changed → treat as EXPIRED; see Notable flips) | B | 151e38d | 2026-06-09 |
| R0609-14 | `crates/pixtuoid/src/version.rs::parse_semver` | prerelease identifiers collapse (intra-prerelease ordering ignored) | REFUTED-design | in-code WHY doc at `parse_semver`: 4th tuple component is 0 for prerelease / 1 for release so `0.5.0-rc1 < 0.5.0` per semver precedence — intra-prerelease ordering deliberately unmodeled | A | 151e38d | 2026-06-09 |
| R0609-15 | hook listener detached per-conn tasks | detached tasks are never drained/joined (events lost on shutdown) | REFUTED-design | review record: matched a documented edge; refuting doc not individually recoverable — mechanism today: permit-RAII + `CONN_TIMEOUT`-bounded `tokio::spawn` in `hook/unix.rs::run` | B | 151e38d | 2026-06-09 |
| R0609-16 | `install/io.rs::lock_config` | advisory `try_lock` failing on contention (instead of blocking) is a bug | REFUTED-design | io.rs doc: "FAIL on contention rather than block — `try_lock` returns `Err(TryLockError::WouldBlock)`"; parity preserved through the fs4→std migration (PR #208). Same seam later yielded a REAL confirmed claim (EXT-03 — see Notable flips) | A | 151e38d | 2026-06-09 |
| R0609-17 | (grouped) remaining ~22 refuted candidates | various — incl. detached-tasks/dedup/lifecycle probes | REFUTED-design (grouped) | killed against then-documented sharp edges (per-crate CLAUDE.md catalogs verified "load-bearing AND accurate"); individual claims not preserved — the gap this ledger closes | B | 151e38d | 2026-06-09 |

Findings #15/#18 of the review's numbering were "out of the agreed bundle" and
their content is not preserved in any source — deliberately NOT backfilled.

## 2026-06-10/11 — external 34-finding review → PRs #229–#233

Sources: maintainer memory (`project_external-review-fixes`) + PR bodies.
Triage at then-HEAD post-#227 (588f78d): 27 valid, 4 already-fixed, 3 duplicates/out-of-scope (not individually preserved), **0 hallucinated**.
External finding numbers (#N) follow the external report.

| # | seam (file/mechanism) | claim (1 line) | verdict | anchor (paths + cited sharp edge / fix PR) | tier | head | date |
|---|---|---|---|---|---|---|---|
| EXT-01 | `config.rs` boot `save_version` + `update_config` (#3) | a one-char config typo + the automatic boot version-save silently wipes the whole hand-written config to defaults | CONFIRMED→PR#229 | PR #229 (75653d9): refuse to persist over a file failing TOML or typed `AppConfig` parse; one-time `.pixtuoid.bak` | A | post-#227 (588f78d) | 2026-06-11 |
| EXT-02 | config write path (#15, #16) | saves destroy comments/unknown keys; a second hand-rolled tmp+rename write authority drifts from `io.rs` (no fsync, lock-unlink race) | CONFIRMED→PR#229 | PR #229: `toml_edit::DocumentMut` round-trip (new dep — toml 1.x no longer ships it transitively); ONE write authority via `install/io.rs` | A | post-#227 (588f78d) | 2026-06-11 |
| EXT-03 | `install/io.rs` lock scope (#7) | the advisory lock doesn't cover read→merge→write — concurrent installs lose updates (TOCTOU) | CONFIRMED→PR#229 | PR #229: `ConfigLock` RAII held across the whole round; `write_config_atomic` = `lock_config().write_atomic()`. Counterpart refutation: R0609-16 (see Notable flips) | A | post-#227 (588f78d) | 2026-06-11 |
| EXT-04 | `install/io.rs` perms (#6) | rewrites silently widen a 0600 settings.json (API keys) to 0644 | CONFIRMED→PR#229 | PR #229: target mode preserved; new files 0600; tmp private from open | A | post-#227 (588f78d) | 2026-06-11 |
| EXT-05 | hook-path / home resolution (#19, #20) | explicit `--hook-path` not honored for Claude on Unix; failed home resolution silently writes `./.reasonix/settings.json` nothing reads | CONFIRMED→PR#229 | PR #229: absolutized + quoted embed (relative resolves against CC's cwd at hook time); hard error "pass --config" | A | post-#227 (588f78d) | 2026-06-11 |
| EXT-06 | `reducer.rs` `enter_delegating` (#4) | an out-of-dedup-window JSONL Task replay clobbers a Waiting parent | CONFIRMED→PR#232 | PR #232 (7bc2777): fire only on FIRST insert into `active_tasks`; sharp edges re-verified (#150 asymmetric dedup, `B1_CASCADE_GRACE`). NOTE: drain re-insert re-opened a lagged-replay hole → R0610-08 (PR #234 90s drained-tuid tombstone) | A | post-#227 (588f78d) | 2026-06-11 |
| EXT-07 | `hook/unix.rs` bind path (#11) | the Unix hook socket silently steals a live daemon's transport; the bail alternative kills the whole CC source + Reasonix (review MEDIUM on blast radius) | CONFIRMED→PR#232 | PR #232: connect probe → typed `SocketBusy` → transcript-only degradation + headless source-death surfacing. SUPERSEDED at the probe level by R0610-02 → PR #235 flock (see Notable flips) | A | post-#227 (588f78d) | 2026-06-11 |
| EXT-08 | `emit_first_sight` id derivation (#13) | hook- and JSONL-created slots carry different ids (Codex JSONL disambiguator was the constant `roll`) | CONFIRMED→PR#232 | PR #232: derive via the source's id deriver; tooltip disambig → 4-hex digest of the whole id (only shape-agnostic choice — rx cwd ids collide head AND tail) | A | post-#227 (588f78d) | 2026-06-11 |
| EXT-09 | `hook/unix.rs` socket perms (#17) | process-global umask dance races every other tokio worker's file creation | CONFIRMED→PR#232 | PR #232: temp-bind + chmod 0600 + atomic rename (+ `sun_path` length guard fallback); now a core CLAUDE.md hook/ sharp edge | A | post-#227 (588f78d) | 2026-06-11 |
| EXT-10 | resurrect-in-place correlation state (#21) | a leftover tuid in `active_tasks`/`gated_before_waiting`/`pending_b1_cascades` suppresses every hook event of the resurrected life forever | CONFIRMED→PR#232 | PR #232: evict on resurrect; `recent_proof_of_life` deliberately survives (process alive by definition). Core CLAUDE.md resurrect edge. Counterpart refutation: R0609-13 (see Notable flips) | A | post-#227 (588f78d) | 2026-06-11 |
| EXT-11 | (review-round, PR #232) 4 reviewer counter-findings | insert-gate breaks #222 seeding / negative-vouch drains / refused-desk orphans; rename drops the listener; uuid-v7 tails collide; resurrect-eviction breaks the b1 trade | REFUTED-wrong-premise | PR #232 body: "Refuted with full traces" — e.g. old children are already cascade-exited, never 30-min lingerers | A | post-#227 (588f78d) | 2026-06-11 |
| EXT-12 | `pixtuoid-hook` shim cluster (#26–#28 area) | non-UTF-8 argv panics (exit 101, visible to CC); near-1MiB payload stalls past the watchdog on Windows; spoofed inbound `_pixtuoid_source` mis-namespaces an AgentId | CONFIRMED→PR#231 | PR #231 (bae3541): `args_os` lossy; `STAMP_HEADROOM = 256`; unconditional inbound strip. Invariant #5 (always exit 0 silently) is the cited contract | A | post-#227 (588f78d) | 2026-06-11 |
| EXT-13 | CLI/runtime cluster (#5, #10, #14, #18 area) | `--max-desks 0` = permanently empty office; `ctrl_c` recreated per-iteration swallows a SIGINT; `--headless` help claims JSON; `--log-level` typo silently disables logging | CONFIRMED→PR#231 | PR #231: parse error + pre-altscreen warn; pinned future; help text; `ValueEnum` | A | post-#227 (588f78d) | 2026-06-11 |
| EXT-14 | CI/scripts/docs cluster (#29, #30, #34–#36 area) | smoke's `\|\| true` makes the invariant-#5 gate unfalsifiable; drift-script files junk issues on transient net errors; `just fuzz` "passes" on an empty corpus; gen-media validates jobs after the release build; 2 misleading code docs | CONFIRMED→PR#231 | PR #231: assert silence; transient bucket routing + exit 2; loud empty-corpus fail; early validation; physics/pathfind doc fixes | A | post-#227 (588f78d) | 2026-06-11 |
| EXT-15 | `layout` `push_desk` (#8) | at 34–66px buffer widths the forced pod's second desk column lands past the band edge / off-buffer (invisible desks, out-of-mask anchors) | CONFIRMED→PR#233 | PR #233 (8a7046e): x clamp mirroring the y clamp; degrade to fewer desks; capacity from real desk count. Twin clamps followed: R0610-11 (#237 aisle slots), #239→PR #240 (south edge) | A | post-#227 (588f78d) | 2026-06-11 |
| EXT-16 | tui motion (#22, #23, #24) | thinking-gate release teleports into mid-walk; snap-back speed from straight-line not A* length; `pick_aimless_dest` can return a blocked cell | CONFIRMED→PR#233 | PR #233: SeatedIdle for the cycle; route once at arm + measure polyline (leg-freeze #66/#68 pins untouched); walkable fallback + `home_desk` param (core break absorbed by the 0.7.0 window) | A | post-#227 (588f78d) | 2026-06-11 |
| EXT-17 | `evict_missing` (#25) | PoseHistory has NO eviction (one entry per AgentId ever rendered, forever); MotionState current-floor-only | CONFIRMED→PR#233 | PR #233: evict both across every floor. Overlaps R0609-06's MotionState half (PR #210) — the PoseHistory half was the net-new valid part at triage | A | post-#227 (588f78d) | 2026-06-11 |
| EXT-18 | whiteboard walkable strip (#31) | TopLeft south-strip blocked ground sat 2px west of the wheels (east wheel column walkable, bare floor blocked) | CONFIRMED→PR#233 | PR #233: center the narrower footprint; 2 walkable goldens regenerated, diff confined; `just gen-check` 29/29 | A | post-#227 (588f78d) | 2026-06-11 |
| EXT-19 | sprite format/painter guards (#32, #33) | `rows_to_frame` silently truncates past `u16::MAX`; `draw_dotted_hline` can overflow / not terminate on `dash=0,gap=0` | CONFIRMED→PR#233 | PR #233: bail on oversize; compute in u32; `blit_frame_outlined` halo contract documented | A | post-#227 (588f78d) | 2026-06-11 |
| EXT-20 | `.github/workflows/release.yml` (#215) | workflow-wide write perms + a tag at an arbitrary unreviewed commit publishes | CONFIRMED→PR#230 (closes #215) | PR #230 (0fb73fd): top-level `contents: read`, per-job escalation; tag-on-main ancestry check (`v*-win.N`/`-rc.N` exempt by design); OIDC prep inert until registry dashboards | A | post-#227 (588f78d) | 2026-06-11 |
| EXT-21 | (grouped) 4 findings already fixed at triage | external #1 (mid-attach visibility), #2+#9 (PR #210 class), #12 (liveness follow-up class) | CONFIRMED→PR#224 / PR#210 / PR#227 (pre-dated) | triage verdict: fixed by the liveness arc before the report landed — d1283aa, 77c5ef5, 588f78d | A | post-#227 (588f78d) | 2026-06-11 |

## 2026-06-10 — whole-codebase review @7bc2777 (16 finders → design-intent + code-trace verifier pairs) → PRs #234–#237, #240

Sources: maintainer memory (`project_codebase-review-2026-06-10`) + PR bodies.
42 candidates → 37 distinct → 25 confirmed (0 crit / 0 high / 6 MED / 19 low),
12 refuted (10 by design). All 25 shipped 2026-06-11 (#234 f4c06fd, #235
09471c8, #236 dcc9851, #237 1d4fa7d); residuals #238/#239 closed by #240
(8ed424c).

| # | seam (file/mechanism) | claim (1 line) | verdict | anchor (paths + cited sharp edge / fix PR) | tier | head | date |
|---|---|---|---|---|---|---|---|
| R0610-01 | `jsonl.rs:346` `emit_session_exit` / `ctx.live` (MED) | instant-exit never purges the dead id from the live admission set → a probe-failure (`None`) pass lets `revouch_gated_files` re-admit the just-ended session → cursor 0 → full-replay ghost SessionStart unreachable by every fast rung | CONFIRMED→PR#234 | PR #234 (f4c06fd): purge in `emit_session_exit` (one exit path); negative-vouch confirm immune by construction (runs after `live` is replaced) | A | 7bc2777 | 2026-06-10 |
| R0610-02 | `hook/unix.rs:38` liveness arbitration (MED) | backlog-saturated LIVE daemon yields ECONNREFUSED (macOS) / EAGAIN (Linux) → #232's errno probe steals the live socket / dies fatally | CONFIRMED→PR#235 | PR #235 (09471c8): flock on sibling `<sock>.lock` for daemon lifetime (also closes the rename TOCTOU); kernel note in `pixtuoid-hook/tests/shim.rs`; now the core CLAUDE.md hook/ sharp edge, mixed-version lock-less steal = documented residual. See Notable flips | A | 7bc2777 | 2026-06-10 |
| R0610-03 | `hook/windows.rs:115` (MED) | SocketBusy degradation is Unix-only — Windows ACCESS_DENIED stays fatal, second instance kills the whole CC source incl. watcher | CONFIRMED→PR#235 | PR #235: map ACCESS_DENIED → typed `SocketBusy` → same transcript-only degradation; cfg(windows) twin in `tests/transport/pipe.rs` | A | 7bc2777 | 2026-06-10 |
| R0610-04 | `crates/pixtuoid-core/CLAUDE.md:21` hook/ entry (MED) | doc claims a busy socket is "surfaced via SourceDeath" while #232's shipped code degrades transcript-only with NO SourceDeath — an agent could "fix" the degradation away to match the doc | CONFIRMED→PR#235 | PR #235: entry rewritten to shipped behavior (docs are load-bearing here — design-intent skeptics refute against them) | A | 7bc2777 | 2026-06-10 |
| R0610-05 | `tui/pathfind.rs:74` `AStarRouter` (MED) | aimless wander (fresh random dest/cycle) + snap-back from live interpolated origins insert ever-new (from,to) keys — no cap, no evict hook, linear retain scan | CONFIRMED→PR#237 | PR #237 (1d4fa7d): `PATH_CACHE_CAP = 512`, clear-on-exceed; mid-leg clear safe because legs are frozen on `walk_path` (workspace CLAUDE.md sharp edge: A* polyline frozen once per leg) | A | 7bc2777 | 2026-06-10 |
| R0610-06 | `tests/transport/socket.rs:79` (MED) | `listener_drops_slow_connection_via_timeout` (+ pipe twin) passes with `CONN_TIMEOUT` deleted — the semaphore already prevents accept-blocking | CONFIRMED→PR#235 | PR #235: assert EOF on the slow conn; mutation-verified (red with the timeout wrapper removed) | A | 7bc2777 | 2026-06-10 |
| R0610-07 | (low theme) liveness edges: `jsonl.rs:608`, `codex.rs:300`, `jsonl.rs:622` | pid-rebind emits a wrong instant SessionEnd for a live resumed session; `CODEX_HOME` users silently lose the whole liveness ladder; watcher goes silently blind on an unreadable root | CONFIRMED→PR#234 | PR #234: unbind-before-insert pid migration; `codex_probe_root` honors resolved `codex_home()`; `FailureLatch` once-per-state-change warn + recovery log | A | 7bc2777 | 2026-06-10 |
| R0610-08 | (low theme) reducer/scope: `reducer.rs:955`, `scope.rs:106` | lagged Task-pair replay re-fires `enter_delegating` (re-opens the gate #232 closed — drained tuid re-insert counts as "first"); `parent_id` cycle = immortal self-exempting slots | CONFIRMED→PR#234 | PR #234: 90s drained-tuid tombstone (tuids never legitimately re-dispatch); `has_ancestor_where` seeds visited with the start node. Mutual-2-cycle residual → R0610-15 | A | 7bc2777 | 2026-06-10 |
| R0610-09 | (low theme) config/install env contracts | empty `PIXTUOID_SOCKET` honored verbatim; empty `XDG_CONFIG_HOME` → CWD-relative config; `PIXTUOID_HOOK` bypasses the `--hook-path` absolutize arm; `ConfigLock` re-resolves symlinks mid-round; `CMD_UNSAFE` misses tab `;` `,` `=`; drive-relative `C:foo.exe` silently no-ops | CONFIRMED→PR#235 + PR#236 | PR #235 (socket env, both shim+daemon, #172 empty=unset policy); PR #236 (dcc9851): XDG empty=unset, env flows through the flag arm + embeds like it `(path, explicit)`, `ConfigLock.read()/backup_once()` pinned to lock-time resolution, delimiter set, hard error; ONE trim-based `io::nonempty` helper | A | 7bc2777 | 2026-06-10 |
| R0610-10 | (low theme) runtime/TUI signals | no SIGINT/SIGTERM altscreen teardown (`tui/mod.rs:232` — external kill leaves raw mode + mouse reporting); headless `ctrl_c` Err arm silently exits 0 | CONFIRMED→PR#236 | PR #236: signals ride the frame-pacing select → `teardown_terminal`; Err arm logs + disarms via pending-future swap (`headless_loop_with_signal` seam) | A | 7bc2777 | 2026-06-10 |
| R0610-11 | (low theme) TUI/layout visuals: `hud.rs`, `layout/compute.rs` | elevator HUD centers/clips by byte length (multi-byte ▲/▼ shift it 2 cells); pod-decor horizontal-aisle slot lands off-buffer at 34–41px widths | CONFIRMED→PR#237 | PR #237: `chars().count()` (the PR #210 convention; baselines regenerated in-PR, 632px/0.04% delta); `push_slot` skip past `band_right` — mirror of #233's `push_desk` clamp | A | 7bc2777 | 2026-06-10 |
| R0610-12 | (low theme) test quality + docs drift ×3 | degradation test rides real FSEvents (nondeterministic); workspace CLAUDE.md claims snap-back is time-compressed (it runs pure physics); ARCHITECTURE.md misses `decode_hook_payload`'s `Vec<AgentEvent>`/Identity prepend + claims Unix-socket-only | CONFIRMED→PR#235 + PR#237 | PR #235: documented polling seam; PR #237: snap-back edge corrected (verified in `pose/mod.rs` first), ARCHITECTURE.md + README transport split | A | 7bc2777 | 2026-06-10 |
| R0610-13 | hook socket location `/tmp/pixtuoid-<uid>.sock` | socket in world-writable `/tmp` is a vulnerability | REFUTED-design | security boundary is the socket itself, not the dir: bound at a temp name, chmod 0600, atomic rename (core CLAUDE.md hook/ edge; `pixtuoid-hook/src/paths.rs` — uid-suffixed, `XDG_RUNTIME_DIR` preferred when set) | A | 7bc2777 | 2026-06-10 |
| R0610-14 | `hook/unix.rs::run` accept loop | accept-error arm (warn + continue) hot-spins under EMFILE | REFUTED-design | memory records "refuted by design"; mechanism reconstructed at backfill: semaphore permit acquired BEFORE `accept()` bounds in-flight conns (`MAX_CONCURRENT_CONNS`), so the listener can't exhaust its own FDs — re-verify against the cited code, not this note | B | 7bc2777 | 2026-06-10 |
| R0610-15 | (grouped) remaining 10 refuted candidates | various (8 more by-design + 2 other) | REFUTED-design / REFUTED-wrong-premise (grouped) | killed against CLAUDE.md sharp edges per the review record; individual claims not preserved | B | 7bc2777 | 2026-06-10 |
| R0610-16 | `scope.rs` mutual-Waiting 2-cycle (residual from #234's review) | a mutual-Waiting `parent_id` 2-cycle still mutually self-exempts from `sweep_stale` (immortal pair) | CONFIRMED→#238→PR#240 | PR #240 (8ed424c): `scope::would_create_cycle` filters at the reducer's ONE `parent_id` write seam (registration + orphan enrichment; Codex SubagentStart flows through it) — prevention over detection, sweeps need no cycle awareness | A | f4c06fd | 2026-06-11 |
| R0610-17 | `layout` pod-decor south edge (found in #237's review, pre-existing) | south decor visual crosses the cubicle-band bottom into the walkway on tall floors (repro 200×116 seed 2) | CONFIRMED→#239→PR#240 | PR #240: `push_slot` vertical twin of #237's east clamp. Premise corrected en route: issue said "south-anchored" but that described the FOOTPRINT — the visual blit is CENTER-anchored; clamp derived from real blit math (footprint ⊆ visual) | A | 1d4fa7d | 2026-06-11 |
| R0610-18 | (fix-arc bot round, PR #236) `update_config` read path | `update_config`'s read should go through the lock-pinned `ConfigLock::read` (the one REAL bot finding of the 4-PR round) | CONFIRMED→PR#236 | PR #236 review round; #234/#237 bot rounds clean, one #235 bot finding self-refuted (claim not preserved) | B | 7bc2777 | 2026-06-11 |

## 2026-06-11 — subagent-lifecycle arc adjudications (#241–#250)

Sources: maintainer memory (`project_cc-subagent-hooks-gap`) + PR bodies
#243/#245/#248/#249/#250. These are review-round and issue adjudications made
during the arc, including upstream-verified premise corrections.

| # | seam (file/mechanism) | claim (1 line) | verdict | anchor (paths + cited sharp edge / fix PR) | tier | head | date |
|---|---|---|---|---|---|---|---|
| SUB-01 | `decoder.rs:717` CC SubagentStart/Stop narrowing | Workflow-fleet subagents have NO clean exit: b1 keys on `Agent` tool_use drain (a fleet is ONE `Workflow` call), no transcript end marker, hooks Codex-narrowed (pre-Workflow YAGNI) → batch sweep-reaps hold every desk | CONFIRMED→#241→PR#243 | PR #243 (e2736c5): `decode_cc_hook_custom` — Start→instant SessionStart (`agent-<bare_id>` + parent link), Stop→SessionEnd via `cc_id_from_path(agent_transcript_path)`; install EVENTS=7; drift watcher diffs the hooks.md event LIST. Verified: 7/7 real-transcript replay lab + 269,804-line fuzz | A | post-#240 (8ed424c) | 2026-06-11 |
| SUB-02 | `Workflow` in `make_tool_detail` | (fix-shape adjudication) mapping `Workflow`→`ToolDetail::Task` would fix the fleet exits | REFUTED-design | deliberately NOT mapped — the vouched-Delegating subtree shield would sweep-EXEMPT finished fleet agents for the workflow's whole life (worse starvation); WHY at `make_tool_detail` + workspace CLAUDE.md sharp edge ("Workflow … deliberately NOT mapped") | A | post-#240 (8ed424c) | 2026-06-11 |
| SUB-03 | (bot MEDIUM, PR #243 review) `agent_transcript_path` as slot key | keying SessionEnd on an untrusted hook-supplied path is a new spoof surface | REFUTED-wrong-premise | the same capability always existed via plain SessionEnd forgery on the 0600-mode socket — no NEW surface (socket perms edge: core CLAUDE.md hook/ entry); recorded in the arc memory | B | e2736c5 | 2026-06-11 |
| SUB-04 | hook reorder, CC + Codex twins | `SubagentStop` decoded BEFORE its `SubagentStart` mints a slot whose end already passed → lingers to the 10/30-min sweeps (tombstone only gated Activity synthesis) | CONFIRMED→#242→PR#245 | PR #245 (04d0ab5): one reducer SessionStart gate — `parent_id.is_some() && !slot_exists && tombstoned` → skip; child-scoped so Reasonix's documented parentless SessionEnd→SessionStart resurrect is exempt by construction; 5s TTL-bounded | A | e2736c5 | 2026-06-11 |
| SUB-05 | residual windows characterized in #245's review | Codex parentless first-sight bypasses the gate; known-id post-GC late discovery re-registers; post-TTL registration | ACCEPTED-residual→#244→PR#249 | PR #249 (17347a4): child ledger (`as_child` stamp on SessionEnd — only the two SubagentStop arms; ledger {applied parent, ended_at, 90s TTL}; parented-start skip closes w2, parentless adopt-don't-block re-link improves w1); post-TTL stays sweep-owned by design | A | 04d0ab5 | 2026-06-11 |
| SUB-06 | Codex multi-turn child re-registration (#246) | child fires SubagentStop per TURN → ended after turn 1; re-registration loses the parent link; NO SessionStart carrier exists at turn N+1 on either transport | CONFIRMED→PR#249 + PR#250 | PR #249 re-link + PR #250 (deb5d77) un-claim side-channel (tee in `ClaudeCodeSource::run`, watcher drains own claims, stragglers to EOF first per #228). KEY premise correction in #249's review: a live codex-rs source check CORRECTED the PR's original multi-turn claim pre-merge. KEY interaction: release = `seen→false` NOT removal — the FD probe vouches a LIVE child's open rollout; removal triggers `revouch_gated_files` full-replay (pinned: `released_claim_is_not_revouched_into_a_full_replay`) | A | 17347a4 | 2026-06-11 |
| SUB-07 | `sessions/<pid>.json` registry consumption (#247) | the probe's one undocumented upstream dependency has no drift detector — a silent key rename degrades mid-attach liveness to mtime-only with zero signal | CONFIRMED→PR#248 | PR #248 (5ec866b): `RegistryParse` Entry/Skip/`ShapeDrift(key)` + warn-once naming the vanished key; 13-key shape pin. Fix-approach "extend `check_upstream_drift.py`" REFUTED-design in the same PR: the script is strictly fetch-and-diff (no local state; CI has no registry) and the consumer warn is strictly faster | A | deb5d77 | 2026-06-11 |
| SUB-08 | (grouped) arc accepted residuals, WHY-documented in code | bytes between Stop and drain consumed silently (next append revives); revival inside the 4.5s exit grace swallowed (turn-N+2 re-arms); exit racing the side-channel paints a ≤4.5s self-correcting walkout ghost; Identity-arm (5s,90s] parentless straggler self-heals via ledger adoption | ACCEPTED-residual | PR #250 body + arc memory; core CLAUDE.md child-ledger sharp edge (clean-exit ladder entry) — each residual carries an in-code WHY | B | deb5d77 | 2026-06-11 |

## 2026-06-11/12 — module-geometry residuals (#252/#254) → PR #256

Source: PR #256 body (the #240 precedent: residuals batched in one PR).

| # | seam (file/mechanism) | claim (1 line) | verdict | anchor (paths + cited sharp edge / fix PR) | tier | head | date |
|---|---|---|---|---|---|---|---|
| GEO-01 | `live_cc_session_ids` registry scan (#252) | duplicate sessionId across two live registry entries binds id→pid by unspecified `read_dir` order — binding flaps across refreshes, churns exit-watch rebinds, and a losing pid's death emits a spurious SessionEnd for a live session | CONFIRMED→PR#256 | PR #256 (5e26cfb): fold into a winners map — newest `startedAt` > has-`startedAt` > larger pid + warn-once. The issue's own "keep the first" fix sketch REFUTED-wrong-premise (itself scan-order-dependent); winner rule proven a strict total order | A | post-#255 (f353350) | 2026-06-12 |
| GEO-02 | `codex.rs::rollout_ids_from_paths` (review-round sibling) | same last-writer-wins shape for two live processes holding one rollout (resume overlap) | CONFIRMED→PR#256 | PR #256 reviewer-2 finding: same larger-pid rule, pinned in both enumeration orders | A | post-#255 (f353350) | 2026-06-12 |
| GEO-03 | `tests/reducer/liveness.rs` unknown_cwd twins (#254) | `unknown_cwd_agent_uses_faster_stale_timeout` is a ~90% duplicate of `unknown_cwd_agent_reaps_faster` | CONFIRMED→PR#256 | PR #256: merged keeping BOTH distinguishing assertions (`unknown_cwd` flag + ghost-`#N` label); reviewer diffed both at base — only incidentals differed (couldn't merge inside move-only #255 without breaking its byte-identical property) | A | post-#255 (f353350) | 2026-06-12 |

## 2026-06-12 — Phase-2 A/B experiment @ a8aaae9 (source/ module) → issue #262

Source: the ledger's first controlled A/B run (its control arm doubles as
the ledger-blind calibration pass — protocol step 8)
([`phase2-ab-2026-06.md`](review-metrics/phase2-ab-2026-06.md), workflow
`wf_04e5f98b-735`): every candidate adjudicated twice — a full skeptic+trace
pair in the control arm, and the ledger-routed treatment arm (full pair for
the 9 unmatched candidates; ONE cheap regression-checker for routed C08).
The run also re-executed the regression-check on R0610-01 +
SUB-01/04/05/06/07 + GEO-01/02 — all fix mechanisms verified present at
a8aaae9 by each of C08's three adjudications (control pair + the routed
cheap check). Three 2-2 split candidates
(inline blocking probe enumeration; codex name-only vouch forgeability; the
un-claim "emits NOTHING" test-artifact claim) are NOT adjudicated — listed in
#262 for the next whole-codebase review to re-derive.

| # | seam (file/mechanism) | claim (1 line) | verdict | anchor (paths + cited sharp edge / fix PR) | tier | head | date |
|---|---|---|---|---|---|---|---|
| R0612-01 | `jsonl/walk.rs:127-128` probe-bypass WHY comment | "CC (the only probe user)" is stale — Codex wires `with_liveness_probe` since #220/#227 (`codex.rs:383-386`); the bypass's safety silently rests on `codex_session_ended` being constant-false | CONFIRMED→PR#272 | walk.rs first-sight gate comment vs codex.rs:383-386; the oversized branch (walk.rs:163-167) already states it correctly | B | a8aaae9 | 2026-06-12 |
| R0612-02 | `hook/unix.rs:172` + `hook/windows.rs:195` CONN_TIMEOUT wrap | timeout cancellation mid-`tx.send()` under >1s back-pressure drops the rest of a decoded payload with no warn breadcrumb (`let _ =` swallows Elapsed) | CONFIRMED→PR#272 | handle_conn's per-event send inside the timeout scope (hook/mod.rs:101); hooks best-effort end-to-end bounds impact — breadcrumb is the actionable part | B | a8aaae9 | 2026-06-12 |
| R0612-03 | `jsonl/walk.rs:90-101` walk_jsonl dir recursion | `tokio::fs::metadata` follows symlinks + unbounded `Box::pin` recursion — symlink loop recurses unbounded; out-of-root symlink walks foreign `.jsonl` (precondition: planted in the user's own root) | CONFIRMED→PR#272 | no `symlink_metadata`/visited-set anywhere in walk.rs; first-sight gate limits registration impact | B | a8aaae9 | 2026-06-12 |
| R0612-04 | `jsonl/walk.rs:138-147` truncation reset × exit-path drains | transcript truncated below cursor at the moment a negative-vouch/instant-exit/un-claim drain runs interacts with the #228 drain-before-unclaim discipline | CONFIRMED→PR#272 | truncation reset arm vs liveness.rs:212-248 drain ordering | B | a8aaae9 | 2026-06-12 |
| R0612-05 | `cc_probe.rs:303` pid_alive on unreaped zombie | healthy snapshot in a zombie window re-vouches a just-ended id → retired transcript replays as a phantom — self-healing within ~one loop turn (second exit synthesizes via ESRCH receipt / immediately-readable pidfd) | ACCEPTED-residual | exit arm purge (jsonl/mod.rs:375-389) + watch(pid) re-end path; rare ms-scale window, cosmetic burst | B | a8aaae9 | 2026-06-12 |
| R0612-06 | decode-boundary content strings (Notification reason, Rename label, Generic tool display) | "content-derived strings reach display uncapped" — display half REFUTED (tooltip 34-char cap, ticker 512, ratatui cell clipping bound every TUI site); headless-summary egress + AgentSlot lifetime retention CONFIRMED | CONFIRMED-partial→PR#272 | shared char-safe `ellipsize` at `MAX_DECODED_FIELD_CHARS=80` across five decode sites (incl. the two siblings the review round added: `cwd_basename_label`, Generic `tool_name`); correlation keys deliberately uncapped (AgentId hashes the full string) | A | 0a2e387 | 2026-06-12 |
| R0613-01 | `source/codewhale.rs` AgentId key (cwd vs session_id) | CodeWhale source keys on `cwd`, not `session_id` — VERIFIED real, not arbitrary: a live capture proved `DEEPSEEK_SESSION_ID` is inconsistent within one session (`sess_<8hex>` on session/turn/tool-after, a raw turn UUID on `tool_call_before` — distinct engine code path), so session_id keying would split every ActivityStart into a 2nd ghost sprite; `DEEPSEEK_WORKSPACE` is identical on every event | VERIFIED (3-lens review, correctness c95) | `codewhale.rs:107-112` keys on cwd; shim `env_payload` never folds session_id; the 2026-06-12 capture (session_id-inconsistency) is the anchor — do NOT "improve" to session_id without re-capturing | A | feat/codewhale-source | 2026-06-13 |
| R0613-02 | `install/codewhale.rs:199-202` uninstall `ours_only` drop | a user's pre-existing LONE `[hooks] enabled = false` (no hooks) is not restored on uninstall — install force-set it true | ACCEPTED-residual | install is an explicit opt-in to visualization (silent-non-fire is the worse outcome) and `enabled` is moot with no hooks, so the only loss is a no-op line; faithful restore would need install to record the flip — not worth the state. WHY documented in the uninstall comment | B | feat/codewhale-source | 2026-06-13 |
| R0613-03 | `pixtuoid-hook/src/main.rs` `env_payload_from` | env-mode folds `DEEPSEEK_TOOL_ARGS`/`WORKSPACE`/`TOOL_NAME` verbatim with NO size cap, unlike the stdin arm's `STDIN_CAP` — a large tool input (a big write/edit) builds a >1 MiB line the 200ms watchdog drops, losing that `tool_call_before` (invariant #5 holds, but the "working" pulse is lost). NOT pure defense-in-depth — a real event-drop for common large edits | CONFIRMED→fixed (PR#275) | per-field `ENV_FIELD_CAP=128 KiB` byte-bounded char-safe truncation (`cap_env_field`); 3×128 KiB ≪ 1 MiB restores the stdin arm's pipe-quota guarantee for env-mode; pinned by `env_payload_caps_oversized_fields_at_a_char_boundary`. **Caught by the PR claude-review bot — MISSED by this PR's own 3-lens review** (its correctness lens checked env reads are panic-free but not the cap-vs-stdin-arm asymmetry) | B | feat/codewhale-source | 2026-06-13 |
| R0613-04 | `install/mod.rs:17,57,92` target-list hints | `NO_CLIS_MSG` + the `--config` single-target conflict + the multi-CLI conflict all listed `claude\|codex\|reasonix[\|all]`, omitting the newly-added `codewhale` — a user whose only CLI is CodeWhale gets a hint that never names `--target codewhale` | CONFIRMED→fixed (PR#275) | added `codewhale` to all THREE strings (bot flagged only line 17; the other two are the same staleness class) | B | feat/codewhale-source | 2026-06-13 |
| R0613-05 | `pixtuoid-hook/src/main.rs` `env_payload` cwd | env-mode keyed cwd SOLELY on `DEEPSEEK_WORKSPACE`, which is UNSET for a fresh `codewhale` launched without `-C` (workspace is `None` at session_start until it resolves) — the shim sent a cwd-less envelope, the decoder dropped it (no AgentId key), and NO sprite appeared. **A real bug** — not defense-in-depth | CONFIRMED→fixed (PR#275) | fall back to the hook child's `current_dir()` (CodeWhale runs the hook with current_dir = its working dir = the workspace; the fallback resolves to the same path the workspace eventually does, so events coalesce). **Caught ONLY by live user testing** — the `-C` capture, the unit tests, the 3-lens pre-push review AND the PR bot review all masked it (the capture forced a workspace via `-C`). The lesson: a hook-only source's identity field must be probed WITHOUT the convenience flags a capture rig adds. Pinned by `env_payload_falls_back_to_cwd_when_workspace_unset` | A | feat/codewhale-source | 2026-06-13 |
| R0613-06 | `source/hook/pid_watch.rs` + shim `_pid` | abrupt CodeWhale exit (SIGKILL/crash/terminal-close) fires no `session_end` → the sprite ghosts 10–30 min (the hook-only-source gap, shared with Reasonix; the JSONL ladder's negative-vouch/instant-exit don't apply — no watcher) | DONE (PR#275) | NOT deferred (the user's no-phasing rule): the shim stamps CodeWhale's pid (`_pid` via getppid — `sh -c` exec's the hook, Unix only; Windows `cmd /C` parent is the wrong pid → omitted) and `HookPidWatch` reuses the standalone `ExitWatch` (kqueue/pidfd) to emit SessionEnd on pid death — a STANDALONE add that never touches the JSONL liveness ladder. False pid (a forking shell) self-heals via the resurrect path. End-to-end pinned by `killing_a_watched_pid_emits_session_end_for_its_agent` (macOS-local/Linux-CI, like the JSONL instant-exit) + registry unit tests | A | feat/codewhale-source | 2026-06-13 |
| R0613-07 | `tui/theme/catppuccin.rs` codewhale hue + `theme/mod.rs` legibility guard | catppuccin's codex (sky `#89dceb`) and codewhale (teal `#94e2d5`) are Manhattan-distance **39** and near-identical luminance (592 vs 587) — perceptually one color at 2-char badge scale; the `source_badges_legible_for_every_theme` guard only asserted exact RGB inequality, so the near-collision passed | CONFIRMED→fixed (PR#275, whole-PR re-review) | catppuccin codewhale → deep teal `#33c0ac` (min pairwise dist 39→110, consistent with the deep-teal codewhale the other 5 themes use); the guard gained a `MIN_SOURCE_HUE_DIST=60` **Manhattan** floor — lum-sum can't see this class (catppuccin's pair is lum-equal), and the tightest legit pair is 82 (normal codex-vs-cw) so 60 keeps margin. Mechanizes the invariant (cf. #273). NIT not defect — `dashboard_line` draws the `[cw]` text label as the primary disambiguator — but fixed anyway per future-proof→do-it. Found by the install-config-platform lens (c80) | B | feat/codewhale-source | 2026-06-13 |
| R0613-08 | `source/codewhale.rs` `decode_cw_subagent` parent_id ↔ shim cwd-fallback | the subagent→parent link resolves only if the child's `workspace` string byte-matches the parent's shim-resolved cwd; under a no-`-C` launch the parent cwd comes from `current_dir()` (R0613-05) while the child carries CodeWhale's internally-resolved `workspace` (NOT through the shim) — they COULD diverge (trailing slash / symlink / abs-vs-rel), pointing parent_id at a non-existent slot | VERIFIED→#276 CLOSED | the user surfaced a REAL captured subagent run (`~/dotfiles/.deepseek/state/subagents.v1.json`: child `agent_ad945f4c`, workspace `/Users/navepnow/dotfiles`). Upstream `ui.rs::execute_subagent_observer_hook` emits `workspace = context.workspace.display()` (the same `App.workspace` that backs `DEEPSEEK_WORKSPACE`), and `/Users/navepnow/dotfiles` is not a symlink (`realpath` == literal) so the shim's `current_dir()` == that string → **byte-match holds**. Child id is `agent_<hex>` (never collides with a `/`-path). Pinned by `real_codewhale_subagent_payload_nests_the_child_under_its_workspace_parent` (decodes the FULL upstream payload — 7+ fields — through `decode_hook_payload` → reducer, asserts the child nests). Residual (a workspace that IS a symlink where CodeWhale canonicalizes but `current_dir()` doesn't) degrades gracefully to an un-nested sprite — not a tracked defect. Found by the correctness lens (c55) | A | feat/codewhale-source | 2026-06-13 |

> **Whole-PR re-review @ a716c5f (PR#275, 2026-06-12→13):** a 4-lens fan-out
> (correctness/grounding · design/blast-radius/semver ·
> shim-transport-never-panic+concurrency · install-config-write+cross-platform)
> over the full diff, each MEDIUM+ finding adversarially verified. Result:
> **0 CRITICAL / 0 HIGH / 0 MEDIUM**, all lenses APPROVE / APPROVE-WITH-NITS.
> Five of the eight findings were pure verification traces (no change). Two NITs
> fixed in-PR (R0613-07 + a stale `// our 5`→`every managed CodeWhale event`
> comment); one LOW deferred with proof-of-impossibility (R0613-08 → #276). The
> shim never-panic / `_pid`-peek-inert / no-Mutex-across-await / Arc<ExitWatch>
> lifetime claims were all independently re-derived and held.

## 2026-06-13 — opencode integration review @ 6183494 (feat/opencode-source) → PR pending

A 4-lens fan-out (correctness/grounding · design/blast-radius/semver ·
install+TS-plugin-artifact+cross-platform · liveness/lifecycle) over the whole
opencode diff, each MEDIUM+ adversarially verified. opencode is the 6th source —
HOOK-ONLY via a bundled TS plugin (the FIRST install target that ships CODE). The
event SHAPES are grounded in the cloned upstream source + the real persisted
`opencode.db` event log (a free-model self-run; the full tool+subagent LIVE
capture is confirm-before-merge, blocked on the throttled free tier). Result:
**0 CRITICAL / 0 HIGH / 1 MEDIUM (held)**, all lenses APPROVE-WITH-NITS; all
findings fixed in-PR.

| # | seam (file/mechanism) | claim (1 line) | verdict | anchor (paths + fix) | tier | head | date |
|---|---|---|---|---|---|---|---|
| R0613-09 | `hook/mod.rs` pid-bind only on `SessionStart` × opencode `resurrects_on_prompt:false` | a MID-ATTACHED opencode session registers via the prepended `Identity` (opencode emits `session.created` once per session, never re-emits it), so `HookPidWatch.note` — fired only on `SessionStart` — never binds its pid; on an abrupt TUI quit (no `session.deleted` either) the sprite ghosts to the 30-min sweep. Invisible for CodeWhale (its `message_submit` re-emits `SessionStart` every prompt) — genuinely new for opencode | CONFIRMED→fixed | bind on `Identity` too via the new pure `pid_bind_target(ev)` (SessionStart \| Identity) — the `_pid` is already on every forwarded event; `note` is idempotent per (pid,agent). Also hardens CodeWhale mid-attach. Pinned by `pid_bind_target_covers_both_registration_carriers`. Found by the liveness lens (c88), adversarially verified holds=true | A | 6183494 | 2026-06-13 |
| R0613-10 | `install/opencode_plugin.ts` tool-part gate + `source/opencode.rs` running→ActivityStart | opencode re-publishes a tool part with status STILL `running` on every output chunk (streaming `bash`/`task`, upstream `prompt.ts`), so the plugin forwarded one shim-spawn PER chunk and the reducer's `tool_call_count` over-counted (no per-callID dedup under the single Hook transport) — contradicting the "one connection per tool-state change" claim | CONFIRMED→fixed | the plugin now tracks last-seen status per `callID` (a `Map`, freed on terminal status) and forwards a tool part only on a status TRANSITION — collapses the spawn burst AND fixes the count. Grounded in upstream source (not schema-inference). Found by the correctness lens (c78) | B | 6183494 | 2026-06-13 |
| R0613-11 | `source/opencode.rs` `decode_permission` reason keys | the reason was read from `title`/`pattern`/`type`/`tool`/`permission`, but the real upstream `permission.v2.asked` schema (`Request.fields` = `{sessionID, action, resources, …}`) carries NONE of them — so Waiting reason ALWAYS degraded to the literal `"permission"` (the synthetic fixture's `title` never occurs upstream) | CONFIRMED→fixed | key order is now `["action","permission",…]` (the v2 verb + v1 name first); the conformance fixture uses the real `{sessionID, action, resources}` v2 shape (snapshot reason now `bash`); tests exercise both v2 `action` and v1 `permission`. Found by the design + correctness lenses (c80) | B | 6183494 | 2026-06-13 |
| R0613-12 | `source/opencode.rs` module doc | the module header still described the SUPERSEDED install design ("registers it in `~/.config/opencode/opencode.jsonc`") — the install actually DROPS a plugin file in the auto-discovered `<config>/plugin/` dir, no jsonc edit | CONFIRMED→fixed | header corrected to the plugin-file-drop design. Plus two NIT doc/comment fixes (the `handle_conn` `_pid`-peek comment named only CodeWhale → CodeWhale+opencode; the `render_plugin` JSON⊂JS-string caveat re U+2028/U+2029, sound for filesystem paths). Found by the install lens (c96) | B | 6183494 | 2026-06-13 |
| R0613-14 | `install/opencode.rs` `default_config_path` plugin DIR name | installed to `<config>/plugin/` (SINGULAR) — taken from the anomalyco fork's permissive `{plugin,plugins}` source glob (so it loaded on the fork, live-proven) — but the CANONICAL opencode docs (opencode.ai/docs/plugins) auto-discover `<config>/plugins/` (PLURAL); canonical opencode would NOT load a singular-dir plugin → a silent no-op for non-fork users | CONFIRMED→fixed | `default_config_path` → `plugins/` (plural): works on the fork (brace glob) AND canonical. Re-verified live (plugin loads from `plugins/`, `oc·` sprite renders). **Found when the user asked "did you verify the official docs first?"** — I'd worked from the fork SOURCE + the real event log + a live e2e, NOT the docs; reading them surfaced the singular/plural divergence. LESSON: read the canonical docs BEFORE reverse-engineering a fork's (more-permissive) source — the fork's brace glob masked a portability bug | A | (post-2941cbb) | 2026-06-13 |
| R0613-13 | `install/opencode.rs` `detect_installed` (the `presence_probe`) | the auto-detect probe keyed on OUR plugin's `@pixtuoid-opencode-plugin` sentinel — so a bare `pixtuoid install-hooks` (auto-detect) would NEVER offer opencode until AFTER our plugin was already installed (chicken-and-egg); the opencode CLI itself was never detected. Every other source's `presence_probe` detects the CLI, not our artifact | CONFIRMED→fixed | `detect_installed` now probes opencode's OWN dirs (`<config>` + `~/.local/share/opencode`), like Reasonix/CodeWhale. `config_present`/uninstall use FILE-existence (unaffected) and `merge_uninstall` keys on the sentinel, so removal stays exact. **Found POST-merge-claim while the user asked "did we install the plugin?"** — the 4-lens install lens checked the sentinel logic but conflated "our plugin installed" with "the CLI is present." Reinforces R0613-05: for a hook-only source, real-use/live checks catch what code review (and green CI) miss — the case AGAINST merging on green alone | A | 722daac | 2026-06-13 |
| R0613-15 | `install/opencode_plugin.ts` `lastToolStatus` dedup-map key | the streaming-dedup Map is MODULE-LEVEL (one plugin process serves a parent AND its `task`-spawned child session concurrently) and was keyed on `callID` ALONE — two concurrent sessions sharing a `callID` would drop the wrong session's tool event | CONFIRMED→fixed (claude[bot] PR review, MEDIUM, flagged twice) | key on `sessionID:callID` (composite) — eliminates cross-session collision; entries still freed on terminal status. PLUS the bot's informational dead-code: `render_plugin`'s `serde_json::to_string(&str).unwrap_or_else("")` is infallible-but-if-hit-deploys-a-broken-`HOOK_PATH=""` → now propagates via `Result`. Re-verified live (composite-key plugin loads, `oc·` sprite renders; a 0-event run in between was a TEST-harness socket-reclaim race from a leftover `/tmp` socket — a 2nd headless instance over a stale socket degrades to transcript-only by design, not a product bug) | B | (post-29b90ae) | 2026-06-13 |

## 2026-06-13 — Connection panel + borderless popups review @ ad1655b (feat/status-panel-borderless-popups) → PR pending

A 3-lens fan-out (correctness/grounding · design/blast-radius · film-critic on
the regenerated popup stills), each MEDIUM+ adversarially verified (0 refuted).
The feature: an in-TUI Connection panel (`c`) unifying hook install/uninstall + live
connection, plus a cross-cutting conversion of EVERY popup to a borderless padded
panel. Result: **1 CRITICAL / 0 HIGH / 1 MEDIUM / 2 NITs**, two lenses
REQUEST-CHANGES; all fixed in the review-round commit (1e63ec1). The borderless
geometry / version-popup click-rect / install-core byte-identity / facet-lifecycle
claims were independently re-derived and held. NITs (both FIXED): a stale
"bordered" module doc in `widgets/dashboard.rs`; a one-line doc on
`ConnectionUi.selected` (usize-index soundness).

| # | seam (file/mechanism) | claim (1 line) | verdict | anchor (paths + fix) | tier | head | date |
|---|---|---|---|---|---|---|---|
| R0613-16 | `tui/connection/mod.rs::build_rows` registry→target join via `install::target::by_name(d.name)` | the REGISTRY claude id is `claude_code::SOURCE_NAME = "claude-code"` but the install target name is `"claude"`, so `by_name("claude-code")` → None → the flagship CLI rendered as a non-actionable `JsonlNoHooks` row (i/u no-op); masked because the unit tests hand-fed `source_id:"claude"` and the snapshot fixture hardcoded the row. The impl-plan ASSUMED `SourceDescriptor.name == Target.name` (false for Claude) — a plan-miss | CONFIRMED→fixed | joined `build_rows` on the existing `Target.core_source` (added by #282) via a new `target::by_source` (my branch predated #282 so I re-derived core_source; the rebase dedups to #282 field + my by_source). Mechanized by `every_target_names_a_registered_source` (target.rs) + `build_rows_makes_every_source_with_a_target_actionable` (connection/tests.rs) — both fail on any future registry/target id drift. Found independently by the correctness AND design lenses (conf 96) | A | ad1655b | 2026-06-13 |
| R0613-17 | `scripts/media.json` — no `connection` render job | the borderless Connection panel shipped with zero rendered CI baseline (only `snapshot --connection` existed, unwired), so a visual regression of the new flagship panel would pass gen-check silently | CONFIRMED→fixed | added a `connection` render job (`snapshot --connection` → `docs/images/connection.png`, gen-check-gated), mirroring `dashboard`; baseline committed. Also moved the fixture's first row id `"claude"`→`"claude-code"` (production source id). Found by the design + film-critic lenses (conf 78) | A | ad1655b | 2026-06-13 |

## 2026-06-13 — Source-connection-gating review (Phase 2, working tree on 0109a8d)

A 5-lens fan-out (correctness · design/blast-radius · security · test-coverage ·
docs-accuracy), each finding adversarially verified by 3 perspective-diverse
skeptics (kept iff ≥2 confirm). The change: gate the ENTIRE source on a persisted
per-source `[sources]` connected flag (gate + idempotent per-tick reconcile) +
rework the Connection panel to a single connect/disconnect toggle + DELETE the
`install-hooks`/`uninstall-hooks` CLI (and its whole orchestration layer + `inquire`).
Result: **1 CRITICAL / 4 HIGH / 2 MEDIUM / 1 LOW confirmed, 8 refuted** — all 7
confirmed fixed in this round; preflight green (1532 tests). The CRITICAL was a
genuine hole the spec *claimed* the reconcile closed but the impl didn't.

| # | seam (file/mechanism) | claim (1 line) | verdict | anchor (paths + fix) | tier | head | date |
|---|---|---|---|---|---|---|---|
| R0613-18 | `runtime/driver.rs::reducer_task` reconcile sweep iterating `REGISTERED_SOURCES` | an identity-less hook event for an unknown id slips the per-event gate (`event_source`→None, `None.is_some_and`==false) → reducer synthesizes a BLANK-source (`source==""`) slot → the reconcile loop only iterated registered source names, so `""` matched none and the ghost survived to the ~30-min stale sweep — contradicting the in-code comment that the sweep "catches anything that slipped the gate" | CONFIRMED→fixed | replaced `Reducer::disconnect_source(src)` (exact-match per registered source) with `Reducer::reconcile_connected(&connected_set)` keyed on the COMPLEMENT of the set (`!connected.contains(source)`), so blank/unregistered slots are evicted within one tick; one call replaces the registry loop. Pinned by `reconcile_connected_evicts_a_blank_source_gate_slipper` (+ the retargeted evict/idempotent twins) in `tests/reducer/lifecycle.rs`. Comment corrected | A | 0109a8d+wt | 2026-06-13 |
| R0613-19 | `tui/mod.rs::connect_source`/`disconnect_source` persist-then-flip ordering | `save_source_connected` failure was only `warn!`ed, then `connected.set` ran unconditionally → runtime gate flips but intent isn't persisted → next restart `resolve_connected` reverts it (connect: re-evicts the just-connected sprites; disconnect: re-shows) — a silent lie | CONFIRMED→fixed | both helpers now PERSIST-OR-ABORT: on save Err, return a "couldn't save config" result and flip NOTHING (no gate change, no hook write). Pinned by `connect_source_aborts_without_flipping_the_gate_when_persist_fails` + the success twins in `tui/mod.rs` tests | A | 0109a8d+wt | 2026-06-13 |
| R0613-20 | deleted `tests/install.rs` (per-target CLI integration suite) | the 320-line suite was the ONLY end-to-end coverage of `install_target`/`uninstall_target` across all 5 targets (real ConfigLock read/merge/backup/atomic-write); deleting it with the CLI left the cores the Connection panel now calls covered only for CLAUDE | CONFIRMED→fixed | added `install_target_round_trips_every_registered_target` (install→AlreadyUpToDate→uninstall→NothingToRemove over `target::TARGETS`, config=Some(tmp), no CLI) — restores per-target round-trip coverage straight against the cores. Per-target merge specifics stay unit-tested in each `install/<t>.rs` | A | 0109a8d+wt | 2026-06-13 |
| R0613-21 | `version.rs::release_notes` (0.4.0–0.7.0 arms) | the in-app version popup's historical notes instruct users to run `pixtuoid install-hooks`, a command this change deleted | CONFIRMED→fixed | reworded the 6 install-hooks fragments to "connect it in the Connection panel (press c)" — display-only strings, dead-command reference removed everywhere | A | 0109a8d+wt | 2026-06-13 |
| R0613-22 | `resolve_connected` first-boot coverage of a newly-added source | no explicit test pinned that every `REGISTERED_SOURCES` entry is decided by the migrate-default on first boot (a new source could fall through) | CONFIRMED→fixed | added `resolve_connected_covers_every_registered_source` (all-installed ⇒ result == the full registry set), so a new registry row is auto-gated by construction | A | 0109a8d+wt | 2026-06-13 |
| R0613-23 | `main.rs` log-routing comment | "Non-TUI (install-hooks, uninstall-hooks, --headless): stderr" names two deleted commands | CONFIRMED→fixed | reworded to "Non-TUI (--headless, validate-pack, init-pack): stderr" | A | 0109a8d+wt | 2026-06-13 |
| R0613-24 | `docs/CONFIGURATION.md` system-managed keys table | the new `[sources]` connection table (visible in users' config) was undocumented | CONFIRMED→fixed | added a `[sources]` row documenting the panel-managed flags + the migrate-default + the "hidden even if hooks/transcripts present" semantics | A | 0109a8d+wt | 2026-06-13 |
| R0613-25 | `runtime/driver.rs::event_source` slot fallback | "event_source can return a STALE cross-source slot for a reused id, gating the wrong source" | REFUTED | `AgentId` is an FNV-1a hash of `(source, opaque_id)` (`source/mod.rs::AgentId::from_parts`), so two sources can't collide on one id absent a hash collision — the slot fallback can't return a foreign source. The artificial mismatched-event test isn't production-reachable. No sharp edge needed (a hashing fact, not a design choice) | A | 0109a8d+wt | 2026-06-13 |
| R0613-26 | `Reducer::reconcile_connected` arg type | "`s.source.as_ref()` vs `&str` is a type mismatch / won't compile" | REFUTED | `Arc<str>::as_ref()` yields `&str`; `HashSet<String>::contains(&str)` holds via `String: Borrow<str>`. Compiles + preflight green | A | 0109a8d+wt | 2026-06-13 |

## 2026-06-13 — PR #284 bot-review round (claude-review + security-review on the live PR)

The two GitHub claude-action passes (review + security) on PR #284. Both converged
on the same real gap the pre-commit Workflow missed; all fixed in the same PR.

| # | seam (file/mechanism) | claim (1 line) | verdict | anchor (paths + fix) | tier | head | date |
|---|---|---|---|---|---|---|---|
| R0613-27 | `tui/mod.rs::connect_source` install-failure path | persist-or-abort covered SAVE failure but not `install_target` failure: on a failed install the flag was already persisted `true` + the gate already open, so the next restart honors a "connected" with no hooks behind it — a hook-only source shows connected yet never produces an agent | CONFIRMED→fixed | connect_source now ROLLS BACK on `install_target` Err (re-save the flag `false` + `connected.set(false)`); disconnect needs none (fail-safe to hidden + inert residual hooks). Pinned by `connect_source_rolls_back_gate_and_flag_when_install_target_fails` (FAIL_TARGET whose default_config_path errs). Flagged HIGH(missing-test)+MEDIUM(security) by two independent bot passes | A | 9ce1afb+wt | 2026-06-13 |
| R0613-28 | `config.rs::save_source_connected` arg type | `source_id: &str` lets a non-registry (runtime) string become a persisted TOML key — defense-in-depth (toml_edit escapes keys, so not an injection, but the type can be tighter) | CONFIRMED→fixed | tightened the whole chain to `&'static str` (save_source_connected + connect_source + disconnect_source); callers pass `ConnectionRow.source_id` (registry `&'static`), so only compile-time-known ids reach the key. Security MEDIUM-2 | A | 9ce1afb+wt | 2026-06-13 |
| R0613-29 | `tui/connection/mod.rs` formatters | `format_connect_result`/`format_disconnect_result`/`no_action_hint` had no unit tests (43% patch coverage) | CONFIRMED→fixed | added 3 unit tests in `connection/tests.rs` (connected/up-to-date + backup/PATH notes; removed/nothing + backup-cleared; NoCli vs actionable hint). review MEDIUM | A | 9ce1afb+wt | 2026-06-13 |
| R0613-30 | site `HowItWorks.astro` / `Install.astro` quickstart | the "Up and running in a minute" tutorial + the install lead still said `pixtuoid install-hooks` / `pixtuoid run` (the deleted CLI) — found by the user, the `.github`/site dirs were outside the earlier grep | CONFIRMED→fixed | rewrote the 3-step tutorial to launch (`pixtuoid`) → press `c` connect → code; Install lead → "run `pixtuoid` and press `c`". `just site-check` green | A | 9ce1afb+wt | 2026-06-13 |

## 2026-06-13 — PR #284 protocol two-lens+escalation review (5 lenses, whole PR at 7ba429f)

The mandatory two-lens review (pr-review.prompt.md) + 3 escalation lenses (lifecycle/editorial/film —
state-machine seam, public copy, regenerated still), each MEDIUM+ adversarially verified by 3 skeptics
(kept iff >=2 confirm). Per-lens: correctness/design/editorial/film APPROVE; lifecycle REQUEST-CHANGES,
but all 3 of its findings refuted >=2/3. **0 confirmed, 37 refuted, 3 nits (1 fixed).** Verdict
APPROVE-WITH-NITS, merge-ready.

| # | seam (file/mechanism) | claim (1 line) | verdict | anchor (paths + trace) | tier | head | date |
|---|---|---|---|---|---|---|---|
| R0613-31 | `runtime/driver.rs` ProofOfLife vs the connection gate | "a disconnected source's ProofOfLife events BYPASS the gate / re-vouch + resurrect its slot, defeating the eviction" | REFUTED (false premise) | `event_source` (driver.rs:179) returns the EXISTING slot's `source` for a ProofOfLife (it carries none), so the gate (driver.rs:215) drops it like any disconnected-source event. And the reducer's ProofOfLife arm only refreshes a vouch on an existing NON-exiting slot — it never creates or resurrects (core/CLAUDE.md ProofOfLife sharp edge: "exiting slots are skipped on insert and aren't sweep candidates"). reconcile_connected's `mark_exiting` therefore sticks. Cite: core ProofOfLife sharp edge + the new pixtuoid CONNECTION GATE "liveness-ladder interactions" note | A | 7ba429f | 2026-06-13 |
| R0613-32 | `reducer.rs::reconcile_connected` / `scope::cascade_exit` cross-source cascade during active delegation | "disconnecting a delegating parent could desync / evict a different source's subtree" | REFUTED (safe by design) | `cascade_exit` BFS-walks children by `parent_id` regardless of source, and a child's source equals its parent's by construction (a subagent inherits the spawner's source), so a single source's subtree exits atomically and a different connected source's tree is never touched; `active_tasks` suppression is AgentId-hashed (source-inclusive). The finding itself concluded "appears safe" and proposed docs, not a code change. Cite: core cascade_exit sharp edge (parent_id BFS) + the new CONNECTION GATE note (cascade is source-agnostic) | A | 7ba429f | 2026-06-13 |
| R0613-33 | `runtime/driver.rs` reconcile vs stale-sweep | "the interaction between per-tick reconcile eviction and the minutes-scale stale-sweep is undocumented / could double-evict" | REFUTED (correct by design; doc gap closed) | A disconnected source is evicted by the 1-Hz `reconcile_connected` (write-once `mark_exiting`), long before any stale-sweep candidate window; `mark_exiting` is idempotent so a later sweep pass is a no-op on the already-exiting slot. Documentation-only gap — now closed by the CONNECTION GATE "liveness-ladder interactions" note in pixtuoid/CLAUDE.md | A | 7ba429f | 2026-06-13 |
| R0613-34 | `tui/connection/mod.rs:221` no_action_hint vs tui/CLAUDE.md | nit: panel renders "not detected on this machine" but the tui guide documented the shorter "not detected" | CONFIRMED→fixed | aligned the tui/CLAUDE.md detail-line doc to the implementation's wording ("not detected on this machine"). Cosmetic | A | 7ba429f | 2026-06-13 |

## 2026-06-14 — Whole-codebase review at b3fa800 (12 finders -> dedup -> 3-skeptic verify -> SYSTEM lens)

Whole-codebase review (the user-requested post-0.7.0 / Fable-5 core-refactor sweep). 12 ledger-BLIND finders
(8 subsystem + 3 lens + 1 SYSTEM) -> 48 raw -> 29 deduped -> each high/med adversarially verified by 3
perspective-diverse skeptics (design-intent-vs-sharp-edges / concrete-repro / CI-or-primary-defense). Result:
**2 confirmed MEDIUM (both defense-in-depth/test-anchor gaps, fixed here), 6 refuted, 21 low/nit.** Zero
high/critical, zero correctness defects. SYSTEM lens: invariant #1 holds, decomposition net-positive, the
registry/bridge-test mesh is the scalability backbone. Verdict: APPROVE — strong health.

| # | seam (file/mechanism) | claim (1 line) | verdict | anchor (paths + fix/trace) | tier | head | date |
|---|---|---|---|---|---|---|---|
| R0614-01 | `source/reasonix.rs::rx_tool_detail` decode-boundary cap | the tool NAME was emitted uncapped (`format!("{tool}{target}")`) while CodeWhale/OpenCode/`make_tool_detail` all ellipsize it — pitfall-3 (content-derived strings capped at the decode boundary) applied to 3 of 4 hook-only decoders | CONFIRMED→fixed (3/3) | ellipsize both name + target via `MAX_TOOL_TARGET_CHARS` (now matches codewhale.rs:271); pinned by `long_tool_name_is_truncated_at_the_decode_boundary` (reasonix.rs). Low real impact (names short) but a regression-prone pattern divergence | A | b3fa800 | 2026-06-14 |
| R0614-02 | `sprite/format.rs` pack load ⇄ `tui/pixel_painter/palette.rs::recolor_frame` | the unique-RGB invariant for recolor keys B/H/S/P (recolor substitutes by RGB equality) was documented as a sharp edge but UNENFORCED — a `--pack-dir` custom pack could collide two keys and silently fail to recolor one (no panic) | CONFIRMED→fixed (2/3; 1 skeptic called it accepted-documented-tradeoff) | added `validate_recolor_palette` called in `build_pack` (bails on a recolor-key RGB collision, runs for embedded AND custom packs); the right layer = core validates / tui consumes (dep direction kept). Pinned by `recolor_palette_rejects_colliding_recolor_keys` | A | b3fa800 | 2026-06-14 |
| R0614-03 | `source/jsonl/liveness.rs` pid rebind/migration | "rebind/migration untested" | REFUTED | covered by `tests/watcher/liveness.rs::rebound_session_survives_old_pid_death_and_follows_the_new_pid`; absence of a UNIT test ≠ missing coverage | A | b3fa800 | 2026-06-14 |
| R0614-04 | `source/jsonl/walk.rs` oversized probe-bypassed re-registration | "a probe-admitted oversized file isn't re-gated on first sight" | REFUTED | the should_seed_at_eof gate EXPLICITLY excludes probe-admitted files (walk.rs:138-143); re-gate via `ended_in_skip` (PR #204, documented design) | A | b3fa800 | 2026-06-14 |
| R0614-05 | `source/jsonl/unclaim.rs` drain-before-unclaim truncation | "concurrent truncation during the drain races" | REFUTED | R0612-04 / PR #272 (47cc1df) — `park_if_truncated_below_cursor` precedes the walk; the remaining window is an ACCEPTED RESIDUAL with WHY (walk.rs:374-378) | A | b3fa800 | 2026-06-14 |
| R0614-06 | `tui/motion` missing-wander-profile freeze | "an absent wander profile freezes the sprite" | REFUTED | provably unreachable — profile snapshotted at transition-in (motion/mod.rs:302/355); two explicit tests; the 1-frame stall is the documented never-freeze-silently convention | A | b3fa800 | 2026-06-14 |
| R0614-07 | `runtime/driver.rs` blank-source gate-slip race | "an identity-less event for a disconnected source slips the gate" | REFUTED | production correct: event_source None → blank synth → `reconcile_connected` catches next tick (R0613-18), pinned by `reconcile_connected_evicts_a_blank_source_gate_slipper`; the candidate self-acknowledged it's a test-gap not a defect | A | b3fa800 | 2026-06-14 |
| R0614-08 | `source/hook` UndeliveredEvents Drop breadcrumb | "the drop breadcrumb is untested" | REFUTED | R0612-02 / PR #272 — `cancelled_conn_leaves_breadcrumb_for_undelivered_events` deterministically asserts the warn fires | A | b3fa800 | 2026-06-14 |

**Low/nit backlog (21, unverified)** — captured in the review output. On closer verification, 2 of the 3 "substantive" nits were NON-issues: the **symlink-loop visited-set** is unneeded (walk.rs:104 refuses symlinked entries wholesale, so recursion can't follow one), and the **probe SessionEnd re-admission** is correct-by-design (ProofOfLife never creates a slot; a probe vouch means the session is alive). Only **hook-only source state-machine fixture breadth** was a real deferred finding → filed as a tracking issue. The rest (WHY comments, module headers, CONN_TIMEOUT breadcrumb, test-isolation `sync::Once`) are doc/test-hygiene for a future sweep. The "CC the only probe user" nit was REFUTED inline (walk.rs:144 already names both probe users incl. Codex's FD probe).

## 2026-06-14 — PR #286 two-lens review (correctness APPROVE, design REQUEST-CHANGES → both fixed)

Two-lens review of the whole-codebase-review follow-up PR. correctness=APPROVE (no behavior bugs); design=REQUEST-CHANGES, 2 confirmed (3/3 skeptics each, 0 refuted) — both latent-inconsistency / DRY defects, fixed in the same PR.

| # | seam (file/mechanism) | claim (1 line) | verdict | anchor (paths + fix) | tier | head | date |
|---|---|---|---|---|---|---|---|
| R0614-09 | `source/reasonix.rs::rx_tool_detail` cap constant | the R0614-01 fix capped the tool NAME at `MAX_TOOL_TARGET_CHARS` (40) and the comment falsely claimed parity with `make_tool_detail`, which caps the name at `MAX_DECODED_FIELD_CHARS` (80) — a latent cross-decoder inconsistency (CodeWhale/OpenCode also used 40 for the name) | CONFIRMED→fixed (3/3) | UNIFIED the tool-NAME cap to `MAX_DECODED_FIELD_CHARS` across ALL THREE hook-only decoders (reasonix + codewhale + opencode), matching `make_tool_detail` (name = content field → 80; the `: target` descriptor stays 40); comments corrected; reasonix test updated to `MAX_DECODED_FIELD_CHARS*3`. The whole codebase now caps tool name at field-width, target at target-width — one convention | A | d035411 | 2026-06-14 |
| R0614-10 | `sprite/format.rs::RECOLOR_KEYS` ⇄ `tui/pixel_painter/palette.rs::recolor_frame` ⇄ embedded test | the recolor key set `[B,H,S,P]` was hardcoded in 3 places (core const, the recolor_frame match arms, the embedded test) — a 5th key = a 3-place lockstep edit; the guard could under-check a key recolor uses | CONFIRMED→fixed (2/2) | made `RECOLOR_KEYS` `pub` in core = SINGLE source of truth: `recolor_frame` now iterates it (hoisted base→agent swaps, behavior-preserving), `validate_recolor_palette` guards it, the embedded test imports it. A 5th key is now a 1-place edit, and recolor + the guard can't drift. tui/CLAUDE.md sharp edge updated (invariant ENFORCED at load, not just documented) | A | d035411 | 2026-06-14 |

## 2026-06-14 — PR #287 three-lens review (#279 serializable SceneState + snapshot golden)

Mandatory review (correctness/grounding + design/blast-radius + a determinism/test-quality lens — the golden runs on Windows CI). Overall **APPROVE-WITH-NITS**; no correctness or determinism defects (the determinism lens independently verified the golden is platform-stable: FNV `AgentId` is platform-independent, `/proj` survives `Path::to_str()` on Windows, all timestamps injected — no Windows-CI risk). CI 23/23 green incl. windows-test + semver-checks (serde derives are additive). The online `claude[bot]` review raised exactly ONE finding (MEDIUM) — the same Idle-coverage gap the determinism lens found (F5), corroborating it. All actionable findings fixed in-PR per no-phasing.

| # | seam (file/mechanism) | claim (1 line) | verdict | anchor (paths + fix) | tier | head | date |
|---|---|---|---|---|---|---|---|
| R0614-11 | workspace `CLAUDE.md:157` ⇄ `crates/pixtuoid-core/CLAUDE.md:108` (same-PR doc currency) | the PR rewrote the per-crate sharp edge to "v2-daemon deferred/out-of-scope" but the workspace sharp-edge INDEX still read "SystemTime (process-local, v2-daemon-ready)" — a self-contradiction the PR itself opened by closing #279/#280/#281 | CONFIRMED→fixed | reconciled `CLAUDE.md:157` to "the whole SceneState tree is Serialize/Deserialize … NOT a stable wire contract — the v2-daemon consumer is closed out-of-scope, #279/#280/#281"; also softened the four surviving `v2 daemon` code doc-comments (runtime/mod.rs, layout/mod.rs, walkable.rs, tui/anim.rs) from "for the future v2 daemon split" to "(serializable; no out-of-process consumer today)" so no reader treats a closed direction as planned | A | #287 | 2026-06-14 |
| R0614-12 | `state/mod.rs` serde-adapter comment | the comment cited "debug state dumps" as a consumer; grep finds NO SceneState serialize call in `src/` outside tests — an aspirational consumer described as current | CONFIRMED→fixed (nit) | reworded to "back the full-scene regression snapshot today; a future debug state dump / daemon snapshot would build on the same shape" — matches the deferred-consumer framing. The comment's load-bearing WHY (hand-rolled adapters ∵ no `Box<Path>: Deserialize`; not a wire contract) was already accurate | A | #287 | 2026-06-14 |
| R0614-13 | `state/mod.rs` `FloorLocalDeskIndex` vs `GlobalDeskIndex` derive asymmetry | only `GlobalDeskIndex` gained serde; the adjacent twin did not — correct YAGNI (twin is unreachable from the serialized tree) but an undocumented asymmetry that invites a future "consistency fix" | CONFIRMED→fixed (nit) | added a doc line to `FloorLocalDeskIndex`: "Deliberately NOT Serialize … transient bridge value, never a stored SceneState field; only GlobalDeskIndex is reachable" — pre-empts the churn | A | #287 | 2026-06-14 |
| R0614-14 | `tests/reducer/snapshot.rs` + round-trip test Idle coverage | NEITHER test ever serialized an `Idle` slot (golden covered only the 3 non-Idle states) — a future Idle field would silently reshape the wire form `"Idle"` → `{"Idle": {..}}` uncaught. **Found independently by the determinism lens AND the online claude[bot] (rated MEDIUM)** | CONFIRMED→fixed | added an Idle slot to BOTH the round-trip unit test and the golden, plus a winding (Active→Idle debounce) slot so the golden also pins a populated `Option<SystemTime>` (`pending_idle_at`). Golden now covers all four ActivityState shapes + a populated Option<SystemTime> | A | #287 | 2026-06-14 |
| R0614-15 | `tests/reducer/snapshots/*.snap` opaque u64 BTreeMap keys | the golden keys agents on raw `AgentId(u64)` FNV hashes — flagged as possibly brittle/hard to review | REFUTED (no action) | the keys are DETERMINISTIC FNV trip-wires over fixed test literals (stable run-to-run AND cross-platform); a reducer-OUTPUT golden is SUPPOSED to diff on shape/output changes, so this is the intended regression signal, not churn. The test module doc already explains it | A | #287 | 2026-06-14 |

## 2026-06-14 — PR #288 review (legible demo glyphs)

3-lens review (correctness + design + visual frame-census, since generated art ships) + the online `claude[bot]`. Root cause: the demo-media rasterizer drew every text cell as a colored block ("tiny glyph box"); the live terminal was always fine. Fix renders real glyphs via font8x8 (dev-dep). Overall APPROVE; the bot independently raised the test-gap as HIGH. All findings fixed in-PR.

| # | seam (file/mechanism) | claim (1 line) | verdict | anchor (paths + fix) | tier | head | date |
|---|---|---|---|---|---|---|---|
| R0614-16 | `examples/snapshot.rs::glyph8x8` / `blit_glyph_cell` test gap | the two new pure rasterizer fns had NO unit tests (repo is TDD-first); a font8x8 bit-order flip would silently MIRROR all text across the generated media with no automated catch — **online claude[bot] rated this HIGH** | CONFIRMED→fixed | added `blit_glyph_cell_lsb_is_leftmost_and_doubles_vertically` (pins LSB=leftmost + the 1×2 vertical double + origin offset) and `glyph8x8_covers_ascii_and_custom_dots_but_not_arbitrary_symbols`; both run in CI (the `[[example]]` has `test = true`, nextest picks them up — verified, 1542 tests) | A | #288 | 2026-06-14 |
| R0614-17 | `examples/snapshot.rs` glyph fallback ⇄ `tui/widgets/dashboard.rs:121-124` status dots | the `●◐○` state dots (U+25CF/25D0/25CB) aren't in any font8x8 set → all three collapsed to the SAME fallback block, distinct only by color; the dashboard's whole purpose is at-a-glance state | CONFIRMED→fixed (low) | added `custom_glyph` with 3 hand-rolled 8×8 bitmaps (● filled disc / ○ ring / ◐ left-half), consulted BEFORE the font8x8 chain — now shape-distinct (visually verified: green disc / gray ring / yellow half). `·`(U+00B7) + ASCII were already legible via font8x8 LATIN/BASIC | A | #288 | 2026-06-14 |
| R0614-18 | demo media legibility (visual money-shot) | the website CH 04 DASHBOARD + connection popups rendered all text as colored blocks | CONFIRMED→fixed (positive census) | visual lens read the regenerated PNGs at 3× — every popup line legible, no mirroring (confirms bit order), office sprites un-regressed (`screenshot.png` pixel-identical to baseline). Regenerated media; `just gen-check` green; NO TUI/product change (example + media only) | A | #288 | 2026-06-14 |

## 2026-06-14 — PR #290 review (showcase 7-channel consolidation + pets/meeting close-ups, #289)

3-lens review (correctness + design + visual frame-census). correctness=APPROVE (gen-check/site-check/astro-check all EXIT 0), visual=APPROVE (all 4 money-shots read correctly; the "undefined?" on the meeting poster traced to `chitchat.rs` CHITCHAT_LINES[16] + confirmed present on the OLD main poster ⇒ pre-existing chitchat bubble, NOT a regression; hero intact). design=REQUEST-CHANGES on one HIGH (the impl missed it). All fixed in-PR.

| # | seam (file/mechanism) | claim (1 line) | verdict | anchor (paths + fix) | tier | head | date |
|---|---|---|---|---|---|---|---|
| R0614-19 | `site/src/features.json` deep-links ⇄ removed/renamed showcase channels | the PR removed OFFICE + renamed FLOORS→AGENTS, but two bento cards' "See it live →" `card.href` still pointed at `#showcase-office` / `#showcase-floors` — dead clicks (`tune()` no-ops on a missing data-stage); the showcase build guard only checked ids INTERNAL to showcase.json so CI stayed green | CONFIRMED→fixed (HIGH) | repointed both hrefs to `#showcase-agents` (AGENTS absorbed both); ADDED a cross-guard in `astro.config.mjs` asserting every `features.json` `#showcase-<id>` resolves to a real channel — MUTATION-TESTED (broke a link → build EXIT 1 with the exact message), so a future channel rename can't silently orphan a link | A | #290 | 2026-06-14 |
| R0614-20 | `scripts/gen-media.py` clip-level `crop` vs the kind:"crop" job's `crops` | the new singular clip `crop` field sits near the pre-existing plural `crops` dict (different mechanism) — name proximity could confuse a maintainer | CONFIRMED→fixed (nit) | added a cross-reference note in the `run_clip` comment ("unrelated to the kind:'crop' job's plural `crops`"); kept the field name `crop` (clear in context, and the keys/job-kinds are disjoint) | A | #290 | 2026-06-14 |

## 2026-06-14 — PR #291 review (v0.8.0 release bump)

Two-lens release review (release-audit + design/blast-radius). release-audit=APPROVE/0 findings (every version bumped — guarded by `path_dep_version_tracks_crate_version` + `current_version_has_release_notes`; the 5 notes bullets each cross-checked against `git log v0.7.0..main`; the breaking install-hooks removal surfaced, not buried; media regenerated so the HUD reads v0.8.0). design=APPROVE-WITH-NITS, both fixed in-PR. KEEPER: the TUI bakes `CARGO_PKG_VERSION` into the HUD status bar (`hud.rs`), so a version bump silently drifts EVERY committed still — `just gen` is a mandatory release step, caught here by the smoke gen-check pixel gate.

| # | seam (file/mechanism) | claim (1 line) | verdict | anchor (paths + fix) | tier | head | date |
|---|---|---|---|---|---|---|---|
| R0614-21 | npm shim comments `npm/pixtuoid/bin/{pixtuoid-hook,resolve.js}` | two more stale `install-hooks` mentions (same class as R0613-21/23, swept everywhere else) — and this PR's own `npm/README.md` edit now disagreed with `pixtuoid-hook:13` | CONFIRMED→fixed (low) | reworded both to the Connection panel ("the Connection panel write the ABSOLUTE path"; "used by the Connection panel's hook install"); comments only, npm-check green | A | #291 | 2026-06-14 |
| R0614-22 | `docs/MIGRATION.md` per-version structure | the install-hooks removal was documented only under the v0.4.0-rename section — a 0.7→0.8 upgrader had no version heading to find | CONFIRMED→fixed (nit) | restructured to per-version notes + added a `## v0.7.x → v0.8.0` section (CLI removal → Connection panel; no non-interactive equivalent; cw·/oc· new sources); site-check renders /migration green | A | #291 | 2026-06-14 |

## 2026-06-14 — PR #292 review (Copilot CLI source, transcript-only)

3-lens review (correctness + design/blast-radius + lifecycle/reducer-interaction — the state-machine-seam escalation lens). correctness=APPROVE/0; design=APPROVE-WITH-NITS; lifecycle=REQUEST-CHANGES (caught a real HIGH the other two missed by tracing the reducer interaction, not the diff). All findings fixed in-PR. The `permission.*`/`subagent.failed` byte-real gap is the documented needs-human-verify item, correctly NOT flagged.

| # | seam (file/mechanism) | claim (1 line) | verdict | anchor (paths + fix) | tier | head | date |
|---|---|---|---|---|---|---|---|
| R0614-23 | `state/reducer.rs::register_slot` unknown-cwd reap ⇄ cwd-less subagents | a parented (subagent) SessionStart with empty cwd (Copilot's `subagent.started` carries none) → `unknown_cwd=true` → the 3-min `STALE_UNKNOWN_CWD_TIMEOUT` ghost-reap fires before the state match → a sub-agent running >3 min is cascade-exited WHILE ALIVE (no probe→no vouch, parent Active not Waiting→no readiness exemption, never healed) | CONFIRMED→fixed (HIGH) | `unknown_cwd: !has_cwd && parent_id.is_none()` — a parented slot is a real subagent, not a startup-seed ghost (mirrors the hook-synthesis exemption). General reducer rule (Copilot is just the first source to expose it). Pinned by `parented_empty_cwd_subagent_is_not_ghost_reaped` (tests/reducer/liveness.rs) | A | #292 | 2026-06-14 |
| R0614-24 | `source/jsonl/walk.rs::extract_cwd` first-sight cwd field-path | extract_cwd read only top-level `cwd`/`payload.cwd`; Copilot nests it at `data.context.cwd` → a gated-then-tail-revived copilot root registers empty-cwd (→ the same 3-min reap) + the head-read gave `cp#N` not `cp·<dir>` | CONFIRMED→fixed (MED) | added a `data.context.cwd` arm (additive — other sources unaffected). Shared head-reader already special-cases per-source paths (Codex's `payload.cwd`) | A | #292 | 2026-06-14 |
| R0614-25 | `scripts/check_upstream_drift.py` Copilot arm | the drift-watch arm (spec step 8 + the named HIGH-risk mitigation, #3551) was silently dropped — a plan-miss, not a §9 YAGNI deferral | CONFIRMED→deferred-with-issue | filed #293 (build the npm-schema diff arm) + the fixture pins copilotVersion as the interim catch; moved to a tracked deferral | A | #292 | 2026-06-14 |
| R0614-26 | `source/copilot.rs` permission.completed | mapped to `ActivityStart{detail:None}` → phantom `tool_call_count += 1` on every approval (Waiting-clear itself correct) | CONFIRMED→fixed (nit) | emit nothing on `result.kind` starts_with("approved") (the approved tool's own start clears Waiting); only emit the clearing ActivityStart on denial/cancel. Test covers both branches | A | #292 | 2026-06-14 |
| R0614-27 | `tui/connection/mod.rs::display_name_for` | the `other => other` fallthrough showed the no-target Copilot row as lowercase `copilot` (peers are title-cased) | CONFIRMED→fixed (nit) | added `"copilot" => "Copilot CLI"`; mechanized by `every_no_target_row_has_an_explicit_display_name_not_the_raw_id` (scoped to no-target rows — opencode's deliberate lowercase brand via Target.display_name is exempt) | A | #292 | 2026-06-14 |

### 2026-06-14 — PR #292 online claude[bot] (incremental) — 2 MEDIUM my lenses missed

| # | seam (file/mechanism) | claim (1 line) | verdict | anchor (paths + fix) | tier | head | date |
|---|---|---|---|---|---|---|---|
| R0614-28 | `source/copilot.rs::copilot_session_ended` | the bare `contains("session.shutdown")` tail-substring false-positives on TOOL OUTPUT (a shell result mentioning the string) → false EOF-seed → a live session's events silently dropped — the CC "content must never drive lifecycle" invariant | CONFIRMED→fixed (MED) | anchored on the structural `"type":"session.shutdown"` (compact on-disk shape); negative test pins a tool-result mentioning the string does NOT end the session | A | #292 | 2026-06-14 |
| R0614-29 | `source/copilot.rs` permission.completed ⇄ reducer Waiting-clear | the denied-permission `ActivityStart{None}` Waiting-clear was correct but UNTESTED at the reducer boundary (decoder test proved emit, not clear) — a stuck-60-min-Waiting risk if wrong | CONFIRMED→test-added (MED, behavior already correct) | verified `enter_active` (reducer.rs:707) flips Waiting→Active + drops `gated_before_waiting`; added cross-layer `copilot_denied_permission_clears_waiting_through_the_reducer` (decode→reducer) | A | #292 | 2026-06-14 |

### 2026-06-14 — PR #295 two-lens review (correctness/grounding + design/blast-radius) — Copilot drift-watch arm (closes #293, resolves R0614-25)

| # | seam (file/mechanism) | claim (1 line) | verdict | anchor (paths + fix) | tier | head | date |
|---|---|---|---|---|---|---|---|
| R0614-30 | `scripts/check_upstream_drift.py` Copilot arm (the deferral of R0614-25) | the #293 deferral — build the npm-schema diff arm — was outstanding; the fixture's copilotVersion pin was only the interim catch | CONFIRMED→fixed (resolves R0614-25) | added `read_copilot_events` (regex over copilot.rs's `match kind` block) + `upstream_copilot_events` (parse `@github/copilot` `session-events.schema.json` `definitions[*].properties.type.const`) + an ASYMMETRIC mapped⊆upstream check wired into `run_checks`/`main`. Live-verified: all 10 mapped events present in the 109-const schema, breaking branch fires on a simulated rename | A | #295 | 2026-06-14 |
| R0614-31 | `read_copilot_events` match-block regex scope | could the `let out = match kind {…\n    };` scope or `[a-z._]+` char-class leak test fixtures (same strings embedded as JSON below the fn) or drop an arm? | REFUTED (no action) | correctness lens verified: captured set == the 10 arm-LHS keys byte-for-byte; nested `};` inside arm bodies sit at 12-16 space indent (never the 4-space terminator); field-name literals (`"toolCallId"` etc.) are camelCase with no dot so the prefix+`.` regex skips them | A | #295 | 2026-06-14 |
| R0614-32 | `COPILOT_SCHEMA_URL` unpkg "latest" via redirect | relying on the bare unpkg path (302→latest pinned version) — could a future maintainer "pin" it and defeat the watch, or does latest mask a rename? | CONFIRMED→fixed (low) | latest is DELIBERATE for a drift watch (tracks the published shape, doesn't freeze a tested one) — a rename surfaces, never masked; added a comment saying so to pre-empt a defeating pin. Schema-restructure fails safe to the breaking `upstream is None` branch | A | #295 | 2026-06-14 |
| R0614-33 | `.github/workflows/upstream-drift.yml:3` header comment | the per-source enumeration ("CC/Codex/Reasonix") was stale — already missing CodeWhale/opencode before this PR, now also Copilot | CONFIRMED→fixed (low, pre-existing) | replaced the rotting enumeration with "across all registered sources"; the script docstring + `pixtuoid-core/CLAUDE.md` defense-#4 bullets backfilled to all 6 sources in the same PR | A | #295 | 2026-06-14 |

### 2026-06-14 — popup field marquee two-lens review (correctness + design, adversarial-verified) — feat/popup-field-marquee

| # | seam (file/mechanism) | claim (1 line) | verdict | anchor (paths + fix) | tier | head | date |
|---|---|---|---|---|---|---|---|
| R0614-34 | `widgets/mod.rs::marquee_window` scroll-back branch | the back-scroll bottoms at off=1, not off=0, so the head only gets its full dwell at cycle start, not symmetrically at the back end | REFUTED (no action) | correctness lens reproduced the arithmetic: wrap transition is off=1→off=0 (single-char step, no visual jump); head DOES get a full `MARQUEE_END_PAUSE_MS` dwell each cycle; `chars[off..off+width]` always in-bounds (off ∈ [0,max_off]). Correct ping-pong, no incorrect output/panic — the finding self-labels "not a bug" | A | — | 2026-06-14 |
| R0614-35 | `widgets/dashboard.rs::dashboard_line` name = prefix+label+`(N)` marquee'd whole | on a SELECTED overflowing row the tree-chevron (▸/▾/└) and collapsed child-count `(N)` scroll off-screen mid-cycle, briefly losing the fold affordance | nit (no fix) | mechanism verified, but narrow+transient: only a selected row whose prefix+label+suffix exceeds LABEL_W=32 at a non-head phase; fold state is redundantly shown by child-row indentation + the `⋮ N more ▾` cue; self-heals each ping-pong cycle. Deliberate "the whole field scrolls" choice (matches the user's scope answer) | A | — | 2026-06-14 |
| R0614-36 | `widgets/connection.rs::paint_connection_panel` detail-line width | `detail_w = inner.width - 2` only accounts for the 2-space left indent, so a full-width scrolling path runs flush to inner's right edge → margins 3-left/1-right (asymmetric vs the panel's `PANEL_PAD_X`) | CONFIRMED→fixed (nit) | changed to `saturating_sub(4)` so the scrolling detail line keeps a symmetric 2-col right margin; no render change to the fixture (37-char path fits either way, gen-check still byte-identical) | A | — | 2026-06-14 |
| R0614-37 | `widgets/connection.rs` socket line (`:52`) + per-row Live column (`:179`) not marquee'd | the socket path hard-clips with no ellipsis; the Live column has no width cap | REFUTED (out of scope, pre-existing) | both are pre-existing (untouched by this PR's diff) and correctly NOT marquee'd: the marquee is focused-row-scoped, while the socket line is a panel-global header and the Live column renders for every row (not selection-driven). The long-socket-path no-ellipsis clip is a trivial pre-existing nit, not this change | A | — | 2026-06-14 |

### 2026-06-14 — Cursor CLI source two-lens review (correctness + design, adversarial-verified) — feat/cursor-cli-source

| # | seam (file/mechanism) | claim (1 line) | verdict | anchor (paths + fix) | tier | head | date |
|---|---|---|---|---|---|---|---|
| R0614-38 | `install/cursor.rs::hook_command` Unix env-prefix ⇄ shell-vs-exec | if Cursor exec's the hook command WITHOUT a shell, the `PIXTUOID_SOURCE=cursor` env-prefix is lost → cursor events mis-attributed to `cc·` | REFUTED (fail-safe) — capture confirmed Cursor DOES shell the command | the capture (2026-06-14) verified Cursor shells the `command` (the `{…}>>log` block ran), so the env-prefix takes and this is moot. Even in the hypothetical no-shell case it's fail-safe: an unstamped payload defaults to claude-code → CC custom returns Ok(None) → the shared CC arm's PascalCase `match event` has NO arm for Cursor's camelCase names (`sessionStart`/`preToolUse`/…) → `other => bail!` (logged drop), NEVER a ghost CC sprite. (NOTE: the original "Cursor has no session_id → the session_id guard bails" mechanism is STALE — the finalize commit proved session_id IS present; the camelCase event-name mismatch is the real backstop, verified empirically.) Worst case = no `cu·` sprite until switching to the bare `--source` form | A | — | 2026-06-14 |
| R0614-39 | `install/cursor.rs::json_merge_uninstall` version handling | a user's `{"version": N}` (no hooks) → install (preserves N) → uninstall (unconditionally removed `version` when hooks emptied) → `{}` = user data loss | CONFIRMED→fixed (low) | uninstall no longer removes `version` (can't distinguish our set-if-absent `1` from a user's value) — a leftover `{"version":1}` after a from-scratch install→uninstall is an accepted harmless residual. Pinned by `uninstall_preserves_a_users_version_only_file` + `uninstall_all_managed_drops_hooks_but_keeps_version` | A | — | 2026-06-14 |
| R0614-40 | `check_upstream_drift.py::read_cursor_events` match-block parse | regex over source/cursor.rs's `match event` block is brittle — a future camelCase field lookup in an arm would leak a phantom event → false drift alarm | CONFIRMED→fixed (nit) | switched to read the explicit `CURSOR_EVENTS` const in `install/cursor.rs` (leak-free; mirrors `read_reasonix_events`/`read_codewhale_events`, already the source of truth for `every_registered_cursor_event_decodes`). Docstring + CLAUDE.md bullet updated | A | — | 2026-06-14 |
| R0614-41 | `site/src/sources.json` Cursor row ⇄ `supported_sources_manifest` bridge | registering cursor forced `status:"supported"` with macOS/Linux `"yes"` while the CLI hook-firing is UNVERIFIED — an overclaim, with only human memory between it and a premature merge (the opencode "claimed ready twice" class) | CONFIRMED→mitigated (MED) | platforms macOS/Linux → `"experimental"` (honest: firing unverified on every platform); filed tracking issue #297 with the exact capture + checklist; PR opened as DRAFT gated on the capture. (`status:"supported"` stays — the bridge test pins supported⇔REGISTERED_SOURCES exact-equality, so a registered source can't be non-supported without weakening the bridge; the experimental platforms + #297 + draft carry the honesty) | A | #297 | 2026-06-14 |
| R0614-42 | `check_upstream_drift.py` Cursor `stop` rung | `\bstop\b` matches the common word "stop" on the docs page regardless → a `stop`-event rename can't trip the alarm (zero coverage for that one rung) | REFUTED (honestly-documented no-op) | intentional + openly flagged in the URL comment + CLAUDE.md ("`stop` is low-confidence ... the distinctive `sessionStart`/`sessionEnd`/`preToolUse`/`postToolUse` carry the check"); the finding's own verdict is "leave it". The distinctive 4 events provide real one-directional coverage; the dead `stop` rung costs nothing | A | — | 2026-06-14 |

### 2026-06-14 — Cursor whole-PR re-review (correctness + design + lifecycle, adversarial-verified) + online claude[bot] — feat/cursor-cli-source

Correctness lens: 0 findings. Online security review: 0 findings. Online code-review + design lens converged on one stale docstring (R0614-43); the rest are nits. (Both captures' decode/keying/install/dispatch verified correct.)

| # | seam (file/mechanism) | claim (1 line) | verdict | anchor (paths + fix) | tier | head | date |
|---|---|---|---|---|---|---|---|
| R0614-43 | `registry.rs:333` CURSOR doc + `pixtuoid-core/CLAUDE.md:121` | the doc-comments still said "no usable session id … keys on cwd" / "claims-all and keys on cwd" after the capture flipped keying to `session_id` (self-contradicting the same row's caps comment) — also flagged by online claude[bot] as MEDIUM | CONFIRMED→fixed (low) | rewrote both to "keys on `session_id` (capture-verified; workspace_roots[0] fallback); subagents render FLAT". Code (`cursor.rs`) was already correct — docs-only drift from the finalize commit | A | #298 | 2026-06-14 |
| R0614-44 | `docs/REVIEW-LEDGER.md` R0614-38 refutation mechanism | the fail-safe rationale ("Cursor has no session_id → the session_id guard bails") went STALE when the finalize commit proved session_id IS present — the real backstop is the camelCase-vs-PascalCase event-name mismatch → `other => bail!` | CONFIRMED→fixed (nit) | corrected R0614-38's mechanism in-place (same-branch, pre-merge); verdict (REFUTED/fail-safe/no `cc·` ghost) unchanged + empirically re-confirmed | A | #298 | 2026-06-14 |
| R0614-45 | conformance fixture lacked a `Task`/`subagent_type` line | the Cursor Delegating lifecycle was pinned only by the decode unit test + the source-agnostic Reasonix reducer twin — no conformance-level exercise | CONFIRMED→fixed (nit) | added a `Task` (subagent_type) `preToolUse` line to the fixture (same parent session → one AgentId) so the snapshot now exercises `Task`→`ToolDetail::Task` (Delegating) end-to-end through the harness | A | #298 | 2026-06-14 |
| R0614-46 | conformance fixture line 2 stray `tool_use_id` | a `"tool_use_id":"357c27e0"` the decoder always discards (emits None) — inconsistent with the other preToolUse line + the "always None" contract + the doc wire example | CONFIRMED→fixed (nit) | removed it from the fixture (matches the always-None contract; byte-realism preserved on the load-bearing fields) | A | #298 | 2026-06-14 |

### 2026-06-14 — Copilot byte-real fixtures two-lens review (correctness + design, adversarial-verified) — feat/copilot-byte-real-fixtures-294 (#294/#299)

3 findings raised, 0 refuted — all LOW/nit (0 crit/high/med). Decoder logic unchanged (fixture realism only); 1600 tests + lint + clippy green; a deterministic fidelity check confirmed every byte-real *event* line `grep -Fxq`-matches a raw capture.

| # | seam (file/mechanism) | claim (1 line) | verdict | anchor (paths + fix) | tier | head | date |
|---|---|---|---|---|---|---|---|
| R0614-47 | `tests/sources/fixtures/copilot/permission/events.jsonl` `session.start` cwd | the conformance fixture's `session.start` cwd was scrubbed (`/private/tmp/cop-deny.dYRB` → `/home/user/project`), so that one line is NOT byte-exact vs the live capture — a truth-in-labeling gap against the unqualified "byte-real" framing | CONFIRMED→fixed (low) | the scrub is the RIGHT call (committing the temp path would bake a machine-specific dir into the insta golden — verifier agreed); ALL event-bearing lines (permission.*/subagent.*) ARE byte-exact (deterministic `grep -Fxq` proof). Fixed the honesty gap in-tree: `copilot.rs` module docstring now states the lone neutralized field is this fixture's `session.start` cwd | A | #299 | 2026-06-14 |
| R0614-48 | commit/PR body wording "two schema-faithful Copilot **fixtures**" | implies two on-disk fixture FILES were swapped; in reality the two schema-faithful items were INLINE test mocks (the lone pre-existing `tool-run` fixture was already byte-real + untouched), and a new `permission` fixture was ADDED | CONFIRMED→fixed (nit) | PR body reworded to "inline test mocks → byte-real + a new conformance fixture added" (the squash record); commit body left as harmless history (amend+force-push disproportionate for a cosmetic nit, and force-push needs explicit user OK) | B | #299 | 2026-06-14 |
| R0614-49 | gitignored `docs/superpowers/specs/2026-06-14-copilot-cli-source-design.md` "needs-human-verify / bytes pending" | the spec the tracked `copilot.rs` docstring points to still framed permission.*/subagent.failed as schema-pinned-bytes-pending after #294 resolved exactly that | CONFIRMED→fixed-locally (nit, OUT OF SCOPE) | the spec is gitignored (`.gitignore: docs/superpowers/`) + CLAUDE.md-declared unversioned, so NOT a tracked-docs-currency violation and not in the PR; updated the local copy's stale lines to "resolved by #294" for accuracy. No GitHub issue needed (resolved, not deferred) | B | #299 | 2026-06-14 |

### 2026-06-14 — Whole-codebase review at HEAD c45ecb5b (16 finders → dedup → 3-lens adversarial verify)

16 finders (13 subsystem + concurrency/panic-invariant/security-resource lenses) over the full workspace → 2 raw findings → 1 confirmed (low) + 1 refuted. The codebase remains very clean; the design-intent skeptic + ledger dedup killed the rest before they surfaced.

| # | seam (file/mechanism) | claim (1 line) | verdict | anchor (paths + fix) | tier | head | date |
|---|---|---|---|---|---|---|---|
| R0614-50 | `runtime/driver.rs:290` headless `SourceDeath` print | the headless health arm `println!`s `d.error` (an `anyhow {e:#}` chain that can embed a malformed transcript path / parse-error content carrying terminal escapes) straight to the terminal WITHOUT `sanitize_line` — the same escape-injection class fixed in R0609-02 for agent labels/details/Notification, missed on the source-health path (added #157) | CONFIRMED→fixed (low; 2/3 verify votes, both adjusted to low) | extracted a pure tested `runtime::format_source_death(&SourceDeath)` that `sanitize_line`s BOTH `source` (defense-in-depth — it's a static REGISTERED_SOURCES name) and `error` (the real vector); driver.rs prints via it. Pinned by `format_source_death_strips_terminal_escapes_from_both_fields` (mirrors `summarize_strips_terminal_escapes_from_untrusted_fields`) | A | — | 2026-06-14 |
| R0614-51 | `layout/mask.rs:266` pantry counter south-strip | the pantry counter mask "blocks one row too far north" | REFUTED (1/3 — documented design) | the pantry counter blocks only a shallow `PANTRY_FOOTPRINT_DEPTH` south strip BY DESIGN (overhang occlusion, invariant #6 + the `pixtuoid-core/CLAUDE.md` "Pantry counter blocks only a shallow … south strip" sharp edge); the stamping is correct per that invariant. Outvoted 2-to-1 by the grounding + design-intent lenses | A | — | 2026-06-14 |

### 2026-06-15 — Decode-drift breadcrumbs two-lens review (correctness/noise + design/invariants) — feat/decode-drift-breadcrumbs (PR #303)

3 findings, 0 refuted — all confirmed minor; the PR's central judgment (breadcrumb only on a REQUIRED field of a committed event, never on a type-discriminator) verified sound across all 9 missing_field sites + both shape_drift sites. Invariants #1/#2/#3 hold.

| # | seam (file/mechanism) | claim (1 line) | verdict | anchor (paths + fix) | tier | head | date |
|---|---|---|---|---|---|---|---|
| R0615-01 | `antigravity.rs::decode_ag_line` PLANNER_RESPONSE tool-call `name` | the "uniform missing_field on every required tool-name read" discipline missed antigravity's `name` read (line 84) — it still defaulted to `?` silently while every other transcript source got the breadcrumb | CONFIRMED→fixed (med) | added `drift::missing_field(SOURCE_NAME, "PLANNER_RESPONSE", "name")` in the unwrap_or_else; `step_type` (74) stays a silent discriminator (correct). Now uniform across all sources | A | #303 | 2026-06-15 |
| R0615-02 | `cc_probe.rs:86` shape_drift `source` field | the new shape_drift breadcrumb used the literal `"claude-code"` while its sibling (claude_code.rs SubagentStop) used `SOURCE_NAME` — would silently diverge from REGISTERED_SOURCES on a future CC rename, feeding doctor/the Layer a non-registered name | CONFIRMED→fixed (nit) | `source = crate::source::claude_code::SOURCE_NAME` (the consumers key on a valid REGISTERED_SOURCES name; drift.rs doc says so) | A | #303 | 2026-06-15 |
| R0615-03 | claim-all hook-only decoders' `other => bail!` | `unknown_event` lived only in the shared `decode_hook_payload` `other` arm, unreachable for the 4 claim-all sources (they bail/skip in their own match) → asymmetric drift coverage (not a false-positive; defense #3's `every_registered_*_event_decodes` already covers the bailers' closed sets) | CONFIRMED→fixed (nit) | added `drift::unknown_event(SOURCE_NAME, other)` before the bail in cursor/reasonix/codewhale for doctor parity; opencode's `_ => Ok(vec![])` stays silent BY DESIGN (one-directional ~50→handful mapping; a breadcrumb there would flood — documented at the site) | A | #303 | 2026-06-15 |

#### Online claude[bot] review of PR #303 (2 actionable + 1 forward-note)

| # | seam (file/mechanism) | claim (1 line) | verdict | anchor (paths + fix) | tier | head | date |
|---|---|---|---|---|---|---|---|
| R0615-04 | `drift.rs` emission test needle list | the capture test asserted only `"copilot"` as a source value → 3 of 4 breadcrumb kinds' `source` field went untested; a future accidental drop of `source = %source` from a helper wouldn't be caught | CONFIRMED→fixed (nit) | added `"codex"` (unknown_event source) + `"claude-code"` (unknown_dispatch + shape_drift source) to the needles | A | #303 | 2026-06-15 |
| R0615-05 | cc_probe + claude_code inline `shape_drift` warns | the two inline shape_drift sites set target/source/kind but NO `detail` field (free-form msg / stem+wire), diverging from the `shape_drift()` helper's contract (`detail = %detail`) that PR 3's `doctor` log-scan will key on → it would silently miss them | CONFIRMED→fixed (low) | routed BOTH through `drift::shape_drift(source, &format!(…))` so every shape_drift emits a uniform `detail` (also makes the helper actually used + subsumes R0615-02's literal→const fix) | A | #303 | 2026-06-15 |
| R0615-06 | untrusted values in drift breadcrumbs (`unknown_event`/`unknown_dispatch`) | crafted hook event / tool names are logged unsanitized; `tracing_subscriber::fmt` doesn't strip newlines/ANSI → multi-line log injection | ACCEPTED-residual / FORWARD-NOTE (not MED — self-injection, 0600 socket + user-owned JSONL, no cross-user vector; security review: "clear to merge") | the drift.rs module doc already states consumers sanitize; the LOAD-BEARING action is in PR 3 — `pixtuoid doctor`'s log-scanner MUST `sanitize_line` these fields before display/action (the 3rd consumer). Tracked for PR 3 | B | #303 | 2026-06-15 |

#### PR #304 (`pixtuoid doctor`) review — online claude[bot] (1 MED) + two-lens (3 nit)

| # | seam (file/mechanism) | claim (1 line) | verdict | anchor (paths + fix) | tier | head | date |
|---|---|---|---|---|---|---|---|
| R0615-07 | `doctor::run` config-load warnings | warnings from `config::load` were collected but never surfaced — a malformed config would show every source disconnected with NO explanation (ironic for a diagnostic) | CONFIRMED→fixed (med, online) | render `⚠ config: <sanitized>` lines INTO the report (stdout, where the diagnosis lives — better than eprintln) | A | #304 | 2026-06-15 |
| R0615-08 | `doctor::scan_log_for_source` loose `contains(TARGET)` | a non-drift line mentioning the literal target string + carrying source/kind would be counted (false-positive PATH) | CONFIRMED→deferred (nit) — filed | latent: no production code emits the literal outside drift.rs; only other source= line (manager.rs) won't carry a drift kind. Tracked for a scanner-hardening PR (structural ` TARGET: ` anchor + a test) | B | #304 | 2026-06-15 |
| R0615-09 | `doctor::field` first-match + spaced values | first `source=` wins (would undercount if a future tracing span carried `source`); a spaced wire name truncates the sample | CONFIRMED→deferred (nit) — filed | latent: ZERO tracing spans exist; no wire name has spaces; sample stays sanitized + count stays correct. Tracked with R0615-08 (one parse_drift_line hardening + per-case tests) | B | #304 | 2026-06-15 |

#### PR #306 (version-awareness) two-lens review — 3 findings (2 fixed, 1 filed)

| # | seam (file/mechanism) | claim (1 line) | verdict | anchor (paths + fix) | tier | head | date |
|---|---|---|---|---|---|---|---|
| R0615-10 | `doctor::probe_version` inherited stdin | `Command::output()` inherits the parent TTY stdin + has no timeout → a `<cli> --version` that reads stdin would hang doctor (contradicts its own "no hang" doc) | CONFIRMED→fixed (low) | `.stdin(Stdio::null())` (verifier: exactly right + sufficient; a wall-clock timeout is YAGNI). Latent only — all 8 registry `--version` argv short-circuit (<3s total) | A | #306 | 2026-06-15 |
| R0615-11 | `doctor.rs` module-doc "Strictly READ-ONLY" list | the enumeration (log + config + install-state) omitted the NEW `<cli> --version` subprocess probes | CONFIRMED→fixed (nit) | added the version-probe (stdin-nulled, static argv) to the READ-ONLY enumeration | A | #306 | 2026-06-15 |
| R0615-12 | `doctor::parse_version` first-dotted-run | a future anchored row whose `--version` banner prints a dotted date/build BEFORE the semver would silently mis-skew | CONFIRMED→deferred (nit) — filed | latent: copilot's banner leads with the version; cursor is `unknown` (never compared). UTF-8 slicing verified SAFE (panic worry refuted). Filed for a per-anchor banner-parse test / smarter extractor | B | #306 | 2026-06-15 |

#### PR #306 online security review — 2 MEDIUM (both fixed)

| # | seam (file/mechanism) | claim (1 line) | verdict | anchor (paths + fix) | tier | head | date |
|---|---|---|---|---|---|---|---|
| R0615-13 | `doctor::probe_version` no timeout | `Command::output()` blocks indefinitely — a `<cli> --version` that hangs (network call, prompt, PATH-substituted binary) hangs doctor; the stdin-null fix (R0615-10) only covers stdin-blocking. Two reviewers flagged the hang | CONFIRMED→fixed (med) | spawn + `try_wait` poll loop with a 5s deadline → `child.kill()` on timeout (no new dep, no orphan); stdin still nulled. Now genuinely no-hang | A | #306 | 2026-06-15 |
| R0615-14 | `doctor::probe_version` unsanitized output | the version-probe subprocess output flowed to `print!` RAW (not through `sanitize`) — a PATH-substituted binary's `--version` could emit ANSI/OSC to drive the terminal; the module's own R0615-06 claim ("untrusted values sanitized") didn't hold for this path | CONFIRMED→fixed (med) | extracted pure `first_sanitized_line` (control-char-strips before display) + a test (ANSI/OSC/BEL stripped, first-non-empty, empty→None). Now every displayed untrusted value is sanitized | A | #306 | 2026-06-15 |

#### PR #306 online code-review — 3rd distinct finding (+ a doc-orphan fix)

| # | seam (file/mechanism) | claim (1 line) | verdict | anchor (paths + fix) | tier | head | date |
|---|---|---|---|---|---|---|---|
| R0615-15 | `doctor::probe_version` nonzero-exit | `wait_with_output().ok()?` returns a FAILED `--version`'s captured output, so a CLI whose `--version` exits nonzero + prints an error ("cannot reach daemon") would display that error AS the version (the doc claimed "nonzero exit → None"; the code didn't) | CONFIRMED→fixed (med) | `if !output.status.success() { return None }` — a broken `--version` is "unknown", never its error text. Also fixed a doc-orphan from the timeout rewrite (the probe_version doc had detached onto first_sanitized_line); contract now matches code. All 8 real CLIs exit 0 → live output unchanged | A | #306 | 2026-06-15 |

#### PR #308 (live decode-drift footer nudge) two-lens review — 2 fixed + 1 deliberate-design + 1 deferral

| # | seam (file/mechanism) | claim (1 line) | verdict | anchor (paths + fix) | tier | head | date |
|---|---|---|---|---|---|---|---|
| R0615-16 | `tui/mod.rs` throttle comment | the `match`-over-`is_none_or` comment claimed "MSRV is 1.78" so `is_none_or` (1.82+) is forbidden — but the workspace `rust-version = "1.89"` and `is_none_or` is already used 15× incl. this file (l.882). The WHY comment was factually false (a stale 1.78-era memory); a future reader could cite it to reject a valid `is_none_or` | CONFIRMED→fixed (correctness lens, 100-conf) | switched to `last_drift_scan.is_none_or(\|t\| t.elapsed().as_secs() >= 15)` — cleaner, clippy-clean (the lint it was dodging is satisfied, not suppressed), false comment deleted | A | be7d119 | 2026-06-15 |
| R0615-17 | `tui/CLAUDE.md` "footer color-code state" | the entry described only the source-death footer path; this PR added a 2nd tier (decode-drift nudge merged via `doctor::footer_warning`) + the 15s log-scan seam — "keep docs current" owed in the same change | CONFIRMED→fixed (design lens) | rewrote the entry: two-tier merge (death short-circuits > drift nudge), the `RunConfig.log_path` throttle-scan, drift≠version-skew, headless `None`, the `.old`-rotation caveat | A | #308 | 2026-06-15 |
| R0615-18 | `tui/mod.rs` 15s log re-scan | the throttled log-scan derives a bool ("has drift") by re-reading history — superficially the "no scan-the-history" convention; risks becoming a precedent for per-feature log scanners | CONFIRMED→deliberate-design (design lens) | the no-history rule guards the REDUCER's lifecycle state, not a passive diagnostic read of the log artifact; documented as the ONE exception (WHY comment at the scan site + tui guide) incl. why a counting `tracing::Layer` was rejected (stateful blast radius on the single global file subscriber) — satisfies "deliberate design MUST add a sharp edge" | A | #308 | 2026-06-15 |
| R0615-19 | `crates/pixtuoid/CLAUDE.md` doctor.rs listing | the doctor.rs pure-fn listing named scan_log_for_source/format_doctor_row/parse_version/version_status but omitted the new `drifted_sources`/`footer_warning` (public API) | CONFIRMED→fixed (online code-review, med) | appended both to the listing with a one-line pointer to the live footer nudge | A | #308 | 2026-06-15 |
| R0615-20 | silent-dead source detection | the planned "connected + hooks installed + zero events ⇒ warn" was DROPPED from the PR | REFUTED-by-design→deferral filed (#309) | red-teamed OUT: zero-events can't distinguish "hooks broken" from "user isn't running that CLI" — both = no events → cries wolf, poisons the shared footer. A real signal needs process-presence / install-schema re-validation, not event-counting. Filed #309 | B | #308 | 2026-06-15 |
| R0615-21 | `tui/mod.rs` blocking `read_to_string` in async loop | the 15s scan does sync file I/O on the async executor thread; an FS stall hangs the render loop | ACCEPTED-residual (nice-to-have, both lenses) | bounded: 15s throttle + 5MB log-rotation cap; consistent with the existing `event::poll(tick)` blocking in the same loop (online review concurred). `spawn_blocking` is a clean future improvement if a stall is ever observed; not warranted now | C | #308 | 2026-06-15 |

#### Post-merge visual-verification of #308 — a double-glyph regression the snapshot caught

| # | seam (file/mechanism) | claim (1 line) | verdict | anchor (paths + fix) | tier | head | date |
|---|---|---|---|---|---|---|---|
| R0615-22 | `doctor::footer_warning` drift string | the drift message embedded its own leading `⚠`, but the footer painter (`hud.rs` `" ⚠ {warn} "`) already owns the glyph → live render double-printed `⚠ ⚠ decode drift` (the source-death message correctly carries NO glyph). Slipped BOTH the two-lens review and online review — neither rendered it; a `--drift-warning` snapshot did | CONFIRMED→fixed (visual) | dropped the leading `⚠` from `footer_warning` (painter is the sole glyph owner, matching `source_warning_message`) + a regression-guard assert (`!w.contains('⚠')`); added the `snapshot --drift-warning cc,cx` affordance (parallels `--source-warning`) that surfaced it. LESSON: a footer-text change MUST be rendered, not just unit-tested — the painter's wrapping is invisible to a string-equality test | A | 78e6b4e | 2026-06-15 |
| R0615-23 | `doctor.rs` glyph-guard test | the guard's comment claimed "neither message may embed ⚠" but only the DRIFT path was asserted; the death tier was checked with a hardcoded literal (`footer_warning(Some("source 'x' died"), …)`), so a future glyph in the REAL `source_warning_message` would slip through | CONFIRMED→fixed (online review, med) | route the actual `source_warning_message(&[SourceDeath::new(…)])` output through `footer_warning` and assert `!contains('⚠')` — closes the gap the comment promised (stronger than the bot's literal-based suggestion, which didn't close its own finding) | A | #310 | 2026-06-15 |

#### PR #311 — drains the deferred doctor-parser tail (R0615-08/-09/-12)

| # | seam (file/mechanism) | claim (1 line) | verdict | anchor (paths + fix) | tier | head | date |
|---|---|---|---|---|---|---|---|
| R0615-08 | `doctor::scan_log_for_source` loose `contains(TARGET)` | (deferred from #304) a non-drift line MENTIONING the target literal + carrying source/kind would be counted | CONFIRMED→fixed (#311) | `parse_drift_line` anchors on the structural tracing-fmt `target:` marker (space-preceded standalone token), not a substring; test `scan_ignores_a_body_mention_of_the_target_string`. Accepted residual documented (a value literally embedding ` <TARGET>: source=… kind=… ` — no in-tree emitter) | A | #311 | 2026-06-15 |
| R0615-09 | `doctor::field` first-match + spaced values | (deferred from #304) a future tracing span carrying `source` would undercount; a spaced wire name truncated the sample | CONFIRMED→fixed (#311) | fields parsed only AFTER the marker (span fields render before it → not picked up) + `field_value` runs an unquoted value to the next ` <ident>=` boundary (spaced value kept whole); tests `scan_parses_event_fields_not_a_span_field_of_the_same_name` + `scan_preserves_a_spaced_sample_value` | A | #311 | 2026-06-15 |
| R0615-12 | `doctor::parse_version` first-dotted-run | (deferred from #306) a banner printing a dotted date/build BEFORE the semver would silently mis-skew | CONFIRMED→fixed (#311) | smarter extractor (option b): prefer a `v`-prefixed run → else first plausible (`< IMPLAUSIBLE_MAJOR=1000`) major → else fallback first (CalVer-safe); test `parse_version_is_banner_order_robust`. No happy-path change (every real CLI banner parses identically) | A | #311 | 2026-06-15 |
| R0615-24 | PR #311 docstring/test ledger citations | the new parser cited `R0615-07` (the unrelated config-warnings finding) for the target-anchor + had the span/body citations off by one | CONFIRMED→fixed (design lens, premise-verified vs the ledger before acting) | re-cited to R0615-08 (anchor/body) and R0615-09 (span/spaced) across `parse_drift_line`/`DriftLine`/the two tests; this row is the missing-ledger-trace the same lens flagged | A | #311 | 2026-06-15 |

#### PR #312 (Reasonix PermissionRequest → Waiting) online security review — 1 MEDIUM

| # | seam (file/mechanism) | claim (1 line) | verdict | anchor (paths + fix) | tier | head | date |
|---|---|---|---|---|---|---|---|
#### PR #314 (silent-dead install-schema check + unified health board, #309) — 3-lens + code-architect

| # | seam (file/mechanism) | claim (1 line) | verdict | anchor (paths + fix) | tier | head | date |
|---|---|---|---|---|---|---|---|
| R0615-26 | `main.rs` boot preflight | the boot warning called `install::verify_target` directly, BYPASSING `doctor::diagnose` — splitting the very consolidation the PR claims ("all surfaces read the ONE rollup") | CONFIRMED→fixed (design lens, must-fix, 90) | routed boot through `doctor::diagnose(src, "")` (empty log = no drift scan; broken-only) + `is_broken()`; `diagnose` gates `has_hooks` internally so the outer guard dropped. Now every surface reads the one rollup | A | #314 | 2026-06-15 |
| R0615-27 | `install::verify_target` `ShimRef::Unknown` arm | Unknown (couldn't extract a shim path) was a HARD "broken" issue — a future source with a novel-but-valid command shape would false-flag broken | CONFIRMED→fixed (code-architect, finding 5) | demoted to a SOFT note: we can't CONFIRM the shim but can't prove broken; the genuine no-hooks case is already a HARD issue from the sentinel/event-set check, so nothing is masked. False-positive-free wins | B | #314 | 2026-06-15 |
| R0615-28 | doc gaps (main.rs TARGETS-not-REGISTERED_SOURCES; doctor::run fresh config read) | the boot loop's source-set asymmetry + doctor's independent config read were correct-but-unexplained | CONFIRMED→fixed (code-architect, findings 2+4) | one-line comments at both sites (transcript-only sources have no install schema; doctor is a separate-process snapshot diagnostic) | A | #314 | 2026-06-15 |
| R0615-29 | whole-flow architecture | is the Connection/install/health/drift/doctor coordination structurally sound (single-SoT, layering, bridge, actuator, extensibility)? | VALIDATED well-built (code-architect) | each concern one owner; deps one-way no cycles (core has no terminal deps); registry↔target bridge test-pinned 3 ways; panel is sole actuator (persist-then-install + rollback); doctor::diagnose the one health rollup; add-a-source fully test-guarded. Residual trade-offs (disconnect no-rollback on uninstall-fail; has_hooks Err→true) deliberate + pinned | — | #314 | 2026-06-15 |
| R0615-30 | schema-issue shim path → real terminal | online security (2 MED): the `shim binary missing: <path>` issue carries a path from the user's hand-editable hook command (via `shell_shim_ref`) and reached `doctor`'s `print!` + the boot `eprintln!` UNSANITIZED — ANSI/OSC injection (write-access-gated, but the one stdout boundary skipping the module's R0615-06 sanitize discipline) | CONFIRMED→fixed (both valid) | sanitized at the SOURCE (`verify::display_safe` in `verify_target`, where the untrusted path enters the issue Vec) NOT per-output-site as suggested — per-site already missed the doctor.rs surface this very review, so source-sanitize covers every present+future surface (doctor/boot/panel) in one place + unit-pinned | A | #314 | 2026-06-15 |
| R0615-25 | `reasonix.rs` PermissionRequest reason build | the `(Some,Some)` arm `format!`s `toolName`+`subject` into an intermediate String BEFORE the outer `ellipsize` caps it — an oversized `subject` causes a ~2× transient allocation | CONFIRMED→fixed (defense-in-depth, NOT a vuln — premise-verified: the payload is already parsed in-memory, bounded by the hook-socket framing, and a writer to that socket already controls the agent) | pre-cap each component (`ellipsize(t,…)`/`ellipsize(s,…)`) before concat, matching this file's own `rx_tool_detail`; outer ellipsize stays authoritative so display is identical. Both online lenses agreed it didn't block merge; applied for local-idiom consistency | C | #312 | 2026-06-15 |

#### PR #315 (doctor verdict-glyph + panel health flag + "Connection"→"Sources"/`s` rename) — two-lens review

| # | seam (file/mechanism) | claim (1 line) | verdict | anchor (paths + fix) | tier | head | date |
|---|---|---|---|---|---|---|---|
| R0615-32 | `tui/widgets/connection.rs` column header | online code-review (1 MED, both pre- and post-rename runs): the new fixed 2-col `health_flag` slot (added between the Connection and Live columns this PR) shifted every data row's live span right by 2, but the static header format string was untouched → the "Live" header sat 2 cols LEFT of its data column (data live at col 37, header at 35). Premise-verified by hand: `prefix(2)+badge(4)+space(1)+name(13)+conn(15)+flag(2)=37` vs header `2+18+15=35`. No test caught it (none asserts column alignment) | CONFIRMED→fixed (premise-verified, both online runs flagged the same) | extracted the header into `column_header()` (one source of truth — the test references the SAME fn `render` does, so a future drift can't slip a replicated literal) with two trailing spaces mirroring the flag slot; added `live_header_aligns_with_the_live_data_column` asserting `column_header().find("Live") == Σ(data spans before the live span)` for BOTH health states (None blank-flag / Some ⚠-flag, each 2-wide). Regenerated `docs/images/connection.png` (the rename + this fix change the panel render → `gen-check` would else go red) | A | #315 | 2026-06-15 |
| R0615-31 | the `c`→`s` / "Connection panel"→"Sources panel" rename | BOTH lenses (correctness + design/blast-radius): the rename was applied to the dispatch keybind, panel title/footer, in-panel help, and README — but MISSED the many other user-facing surfaces that name the panel/key: the boot preflight warning, the `--version` release-note popups (7 strings), the opencode on-disk plugin artifact + its uninstall stub comment, the Windows 8.3-reject hint, several `docs/*.md`, `npm/README.md`, `site/`'s HowItWorks, the CI/release workflow comments, and the per-crate CLAUDE.md/source-module prose — an inconsistent vocabulary where half the product still says "Connection panel / press c" | CONFIRMED→fixed (both lenses, sole must-fix) | swept EVERY user-facing + comment surface to "Sources panel"/`s` via grep-verified pass (`version.rs` ×7, `main.rs` boot msg, `opencode.rs`+`opencode_plugin.ts` disk comment, `hook_cmd/windows.rs`, `config.rs`/`runtime/*`/`tui/*` doc-comments, `doctor.rs`, all `docs/*.md`, `npm/README.md`, `site/`, `.github/`, the 3 nested CLAUDE.md + 5 core source modules). DELIBERATELY PRESERVED: internal identifiers (`connection_open`, `ConnectedSources`, `ToggleConnection`, `tui/connection/`, `widgets/connection.rs`) — renaming code symbols is churn with no user-facing payoff; and the historical ledger rows + the intentional rename-history notes in the tui guide (they DOCUMENT the old name). Reviewers' verdict on the rest of the PR (the `↳` multi-line doctor verdict-glyph format, the `SourceDiagnostics` rollup, the health-flag-vs-ConnState separation) — clean, well-designed | A | #315 | 2026-06-15 |

## 2026-06-15 — OpenClaw "Molty" mascot PR #316 (78-agent review: 8 dimensions × 3 adversarial lenses; 17 confirmed / 6 refuted)

| ID | seam | claim | verdict | mechanism/fix | tier | HEAD | date |
|---|---|---|---|---|---|---|---|
| R0615-OC1 | `runtime/driver.rs` presence arm + `reducer::reconcile_connected` | MEDIUM: the presence side-channel BYPASSES the connection gate — `apply_presence` runs unconditionally and `reconcile_connected` ignores `source_presence`, so a panel-disconnected gateway keeps rendering Molty (and `last_seen` refreshes forever → TTL never fires) | CONFIRMED→PR#316 | gate the presence arm on `connected.is_connected(openclaw::SOURCE_NAME)` (drop hooks when disconnected); sweep-tick `openclaw::mark_presence_down` walks the mascot out; `sweep_presence_ttl` now `retain`s + removes `Down` after `DOWN_REMOVE_MS`. Core (`mark_presence_down…`) + harness pins | A | PR#316@cd3f2ca | 2026-06-15 |
| R0615-OC2 | `source/openclaw.rs::sweep_presence_ttl` | LOW (L1): TTL→Down doesn't re-anchor `last_seen` → on the SIGTERM/5-min path `now − last_seen ≫ LEAVE_MS` so the mascot VANISHES with no walk-out | CONFIRMED→PR#316 | re-stamp `last_seen = now` on the TTL Down transition (mirrors the explicit Down paths) + `sweep_takes_the_daemon_down…` asserts the anchor | A | PR#316@cd3f2ca | 2026-06-15 |
| R0615-OC3 | `install/target.rs` `extra_artifacts` doc | LOW (L2): doc said "written idempotently (only if content differs)" but `install_target` writes unconditionally | CONFIRMED→PR#316 | corrected the doc to "rewritten verbatim on every (re)install" (the deliberate plugin-refresh); NO content-diff added (would contradict the documented refresh) | A | PR#316@cd3f2ca | 2026-06-15 |
| R0615-OC4 | `install/target.rs` OPENCLAW const | LOW but MERGE-BLOCKER (L5): branch predates #314's now-required `Target.verify_schema` → clean merge that won't compile | CONFIRMED→PR#316 | merged `origin/main`; authored `openclaw::verify_schema` (config-level enabled-entry + `load.paths` checks; `ShimRef::Unknown` since the shim lives in the plugin file) + wired the field. `verify_target_is_sound_after_a_real_install_for_every_target` covers it | A | PR#316@cd3f2ca | 2026-06-15 |
| R0615-OC5 | `tests/sources/conformance.rs` presence-only branch | LOW (L4): the byte-real fixture was never fed through `decode_openclaw_hook_payload`, so the assertion was vacuous (a `runId→run_id` rename would pass) | CONFIRMED→PR#316 | added `hooks_raw`; the branch now decodes each captured line through the presence decoder and asserts a non-empty delta set + a non-empty fixture (byte-real pin, Copilot #294 standard) | A | PR#316@cd3f2ca | 2026-06-15 |
| R0615-OC6 | test coverage (M2/M3/M4/N2/N3/TA-7) | new behaviour-bearing paths untested: busy bubbles, `entered_at` resurrection re-anchor, floor-0 projection, tooltip text, `handle_conn` presence duplex, `run_key` fallback | CONFIRMED→PR#316 | busy-bubble harness test (runs≠sessions, idle-never-bubbles), `entered_at_reanchors…`, `source_presence_projects_onto_the_ground_floor_only`, `mascot_tooltip_verb…`, `handle_conn_routes_openclaw_presence_to_the_side_channel_only`, `run_key_fallbacks_are_coarse_by_design` | B | PR#316@cd3f2ca | 2026-06-15 |
| R0615-OC-N1 | `drawable.rs` Down-branch walk-out origin | NIT: Busy→Down reconstructs `from` with IDLE spots → a 1-frame jump on a gateway killed mid-run | REFUTED (deliberate) → doc breadcrumb | the mascot is STATELESS (position = pure fn of `now`; no prev-state field on `DaemonState`), so Idle is the only reconstructable wander from a `Down` presence — sharp edge ADDED inline at `drawable.rs` (the walk-out `from` comment) per rule #5 | A | PR#316@cd3f2ca | 2026-06-15 |
| R0615-OC-R1 | `openclaw_plugin.js` allowlist | the plugin forwards 5 keys without JS `typeof` narrowing | REFUTED (deliberate) | the Rust decoder is the load-bearing sanitizer (reads only allowlisted scalars via `as_str`/`as_i64`); pinned by `message_content_and_session_file_never_reach_the_updates`; the field-name allowlist is spec-mandated; JS `typeof` is redundant | A | PR#316@cd3f2ca | 2026-06-15 |
| R0615-OC-R2 | `check_upstream_drift.py` openclaw | the drift watch ignores an upstream deprecation signal | REFUTED | the watched `hook-types.ts` is a TS union of quoted string LITERALS — no deprecation metadata exists to parse; a deprecated-but-present hook still fires (mascot doesn't go dark); the watch correctly fires on REMOVAL | A | PR#316@cd3f2ca | 2026-06-15 |
| R0615-OC-R3 | `hook/mod.rs` presence peek hardcodes openclaw | the presence decode is hardcoded to one source (genericity) | REFUTED (deliberate YAGNI) | single-daemon scope is explicit; the genericity claim is about the `source_presence` DATA model, not decode dispatch; multi-daemon deferred (#279-#281) | A | PR#316@cd3f2ca | 2026-06-15 |
| R0615-OC-R4 | `openclaw.rs` GatewayUp `_pid` | the gateway-pid exit watch assumes the plugin runs in-process | REFUTED | capture-verified in-process (the plugin's `process.pid` IS the gateway, confirmed live: kill→walk-out); `PRESENCE_TTL_MS` is the backstop; mirrors the shipped opencode `_pid` pattern | A | PR#316@cd3f2ca | 2026-06-15 |
| R0615-OC-R5 | `claude_code.rs` owns `presence_tx` | CC owning the openclaw presence sender grows a demux hub | REFUTED | CC binds the ONE shared hook socket every source rides (load-bearing, invariant #2 + the #246 child-end-unclaim precedent); `presence_tx` mirrors the existing `child_end_unclaims`/`pid_watch` fields | A | PR#316@cd3f2ca | 2026-06-15 |
| R0615-OC-D1 | a 4th "degraded" state | gateway UP but model-backend auth-broken (the real Apr-2026 Anthropic-ban failure mode) renders as idle, not degraded | CONFIRMED→FIXED (#317, this PR) | the signal EXISTS: `agent_end.success: boolean` (PluginHookAgentEndEvent, capture-verified in the SDK d.ts). Plugin forwards the `success` bool (NOT the error string — privacy); `success:false`→`RunFailed`→`DaemonState::Degraded` (4th state); visual = `degraded_pixel`/`degraded_frame` sickly-red tint + `MASCOT_DEGRADED_CYCLE_MS=14000` sluggish wander + tooltip "model error"; heals on clean RunEnded / new RunStarted / GatewayUp. No-deferral directive — done in-PR, not a future issue | B | PR#316@working | 2026-06-15 |

### PR #316 — automated bot review (findings the 78-agent workflow review MISSED — calibration)

| ID | seam | claim | verdict | mechanism/fix | tier | HEAD | date |
|---|---|---|---|---|---|---|---|
| R0615-OCB1 | `install/mod.rs` round-trip + verify tests | HIGH (bot-caught, workflow MISSED): `extra_artifacts` resolve from `openclaw_home()`, NOT the config override, so the round-trip/verify tests write the plugin DIR into the developer's REAL `~/.openclaw` (and the Windows `ends_with("plugins/pixtuoid")` assertion failed on `\`) | CONFIRMED→PR#316 | isolate both tests under a temp home via `OPENCLAW_STATE_DIR` (nextest = process-per-test); `merge_install_adds_load_path…` made separator-tolerant | A | PR#316@63355f6 | 2026-06-15 |
| R0615-OCB2 | `install/mod.rs` extra_artifacts write | HIGH/MED (bot, escalates the workflow's refuted L3): plain `fs::write` is non-atomic AND follows a symlink planted at the plugin path | CONFIRMED→PR#316 | route through `io::write_config_atomic` (temp→fsync→rename REPLACES a symlink; no torn plugin); each artifact its own lock target, disjoint from the config lock | A | PR#316@63355f6 | 2026-06-15 |
| R0615-OCB3 | `openclaw.rs` `_pid as i32` | LOW (bot): a crafted `_pid: 2^32+1` truncates to `1` → arms ExitWatch on PID 1 | CONFIRMED→PR#316 | checked `i32::try_from(p).ok()` (out-of-range → None) + `gateway_start_out_of_range_pid…` pin | A | PR#316@63355f6 | 2026-06-15 |
| R0615-OCB4 | pixtuoid-core public API (semver) | `SceneState.source_presence` + `ClaudeCodeSource.presence_tx` new pub fields break semver with no version bump | CONFIRMED→PR#316 | encapsulated both (private field + additive pub accessors / builder) — semver-clean WITHOUT a premature release bump (project bumps at release), and tightens the leaky pub-mutable-`BTreeMap` surface | A | PR#316@63355f6 | 2026-06-15 |

### PR #316 — daemon/agent decouple rework (SourceKind + HookRouter + shared multi-daemon layer; 5-lens × adversarial-verify review: 3 confirmed / 1 refuted, all confirmed doc-only)

User directive: "design for multiple daemons, don't assume one"; "no phasing, do it all in one". This rework SUPERSEDES three prior OpenClaw-PR adjudications: **R0615-OCB4** (the "encapsulate, don't bump" semver dodge is no longer viable — the `SceneState.source_presence`→`daemons` rename + `DaemonPresenceUpdate` re-home + `ClaudeCodeSource` field removals are unavoidably breaking, so 0.8.0→**0.9.0** is the honest fix, and `v0.8.0` is already tagged so the dev window is closed); **R0615-OC-R3** (presence demux hardcoded to openclaw — now registry-driven `presence_decoder_for`); **R0615-OC-R5** (CC owns `presence_tx` — now the `HookRouter` does). Design-lock + review both via multi-agent workflows.

| ID | seam | claim | verdict | mechanism/fix | tier | HEAD | date |
|---|---|---|---|---|---|---|---|
| R0615-OCD1 | `hook/mod.rs` `SocketBusy` type-doc (:32-35) + `with_presence` builder-doc (:98-99) | LOW (lens degradation-tee-lifecycle): both seam docs still name `ClaudeCodeSource::run` as the SocketBusy catcher / presence wirer + "degrades to transcript-only", but the socket lifted to `HookRouter` — a maintainer touching the SocketBusy-must-not-kill contract lands on the wrong owner (the parallel test comments WERE updated, proving the re-home was intended) | CONFIRMED→fixed | re-pointed both to `HookRouter::run` + "degrades the HOOK PLANE to a quiet `Ok(())`, transcript watchers run as independent tasks" | A | PR#316@working | 2026-06-15 |
| R0615-OCD2 | `jsonl/unclaim.rs` ChildEndUnclaims module-doc (:39-42) | LOW (lens sourcekind-consumers): the #246 PRODUCER doc still reads "the tee inside `ClaudeCodeSource::run` — the ONE shared socket IT owns"; the tee moved to `HookRouter::run`. The struct-field twins on CC + Codex WERE updated, so this module-level comment is the lone straggler | CONFIRMED→fixed | re-pointed to `HookRouter::run` (owns the shared socket); dropped the obsolete "if sockets ever split per-source" parenthetical (the socket now has a dedicated owner) | A | PR#316@working | 2026-06-15 |
| R0615-OCD3 | `claude_code.rs::default_socket_path` comment (:165-167) | NIT (lens invariants-blast-radius): comment says an empty `PIXTUOID_SOCKET` `""` "makes bind() fail fatally — killing the whole CC source"; CC no longer binds (HookRouter does), AND the empty case never reaches bind() (treated as unset → XDG default by the `!p.trim().is_empty()` guard) | CONFIRMED→fixed | rewrote: the fn stays on CC as the shim↔daemon parity-bridge anchor (CC doesn't bind); empty falls through to the default | A | PR#316@working | 2026-06-15 |
| R0615-OCD-R1 | multi-daemon coverage at the `reducer_task` runtime path | the multi-daemon claim is proven only at the daemon.rs state-machine layer (parameterized openclaw + synthetic `daemon2`); no test drives `reducer_task` end-to-end with two source-tagged daemons | REFUTED (not a defect) | factually accurate but not refactor-introduced and not worth a fix: `reducer_task` (driver.rs) is the documented **codecov-ignored async glue (#103)**, and every building block it calls — `apply_presence`/`sweep_presence_ttl`/`mark_presence_down`/`daemon_sources()`/the demux — IS unit-tested against `daemon2`; the glue is the one untestable seam by design | B | PR#316@working | 2026-06-15 |

### PR #316 — MERGE-GATE review (mandatory 2-lens + lifecycle escalation; 3 lenses: correctness/design APPROVE-WITH-NITS, lifecycle APPROVE; 0 REQUEST-CHANGES). Both code reviewers ran the gates themselves (just preflight EXIT=0/1692, cargo semver-checks EXIT=0).

| ID | seam | claim | verdict | mechanism/fix | tier | HEAD | date |
|---|---|---|---|---|---|---|---|
| R0615-OCM1 | `openclaw.rs:17` module doc | NIT (corr, conf 95): says the plugin is `openclaw_plugin.ts` but the real file + `include_str!` is `.js` (opencode-carryover) | CONFIRMED→fixed | `.ts`→`.js` (verified the only plugin source is `openclaw_plugin.js`) | A | PR#316@working | 2026-06-15 |
| R0615-OCM2 | `theme/mod.rs` SourceColors doc | NIT (design, conf 90): "one color per agent source (cc/cx/rx/ag/cw)" — lists 5 of 9 + frames openclaw (a DAEMON) as an agent | CONFIRMED→fixed | rewrote: 8 agent CLIs + the OpenClaw daemon, count-pinned to REGISTERED_SOURCES; dashboard=agents, Sources panel=all | A | PR#316@working | 2026-06-15 |
| R0615-OCM3 | `openclaw.rs::run_key` doc | NIT (design, conf 86): "fall back to sessionId" but `or_else` fires on runId ABSENCE not emptiness → empty runId collapses to `"_"` (test-pinned by `run_key_fallbacks_are_coarse_by_design`) | CONFIRMED→fixed | tightened the comment to match the test-pinned behavior (coarse by design — busy intensity only) | A | PR#316@working | 2026-06-15 |
| R0615-OCM4 | `scripts/openclaw-live-e2e.sh` | LOW (design, conf 70): assumed OpenClaw already connected (only $PROJ isolated) → would time out on a clean box (presence connection-gate drops a disconnected daemon's deltas) | CONFIRMED→fixed | self-contained via `XDG_CONFIG_HOME=$(mktemp -d)` + an isolated `[sources] openclaw=true` config (NOT `--config` — `run` has no such flag); re-verified PASS | A | PR#316@working | 2026-06-15 |
| R0615-OCM5 | `install/openclaw.rs` external-artifact claims | LOW (design, conf 55): the `2026.6.6` verified_version + config/wire shape rest on a capture not independently re-verified by the reviewer in-session | ACCEPTED (verified in THIS session) | the maintainer DID live-verify this session: a real `openclaw gateway run` loaded the `pixtuoid` plugin → Molty idle/down, and `openclaw status` showed `Version 2026.6.6`. Drift-watch (`check_upstream_drift.py`) + the OPENCLAW_EVENTS⇔decoder consistency test cover future skew. Reconfirm version/shape on the next OpenClaw release | B | PR#316@working | 2026-06-15 |
| R0615-OCM6 | `daemon.rs` `current_pid` / PidExited arm | LOW (lifecycle, conf 82): mid-attach OR reconnect-while-alive leaves current_pid=None (gateway stamps _pid only on gateway_start) → instant abrupt-down unavailable → falls back to the 5-min `presence_ttl_ms` sweep. The daemon analog of opencode's R0613-09; bounded + self-healing (TTL always reaps); the mid-attach variant is partially inherent (non-start events carry no pid) | CONFIRMED→FIXED (#318, this PR) | the "non-start events carry no pid" premise no longer holds: the plugin now stamps `_pid` on EVERY event; decoder prepends `DaemonPresenceUpdate::PidSeen{pid}` to every non-`gateway_start` mapped event; `apply_presence` adopts it into `current_pid` when None (never clobbers a known pid — GatewayUp owns rebinds); driver arms the exit watch on `PidSeen` too. So a mid-attach/reconnect arms the instant abrupt-down off the next event. No-deferral directive — done in-PR. Unit-pinned (`pid_seen_adopts_when_current_pid_is_none`, `pid_seen_never_clobbers_a_known_pid`, decoder `non_gateway_start_event_with_pid_prepends_pid_seen`) + byte-real fixture `gateway_mid_attach_failed_run` | B | PR#316@working | 2026-06-15 |

### PR #316 — #317/#318 MERGE-GATE RE-REVIEW (the new degraded+mid-attach surface; mandatory 2-lens: correctness/grounding + design/blast-radius; both APPROVE-WITH-NITS, 0 CRITICAL/HIGH/MEDIUM, 0 REQUEST-CHANGES). Reviewers traced the state machine + privacy allowlist + exhaustiveness independently; preflight EXIT=0/1705.

| ID | seam | claim | verdict | mechanism/fix | tier | HEAD | date |
|---|---|---|---|---|---|---|---|
| R0615-OCM7 | `daemon.rs::sweep_presence_ttl` Degraded coverage | NIT (corr, conf ~60): no explicit unit test pins that a Degraded daemon is NOT busy-decayed (only Busy is) yet still goes Down at `presence_ttl_ms`. Logic is structurally correct; coverage gap only | CONFIRMED→fixed | added `sweep_does_not_busy_decay_a_degraded_daemon_but_ttl_takes_it_down` (parameterized over both synthetic daemons) | A | PR#316@working | 2026-06-15 |
| R0615-OCM8 | `examples/snapshot.rs` `--crop-mascot` doc | NIT (design, conf 75): doc says "Requires --openclaw" but there is no clap `requires` — enforced at runtime via `ok_or_else`. Reviewer noted the runtime check is arguably MORE correct (`down` past the leave window also has no mascot, inexpressible as a static dep) | CONFIRMED→fixed (doc) | reworded the doc to "Needs a VISIBLE mascot … enforced at runtime, not by clap, since 'visible' isn't expressible as a static flag dependency" — matches the runtime `ok_or_else`; kept the runtime enforcement (correct) | A | PR#316@working | 2026-06-15 |
| R0615-OCM9 | `drawable.rs` Degraded→Down walk-out origin | NIT (corr, conf ~50): the stateless `Busy→Down` walk-out reconstructs `from` with the IDLE spot set; `Degraded→Down` inherits the same 1-frame jump | REFUTED (deliberate, inherited) | same accepted cosmetic edge as R0615-OC-N1 (the mascot is STATELESS — position is a pure fn of `now`; `DaemonState` carries no prev-state, so Idle is the only reconstructable wander from a non-Idle→Down). Degraded inherits it harmlessly; sharp edge already documented inline | A | PR#316@working | 2026-06-15 |

### PR #316 — POST-MERGE claude-action ONLINE review (PROCESS MISS: 026a06c was merged on required-CI-green WITHOUT reading the online bot review first — local 2-lens + preflight only). All 7 findings adversarially re-verified against the MERGED code (6-agent workflow): **6 of 7 STALE/already-fixed before merge** (calibration: "don't blindly accept reviewer findings" — they were flagged on EARLIER commits the bot re-reviewed). ONE real (a test gap) fixed in follow-up branch `fix/pr316-followup-summarize-degraded`.

| ID | seam | claim | verdict | mechanism/fix | tier | HEAD | date |
|---|---|---|---|---|---|---|---|
| R0615-OCP1 | `install/mod.rs` round-trip test | HIGH (bot, re-flagged "still not fixed after 63355f6"): the TARGETS-loop test writes OpenClaw `extra_artifacts` to the REAL `~/.openclaw` | REFUTED (STALE — already fixed) | the merged test sets `OPENCLAW_STATE_DIR=tmpdir` BEFORE the loop (mod.rs:877-878) and `openclaw_home()` honors it first; live `~/.openclaw/plugins/pixtuoid/index.js` verified intact (real cargo-bin HOOK_PATH, not `/fake`). = R0615-OCB1, fixed pre-merge. Bot re-reviewed an old commit | A | main@ca0a4fa | 2026-06-15 |
| R0615-OCP2 | `install/mod.rs` verify_target test | HIGH (bot): `verify_target_is_sound_…` also writes the real `~/.openclaw` | REFUTED (STALE) | same fix already present (mod.rs:926-927 sets `OPENCLAW_STATE_DIR`). = R0615-OCB1 | A | main@ca0a4fa | 2026-06-15 |
| R0615-OCP3 | `openclaw.rs` `_pid as i32` | LOW (bot): silent truncation of out-of-range `_pid` | REFUTED (STALE) | merged code uses `i32::try_from(p).ok()` + `gateway_start_out_of_range_pid_is_dropped_not_truncated` pin. = R0615-OCB3/OC-D earlier | A | main@ca0a4fa | 2026-06-15 |
| R0615-OCP4 | `install/target.rs` extra_artifacts docstring | MEDIUM (bot): stale "Written idempotently (only if content differs)" contradicts the unconditional write | REFUTED (STALE) | the merged docstring ALREADY reads "Rewritten verbatim on every (re)install … `install_target` does an unconditional write, NOT a content-diff"; the quoted bad wording does not exist (grep: no match) | A | main@ca0a4fa | 2026-06-15 |
| R0615-OCP5 | `hook/mod.rs` `handle_conn`→`presence_tx` | MEDIUM (bot): the openclaw presence-demux wiring has no unit test | REFUTED (STALE) | `handle_conn_routes_openclaw_presence_to_the_side_channel_only` (hook/mod.rs:423-473) already drives `presence_tx=Some`, asserts the openclaw payload routes a `GatewayUp` to presence + the CC line stays off it + zero AgentEvents — verifier RAN it, passed | A | main@ca0a4fa | 2026-06-15 |
| R0615-OCP6 | `runtime/mod.rs` `summarize()` Degraded arm | MEDIUM (bot, conf high): the `Degraded => "degraded"` arm has no unit test — `summarize_reports_daemon_presence` covers idle/busy/down only | CONFIRMED→FIXED | added a `RunFailed`→`daemons=[openclaw:degraded]` assertion to `summarize_reports_daemon_presence` (the e2e covered it, the unit test now does too) | A | fix/pr316-followup | 2026-06-15 |
| R0615-OCP7 | PR #316 body | MEDIUM (bot): the body's "Follow-ups (not in this PR)" still lists the Degraded state as deferred, but #317/#318 landed in this PR | CONFIRMED→FIXED (doc) | edited the PR #316 body to record that #317 (degraded) + #318 (mid-attach pid) were resolved in-PR via the `agent_end.success` boolean signal | A | PR#316 body | 2026-06-15 |

### Pre-0.9.0 whole-codebase COUPLING/CONSOLIDATION review (core-focused, 56-agent workflow: 9 subsystem finders → adversarial verify vs the code + sharp edges). 46 findings → **19 CONFIRMED (all implemented), 2 DEFERRED, 25 REFUTED-as-deliberate**. Zero correctness bugs — the core's intentional flatness held (the high refutation rate validates the prior "well-built + scalable" verdicts). All 19 confirmed are behavior-preserving extractions; preflight 1707 green.

| ID | seam | claim | verdict | mechanism/fix | tier | HEAD | date |
|---|---|---|---|---|---|---|---|
| R0615-CR1 | `daemon.rs` go-Down trio | the `state=Down + active_sessions=0 + in_flight.clear()` reset hand-rolled at 4 sites | CONFIRMED→FIXED | `DaemonPresence::enter_down()`/`clear_concurrency()` (daemon mirror of fsm's single-owner transitions) | A | review/core-coupling | 2026-06-15 |
| R0615-CR2 | `driver.rs` vs `apply_presence` | the variant→exit-watch-pid match duplicated in driver + adoption arms | CONFIRMED→FIXED | `DaemonPresenceUpdate::armable_pid()` (one place) + unit pin | A | review/core-coupling | 2026-06-15 |
| R0615-CR3 | `drawable.rs` mascot tables | `gateway_mascot_anims` + `gateway_display_name` two parallel `match source` | CONFIRMED→FIXED | one `gateway_mascot_def(source) -> GatewayMascotDef` (a 2nd daemon = one arm) | A | review/core-coupling | 2026-06-15 |
| R0615-CR4 | `install` reports/Target | dead `restart_noun`/`post_install_note` (written, never read) | CONFIRMED→FIXED | removed from Target + both reports + all rows/fixtures (kept config_path) | A | review/core-coupling | 2026-06-15 |
| R0615-CR5 | `Target` bools | `needs_path_warning` + `needs_resolved_binary` bool-soup | CONFIRMED→FIXED | one `BinaryStrategy { BareNameOnPath \| EmbedAbsolute }` enum | A | review/core-coupling | 2026-06-15 |
| R0615-CR6 | `install/*` parse+merge | `parse_or_empty` ×6 + Reasonix/Cursor whole flat-JSON merge body dup | CONFIRMED→FIXED | shared `parse_{json,toml}_or_empty` + `flat_json_merge_{install,uninstall}` in verify.rs | A | review/core-coupling | 2026-06-15 |
| R0615-CR7 | `hook/windows.rs` | `ServerOptions` pipe-builder chain + SAFETY comment dup ×3 | CONFIRMED→FIXED | one `create_hook_pipe(name, attrs, first)` (cfg(windows) — windows-check CI verifies) | A | review/core-coupling | 2026-06-15 |
| R0615-CR8 | `pose` walking frame | `(elapsed/MS)%FRAMES` index recomputed ×8 across 2 crates | CONFIRMED→FIXED | `pose::walking_frame(elapsed_ms)` | A | review/core-coupling | 2026-06-15 |
| R0615-CR9 | `jsonl/mod.rs` scan pass | refresh→drain→scan→proof sequence copy-pasted ×3 | CONFIRMED→FIXED | one `run_scan_pass(…, drain)` (seed passes drain=false) | A | review/core-coupling | 2026-06-15 |
| R0615-CR10 | `SceneLayout` meeting | `meeting_sofas`+`meeting_tables` parallel Vecs, room-grouping by index math | CONFIRMED→FIXED | one `Vec<MeetingRoom { sofas: [Point;2], table }>`; mask byte-identical | A | review/core-coupling | 2026-06-15 |
| R0615-CR11 | `daemon.rs` PresenceMsg | `(String, Update)` tuple used positionally at the routing seam | CONFIRMED→FIXED | named `PresenceMsg { source, delta }` struct | A | review/core-coupling | 2026-06-15 |
| R0615-CR-D1 | `physics`/walkable grids | 3× `(w,h,Vec)` grids re-implement `y*w+x` + bounds | DEFERRED | a `Grid<T>` newtype is a real but separate refactor (walkable + occupancy + reach); follow-up | B | review/core-coupling | 2026-06-15 |
| R0615-CR-D2 | exit-watch scaffolds | `ExitWatch` + `HookPidWatch` near-identical pid-drain scaffolds | DEFERRED | genericizing over value-type + emitted-message is a real but larger seam; follow-up | B | review/core-coupling | 2026-06-15 |
| R0615-CR-REF | 25 REFUTED-as-deliberate | coupling findings the sharp edges justify | REFUTED (deliberate) | e.g. `build_walkable_mask`'s 17 params (SceneLayout fields, documented), `(AgentId,String)` keys + `tuid.to_string()` (perf-vs-newtype tradeoff), the Correlation-vs-Reducer map ownership split (Layer-A/B doc), `run_tui`'s 11 params + `build_source_set`'s per-source-typed config ("genuinely flat — don't fix"), `verified_version`/`version_probe` split, the dual REGISTERED_SOURCES list (const-projection limitation, bridge-tested), DaemonState-vs-(busy,degraded) bools — all cite documented sharp edges; the verifiers killed them rather than churn deliberate design | A | review/core-coupling | 2026-06-15 |

| R0615-CR-ON1 | PR #321 online claude-action review | MEDIUM (bot): `pixtuoid-core/CLAUDE.md:25` still says `PresenceMsg=(source,delta) tuple` after this PR promoted it to a struct (doc-currency rule) | CONFIRMED→FIXED | updated CLAUDE.md:25 to `PresenceMsg{source,delta} struct`; PROACTIVELY caught + fixed two more stale refs the bot missed from the same PR — `pixtuoid/CLAUDE.md` `needs_resolved_binary`→`binary_strategy=EmbedAbsolute`, `tui/CLAUDE.md` `meeting_tables`→`meeting_rooms`. Security review clean, 0 invariant violations, dead-field removal verified, codecov green | A | review/core-coupling | 2026-06-15 |

### Naming-ambiguity pass (post-consolidation, "would a human reading the logic be misled?" — names that lie about what they do/hold). 6 CONFIRMED renames (identifiers + corrected prose), behavior-preserving; preflight green. Triggered by `meeting_room` (a `Bounds`) vs `meeting_rooms` (the new `Vec`) sitting one `s` apart in the same struct.

| ID | name | why it misleads | rename | HEAD | date |
|---|---|---|---|---|---|
| R0615-NM1 | `SceneLayout::meeting_rooms` / `MeetingRoom` | one `s` from the unrelated `meeting_room: Option<Bounds>` in the SAME struct — a human edits the wrong one | `meeting_furniture` / `MeetingFurniture` (holds sofas+table, not a room region) | review/core-coupling | 2026-06-15 |
| R0615-NM2 | `pose::cycle_ms_for` (+ `CYCLE_MS_*` consts) | "cycle" reads as the wander cycle length; it's the off-screen-resume sentinel gap (a different quantity) — prose doc already said so, the name didn't | `stale_resume_gap_ms` / `STALE_RESUME_GAP_{BASE,RANGE}_MS` | review/core-coupling | 2026-06-15 |
| R0615-NM3 | `SceneLayout::walkway` | doc said "corridor used by cat"; it's only the cubicle-band-width appliance aisle, NOT the full-width corridor — a human would route the cat through the wrong band | `cubicle_aisle` (+ corrected doc) | review/core-coupling | 2026-06-15 |
| R0615-NM4 | `approach::approach_footprint` | returns the VISUAL clearance extent, not the ground footprint — inverts invariant #6's footprint-vs-visual vocabulary at the one place it matters | `approach_clearance_extent` | review/core-coupling | 2026-06-15 |
| R0615-NM5 | `DoctorSourceRow::name` | holds the registry id (`claude-code`), collides conceptually with `Target::name` (the human label) — a human joins on the wrong field | `source_id` | review/core-coupling | 2026-06-15 |
| R0615-NM6 | `drawable::paint_coffee_table` (caller) | the helper paints the meeting-room table, not a coffee/pantry table — leftover name from before the meeting split | `paint_meeting_table` | review/core-coupling | 2026-06-15 |

| R0615-NM-ON1 | PR #321 online claude-action review (naming commit `431f4e2`) | 2 MEDIUM (bot): `furniture.rs:12` docstring still "Low coffee table in front of the lounge couch" + `tui/CLAUDE.md:80` "furniture.rs (coffee table, …)" — stale after the `paint_coffee_table`→`paint_meeting_table` rename | CONFIRMED→FIXED | premise verified (the fn is wired to `DrawableKind::MeetingTable`/`Furniture::MeetingTable`; no `CoffeeTable` enum exists anywhere). Swept ALL 8 stale "coffee table" prose refs the bot's 2 were a subset of: furniture.rs module-doc + 3 docstrings, drawable.rs + mod.rs + decor.rs comments, tui/CLAUDE.md:80. Security review clean (0 findings, rename-only delta), 23/23 checks green | A | review/core-coupling | 2026-06-15 |

## 2026-06-16 — census #266 follow-up adjudications (the two DEFERRED #321 refactor rows reach terminal states)

Source: the second review-history census ([`review-metrics/mining-2026-06-262-328.md`](review-metrics/mining-2026-06-262-328.md), disposition leg) flagged R0615-CR-D1/D2 as DEFERRED ledger rows with no tracking issue (a silent-drop per the deferred-finding rule). Filed as #333/#334, then adjudicated. Append-only: these SUPERSEDE the DEFERRED verdicts of R0615-CR-D1/D2.

| # | seam (file/mechanism) | claim (1 line) | verdict | anchor (paths + cited sharp edge / fix PR) | tier | head | date |
|---|---|---|---|---|---|---|---|
| C266-01 | `walkable.rs` / `layout/reach.rs` grids (supersedes R0615-CR-D1) | `WalkableMask` + `ReachSet` re-implement the same `y*w+x` row-major indexing + bounds + alloc | CONFIRMED→#333 | extracted `core::grid::Grid<T>` (checked filled/get/set/get_or). **`WalkableMask` is now the clean `pub type WalkableMask = Grid<bool>` ALIAS** (obstacle ops on `impl Grid<bool>`) — NOT the earlier struct-wrapper that mirrored `width`/`height` on both itself and the inner grid (a redundancy kept only to dodge `struct_missing`). `ReachSet` wraps a `Grid<bool>` (private field, no mirror). Removing the published struct is a breaking change, so it rides a **0.10.0** major bump (cargo-semver-checks: `v0.9.0 -> v0.10.0 (major change)`, no update required). Behaviour byte-identical: walkable+reach+approach+layout suites + binary snapshot golden green; the 0.10.0 version string in the office wall display drifted the 25 gen-check stills → regenerated. A whole-pixtuoid-core audit for OTHER bump-avoidance ugliness found NONE (this was the only one). Only 2 grid sites existed, not the suspected 3 | A | fix/census-266-followups | 2026-06-16 |
| C266-02 | `source/exit_watch.rs` + `source/hook/pid_watch.rs` (supersedes R0615-CR-D2) | `ExitWatch` + `HookPidWatch` are "near-identical pid-drain scaffolds" → genericize over (watched value, emitted message) | ACCEPTED-residual (#334 wontfix) | the premise is FALSE: `HookPidWatch` already COMPOSES `ExitWatch` (`exit: Arc<ExitWatch>`, `ExitWatch::spawn` at pid_watch.rs:51) — ALL 48 platform-backend matches (kqueue/pidfd) live ONCE in exit_watch.rs, ZERO in pid_watch.rs (only ~40 lines unique: the pid→agents registry + the i32→SessionEnd drain). A generic `WatcherBase<T,M>` would force the platform code generic over T/M for ~100 lines saved while adding type params + bounds + 2× the live-test flake surface (#325 just de-flaked exit_watch). Composition > inheritance; revisit only if a THIRD watcher with a non-trivial independent impl appears (YAGNI). Verified by grounding + spot-check this session | B | fix/census-266-followups | 2026-06-16 |
## 2026-06-16 — PR #338 two-lens review (CodeWhale/OpenClaw Windows HOME-first config-path fix)

Seam: per-CLI config-path resolution for the HOME-first install targets. A user reported CodeWhale "installed but no sprite" on Windows; root cause = pixtuoid resolved `~` USERPROFILE-first while the CLI is `$HOME`-first. Two-lens review (correctness/grounding + design/blast-radius); both APPROVE-WITH-NITS, 0 crit / 0 high. All 6 upstream claims independently re-verified by the correctness lens (CodeWhale `effective_home_dir`, OpenClaw `resolveRawOsHomeDir`/`resolveStateDir`, "other 6 USERPROFILE-first" via Node/Go docs). The two MEDIUMs were COMPLETENESS gaps (the SAME bug class via a different env var), not regressions — fixed in-PR per the no-phasing rule.

| ID | seam | claim | verdict | mechanism / anchor | tier | HEAD | date |
|---|---|---|---|---|---|---|---|
| R0616-338-01 | `install/codewhale.rs::resolve_config_path` | MED: mirrors `CODEWHALE_CONFIG_PATH`/`DEEPSEEK_CONFIG_PATH` but NOT `CODEWHALE_HOME` (cw_config.rs:3568 honors it first) → same installed-but-no-sprite via a different env var | CONFIRMED→FIXED | premise verified (cw `codewhale_home` honors `CODEWHALE_HOME` VERBATIM as the `.codewhale`-equiv app dir; `.deepseek` legacy stays under OS home). Layered `CODEWHALE_HOME` into `resolve_config_path` (app-dir-verbatim, NOT a home base) + `detect_installed`; test `config_path_codewhale_home_is_the_app_dir_verbatim` | A | fix/codewhale-windows-config-path | 2026-06-16 |
| R0616-338-02 | `install/openclaw.rs::default_config_path` | MED: omits `OPENCLAW_CONFIG_PATH` (paths.ts:160) + the existing-`openclaw.json`/legacy-`clawdbot.json` preference → plugin registration lands where the gateway never reads | CONFIRMED→FIXED | premise verified (resolveCanonicalConfigPath honors `OPENCLAW_CONFIG_PATH`; resolveConfigPath prefers existing then legacy). Added `resolve_openclaw_config_path` (override → existing openclaw.json → legacy clawdbot.json → fresh); test `openclaw_config_path_override_and_legacy_file_preference` | A | fix/codewhale-windows-config-path | 2026-06-16 |
| R0616-338-03 | `install/openclaw.rs::resolve_openclaw_state_dir` | LOW: always joins `.openclaw`, no legacy `.clawdbot` fallback → a fresh `.openclaw/openclaw.json` shadows a pre-rebrand `.clawdbot` config (the `.deepseek` shadowing CodeWhale avoids) | CONFIRMED→FIXED | premise verified (resolveStateDir legacy fallback, paths.ts:23). Added prefer-existing-`.openclaw`-else-`.clawdbot` (mirrors `.codewhale`/`.deepseek`); test `openclaw_state_dir_prefers_legacy_clawdbot_only_when_modern_absent` | A | fix/codewhale-windows-config-path | 2026-06-16 |
| R0616-338-04 | `install/codewhale.rs::resolve_config_path` env override | LOW: `*_CONFIG_PATH` taken verbatim; upstream `normalize_config_file_path` rejects `..` + resolves a RELATIVE value against cwd | ACCEPTED-residual | a relative override CANNOT be reconciled: the installer and CodeWhale run in DIFFERENT cwds, so cwd-resolving it in pixtuoid would NOT make the two agree — absolute is the only well-defined contract. Documented the absolute-only scope in the fn doc (WHY) | B | fix/codewhale-windows-config-path | 2026-06-16 |
| R0616-338-05 | `pixtuoid/CLAUDE.md:79,133` TARGETS doc | LOW: `TARGETS = [… CURSOR]` (6) omits `OPENCLAW` (7th, target.rs:227) — pre-existing drift, but in the paragraph this PR rewrites | CONFIRMED→FIXED | both occurrences updated to include `OPENCLAW` (doc-currency rule, in-paragraph) | A | fix/codewhale-windows-config-path | 2026-06-16 |
| R0616-338-06 | `doctor.rs::home_split_advisory` scope | LOW: advisory is GLOBAL (fires on `HOME≠USERPROFILE` even with no HOME-first CLI connected) | REFUTED-design | deliberate: a Windows safety net for the residual/closed-source/future-CLI case the fix can't per-source-verify; per-source would require knowing each CLI's resolver — exactly what the hedged advisory ("if a source's sprite is missing") guards against. Bounded noise (one Windows-only sanitized line). WHY in the fn doc | A | fix/codewhale-windows-config-path | 2026-06-16 |
| R0616-338-07 | `doctor.rs::win_path_eq` to_lowercase | NIT: full-Unicode case fold on non-ASCII paths | REFUTED-wrong-premise | reviewer self-ruled benign: `win_path_eq` ONLY decides whether to SHOW the advisory (never resolves a real path); a false-equal merely suppresses a hint. `/c/Users` vs `C:\Users` stays unequal (test-pinned). No code change | A | fix/codewhale-windows-config-path | 2026-06-16 |

### Comprehensive source-location double-check (user-requested final pass, online upstream re-read of EVERY install target)

A deliberate re-audit of all 7 install targets vs each CLI's authoritative upstream config-path resolution. Found a THIRD bug that BOTH two-lens reviewers missed (they checked only the HOME-vs-USERPROFILE axis and the `os.UserHomeDir` fallback branch; the real divergence is the %APPDATA% config-dir axis). The other 6 re-confirmed correct WITH upstream citations. Calibration note: the correctness lens's "reasonix safe (Go os.UserHomeDir=USERPROFILE)" was a FALSE suppression — `os.UserHomeDir` is only the fallback; `ReasonixHomeDir` uses `os.UserConfigDir` (%APPDATA%) on Windows FIRST.

| ID | seam | claim | verdict | mechanism / anchor | tier | HEAD | date |
|---|---|---|---|---|---|---|---|
| R0616-338-08 | `install/reasonix.rs::default_config_path` | HIGH (missed by both lenses): writes `%USERPROFILE%\.reasonix\settings.json` on Windows, but Reasonix's `ReasonixHomeDir` is platform-ASYMMETRIC — Windows = `%APPDATA%\reasonix` (Go `os.UserConfigDir`), so the global hooks file Reasonix reads is `%APPDATA%\reasonix\settings.json` → installed-but-no-sprite on Windows (the %APPDATA% axis, distinct from HOME-vs-USERPROFILE) | CONFIRMED→FIXED | premise verified against the AUTHORITATIVE `docs/CONFIG_PATHS.md` (home: macOS/Linux `~/.reasonix`, Windows `%APPDATA%\reasonix`; "Global hooks = `<home>/settings.json`") + `config.go::reasonixHomeDir`. Added `reasonix_home` (REASONIX_HOME → Windows `user_config_dir()/reasonix` → `~/.reasonix`) routing `default_config_path` + `detect_installed`; pixtuoid's detect ALREADY probed %APPDATA% — the write path was just never aligned. Tests `reasonix_home_is_appdata_on_windows_but_dot_reasonix_elsewhere` + env-override | A | fix/codewhale-windows-config-path | 2026-06-16 |
| R0616-338-09 | codex / opencode / claude / cursor / copilot / antigravity locations | the other 6 install/watch targets write/read where the CLI actually reads, on every platform incl. Windows | REFUTED-wrong-premise (i.e. no bug) | re-verified upstream THIS pass: codex `dirs::home_dir`+`CODEX_HOME` (USERPROFILE); opencode `OPENCODE_CONFIG_DIR`→`xdg-basedir xdgConfig` = `XDG_CONFIG_HOME ?? os.homedir()/.config` = `%USERPROFILE%\.config` (NOT %APPDATA% — core/src/global.ts:13,64); claude `CLAUDE_CONFIG_DIR`(honored both sides)+os.homedir; cursor `~/.cursor/hooks.json` uniform (cursor.com/docs); copilot `~/.copilot` uniform; antigravity `<home>/.gemini/antigravity-cli/brain` (explicit "NOT %APPDATA%" in source). All USERPROFILE/USERPROFILE-`.config`-based on Windows = pixtuoid's generic resolver | A | fix/codewhale-windows-config-path | 2026-06-16 |

### Round-3 per-axis re-verification (user-requested "another round"; 9 agents batched + completeness critic). FLIPPED R0616-338-09's cursor sub-claim (false suppression) + 2 deferred-with-issues. The 3 fixes (codewhale/openclaw/reasonix) re-confirmed source-grounded + faithful; codex/opencode/claude/copilot re-confirmed match. Critic verdict: RESIDUAL-RISK (not a merge-blocker — all 9 DEFAULT paths correct).

| ID | seam | claim | verdict | mechanism / anchor | tier | HEAD | date |
|---|---|---|---|---|---|---|---|
| R0616-338-10 | `install/cursor.rs::default_config_path` (SUPERSEDES the cursor sub-claim of R0616-338-09) | MED: hard-codes `~/.cursor/hooks.json`, ignoring Cursor's documented config-dir overrides `CURSOR_CONFIG_DIR` (all-OS) + `XDG_CONFIG_HOME` (Linux/BSD) → a user who sets either has Cursor read the relocated dir while pixtuoid wrote `~/.cursor` (installed-but-no-sprite). The env-override axis — NOT the %APPDATA% axis (home base is correctly USERPROFILE) | CONFIRMED→FIXED | premise verified against AUTHORITATIVE cursor.com/docs/cli/reference/configuration (`CURSOR_CONFIG_DIR` custom dir + Linux/BSD `XDG_CONFIG_HOME/cursor`) + cursor.com/docs/hooks (hooks.json lives in that dir). Added `cursor_config_dir`/`resolve_config_dir` (CURSOR_CONFIG_DIR → Linux/BSD XDG/cursor → ~/.cursor) routing `default_config_path` + `detect_installed`; test `config_dir_honors_cursor_config_dir_then_xdg_on_linux_else_dot_cursor`. Default path unchanged | A | fix/codewhale-windows-config-path | 2026-06-16 |
| R0616-338-11 | `source/antigravity.rs::default_paths` Windows brain dir | the `<home>/.gemini/antigravity-cli/brain` (USERPROFILE) path is unverified-from-source — `agy` is a CLOSED Go binary, the exact Reasonix-trap shape (`os.UserHomeDir` vs `os.UserConfigDir`/`%LOCALAPPDATA%` unreadable); rests on secondary sources only | DEFERRED→#341 | GENUINELY source-unverifiable (closed binary, no fetchable resolver; impossibility PROVEN — cannot read which Go home-API agy calls, cannot run Windows). Transcript-only (no data risk), default-path-correct on macOS/Linux. Live-Windows verify TODO tracked in #341; current path is best-available-by-inference | B | fix/codewhale-windows-config-path | 2026-06-16 |
| R0616-338-12 | env-override value normalization across install targets | LOW cross-source pattern: pixtuoid takes config-location env overrides VERBATIM while upstreams normalize (OpenClaw `~`-expand+resolve — most plausible; CodeWhale trim; Reasonix `filepath.Clean`; Codex canonicalize) | DEFERRED→#342 | consciously consolidated (critic's recommendation): each case is cosmetic/pathological (shells pre-expand `~`; absolute-override is the documented contract, see codewhale `resolve_config_path` SCOPE note on the cross-process irreconcilability of relative overrides). Tracked in #342 | B | fix/codewhale-windows-config-path | 2026-06-16 |

## 2026-06-16 — PR #343 two-lens review (recreated #338 after the clean rebase onto main 0.10.0)

The repo-mandated two-lens (correctness/grounding + design/blast-radius) over the FULL #343 diff + an adversarial-verify phase (0 rechecks — nothing ≥ medium). Both lenses APPROVE-WITH-NITS, 0 crit/0 high/0 medium; all four upstream premises (CodeWhale/OpenClaw/Reasonix/Cursor) independently re-verified against authoritative source IN-SESSION; the rebase integrated cleanly (install/mod.rs/verify.rs/target.rs untouched). This is on top of the #338 online claude-action review (1 doc nit fixed, 0 security).

| ID | seam | claim | verdict | mechanism / anchor | tier | HEAD | date |
|---|---|---|---|---|---|---|---|
| R0616-343-01 | `install/mod.rs:885` test comment | NIT: comment says the plugin dir "resolves from `openclaw_home()`" but this PR renamed `openclaw_home`→`openclaw_state_dir` — a stale ref left in an untouched file (rename-sweep should cover untouched files) | CONFIRMED→FIXED | premise verified via `grep -rn openclaw_home` (the only stale CALL ref; the `openclaw_home_env` param name is correct). Updated to `openclaw_state_dir()` | A | fix/windows-install-paths | 2026-06-16 |
| R0616-343-02 | `install/reasonix.rs::default_config_path` error message | NIT: says "REASONIX_HOME/HOME/USERPROFILE unset" but the None case is reachable ONLY on non-Windows-no-HOME (the Windows arm always `Some` via `user_config_dir()`), so `USERPROFILE` is dead in that message | CONFIRMED→FIXED | dropped `USERPROFILE` → "REASONIX_HOME/HOME unset" + a WHY comment | A | fix/windows-install-paths | 2026-06-16 |
| R0616-343-03 | `install/reasonix.rs` Windows `%APPDATA%`-unset fallback | LOW: pixtuoid's `user_config_dir()` falls `%APPDATA%`→`<home>/AppData/Roaming` when APPDATA unset, where Go's `os.UserConfigDir` ERRORS (no home) | ACCEPTED-residual | strictly MORE robust than upstream + not a regression (pre-PR wrote `~/.reasonix` unconditionally — worse). Only reachable when Reasonix itself resolves no home (can't read the file either) and the computed path IS the canonical `%APPDATA%` default. Documented the divergence in `resolve_reasonix_home`'s doc | B | fix/windows-install-paths | 2026-06-16 |
| R0616-343-04 | `OPENCLAW_HOME`/`REASONIX_HOME` verbatim (no `~`-expand) | LOW (both lenses): upstream `~`-expands these; pixtuoid takes verbatim → a literal `~/x` override mislands | DEFERRED→#342 | the same pattern already adjudicated as R0616-338-12 → #342 (absolute-only contract; shells pre-expand `~`). Both reviewers confirmed the deferral is intentional. No new action | B | fix/windows-install-paths | 2026-06-16 |
| R0616-343-05 | `install/mod.rs:889,938,977` verify_target tests `set_var("OPENCLAW_STATE_DIR")` | NIT: env set without serialization/`remove_var` → could race under parallel `cargo test` | REFUTED-design | the repo runs `just test` = **nextest = process-per-test**, so the process-global env can't race a sibling; the existing comment at mod.rs:887 already documents this. Pre-existing (not in this PR's diff). No change | A | fix/windows-install-paths | 2026-06-16 |

## 2026-06-16 — #342 env-override normalization (RESOLVED; supersedes R0616-338-12 / R0616-343-04 DEFERRED)

Premise re-verified upstream THIS session before coding: **OpenClaw DOES `~`-expand** its overrides (`infra/home-dir.ts::resolveRawHomeDir` `^~(?=$|[/\\])` + `expandHomePrefix`/`resolveUserPath`), but **Reasonix does NOT** (`cleanEnvDir` = `TrimSpace`+`filepath.Clean`, no `~`) and **CodeWhale only `.trim()`s** — so the two review lenses' "Reasonix expands ~" was a wrong premise. Implemented faithfully (NOT blanket `~`-expand, which would diverge for the verbatim CLIs).

| ID | seam | claim | verdict | mechanism / anchor | tier | HEAD | date |
|---|---|---|---|---|---|---|---|
| R0342-01 | `install/io.rs::expand_tilde` + the install-target env reads | env-location overrides taken VERBATIM while upstreams normalize (`~`-expand / trim) | CONFIRMED→FIXED (#342) | new `io::expand_tilde(value, home)` (trim always; expand leading `~`/`~/`/`~\` against `home` only when `Some`). Applied: **OpenClaw** OPENCLAW_STATE_DIR/HOME/CONFIG_PATH with `home = home_first_dir()` (the real `~`-expand case); **CodeWhale** CODEWHALE_HOME + `*_CONFIG_PATH` and **Reasonix** REASONIX_HOME with `home = None` (TRIM only — they don't `~`-expand, so expanding would DIVERGE). Codex/Cursor left verbatim (no upstream normalization). Tests pin both arms. Closes #342 | A | fix/env-override-normalization | 2026-06-16 |
| R0342-02 | `install/openclaw.rs::detect_installed` | HIGH (online claude review): the write path `openclaw_state_dir()` was updated to `~`-expand `OPENCLAW_STATE_DIR`/`OPENCLAW_HOME` but `detect_installed()` still read them VERBATIM → a `~`-prefixed override installs into `<home>/…` yet detection probes literal `~/…` (never exists) → Sources panel never OFFERS the OpenClaw it just installed into (install/detect asymmetry the PR itself introduced) | CONFIRMED→FIXED | premise verified by reading both fns: write path `.map(|v| io::expand_tilde(&v, home))`, detect path `.map(PathBuf::from)` — real gap. `codewhale::detect_installed` WAS updated (it expands `CODEWHALE_HOME`), `reasonix::detect_installed` shares `reasonix_home()` (already normalized) — OpenClaw was the only miss. Fix: extracted injectable pure core `resolve_openclaw_detect` (parallels `resolve_openclaw_state_dir`) + wired `expand_tilde` at the `detect_installed` boundary identically to the write path; new host-agnostic precedence test `openclaw_detect_probes_the_same_resolved_dirs_as_install` | A | fix/env-override-normalization | 2026-06-16 |
| R0342-03 | `crates/pixtuoid/CLAUDE.md` "How do hooks get installed?" | MEDIUM (online claude review): the OpenClaw env-override chain doc predated the `~`-expansion contract → a future editor could remove the `expand_tilde` calls as "unnecessary" | CONFIRMED→FIXED | documented the `~`-expand contract (all three OpenClaw overrides expand; `detect_installed` mirrors it; CodeWhale/Reasonix TRIM-only via `expand_tilde(.., None)`; explicit "don't blanket-expand all targets" warning) per the "Keep docs current" rule | A | fix/env-override-normalization | 2026-06-16 |

## 2026-06-17 — smell-audit + whole-repo dedup arc → 0.11.0 cleanup PR (#350) + whole-codebase review

Arc: a burden-flipped whole-repo SMELL AUDIT (default = smell unless justified) → 63 findings, ALL personally verified by reading the code (4 fixed early, 22 refuted-as-deliberate with citations, the rest below); then an exhaustive multi-lens DUPLICATION hunt (47 candidates) + a 22-candidate re-verify of the overload-failed batch → 17 confirmed dedups; folded into ONE comprehensive 0.11.0 PR per the no-deferral rule. Closed with a 5-lens whole-codebase review using the NEW duplication/DRY + SYSTEM lenses (added to the PR-review briefs this arc): 2 APPROVE / 3 APPROVE-WITH-NITS, 0 crit / 0 high, all 4 confirmed + 3 critic findings LOW. Grid/derive/dedup byte-identity all VERIFIED (1749 tests + render golden/pixel-regression; semver + gen-check green).

| ID | seam | claim | verdict | mechanism / anchor | tier | HEAD | date |
|---|---|---|---|---|---|---|---|
| R0617-50-01 | `sprite::Frame`/`sprite::RgbBuffer` vs `grid::Grid<T>` | both hand-roll the `width×height` row-major `Vec<T>` storage `Grid<T>` (#333) exists to own — a dissent argued Grid was "deliberately scoped to masks" | CONFIRMED→FIXED (user-directed) | Frame=newtype(Grid<Pixel>) / RgbBuffer=newtype(Grid<Rgb>), Deref keeps `.width/.height/as_slice` transparent; RgbBuffer keeps INHERENT unchecked get/put (shadow Grid's checked get) for the blit hot path. The "scoped to masks" dissent was the user-rejected "kept-for-compat" ugliness, not principled design. Semver-breaking → 0.11.0. core/CLAUDE.md grid line documents the inherent-get sharp edge. byte-identical render | A | chore/smell-audit-cleanup | 2026-06-17 |
| R0617-50-02 | per-source `decode_*_hook_custom` / `*_tool_detail` / `SUBAGENT_TOOLS` consts | "trivial wrappers / parallel fns / same-name consts = duplication" | REFUTED-design | invariant #3 (per-source format knowledge in each source's own module); each owns its CLI's wire format / tool set. Only the format-agnostic generic-display ASSEMBLY was shared (`decoder::generic_tool_display`); dispatch stays per-source | A | chore/smell-audit-cleanup | 2026-06-17 |
| R0617-50-03 | per-CLI install resolution (`default_config_path`/`detect_installed`/`hook_command`/`merge_uninstall`/modern-legacy/XDG) | "N parallel install fns = duplication" | REFUTED-design | per-CLI mirroring of DIFFERENT upstream resolvers (root CLAUDE.md path-resolution-POLICY sharp edge: "mirror each CLI, don't blanket-generalize"). Only genuinely-shared mechanisms extracted (`verify::hook_path_str` ×7, `parse_json_or_empty`, `bake_hook_path`, one `SENTINEL_KEY`) | A | chore/smell-audit-cleanup | 2026-06-17 |
| R0617-50-04 | uniform fn-ptr-table signatures (`hook_command(_, explicit)`, `merge_install(_, _hook_cmd)`, `session_ended` stubs) | "dead param / unused arg / identical stub = smell" | REFUTED-design | the `Target` fn-ptr registry requires a uniform signature; openclaw's `_hook_cmd` (plugin-file install) + the Claude-only `explicit` ignores are the standard cost (target.rs doc) | A | chore/smell-audit-cleanup | 2026-06-17 |
| R0617-50-05 | `SceneState.agents` pub-vs-`daemons`-accessor; `unknown_cwd` bool; `daemons` String key; `floor_idx` snapshot; `DwellWindow::DECOR`; `pending_b1_cascades` placement; `cascade_exit` FOOTGUN comment | various "leaky/redundant/weak-type/lying-comment" PARTIALs | REFUTED-design | each cited: `agents` can't encapsulate (project_floor_scene inserts from a sibling crate); `unknown_cwd` = ledger R0614-23 + 2 pinning tests; `floor_idx` immutable-snapshot sharp edge (re-derive migrates agents); daemons-String = codebase-wide source-identity convention; cascade_exit comment is ACCURATE (suggests StampRoot only IF a 4th caller — hunter misread) | B | chore/smell-audit-cleanup | 2026-06-17 |
| R0617-50-06 | 17 confirmed duplications (lerp_rgb, expand_tilde, sanitizer, SENTINEL, FNV, make_floor_info, render_plugin, parse_or_empty, argv-flag, pet/mascot walk, elapsed_ms, dist2, generic_tool_display, hook_path_str, watchdog, settle_from_pair, wander-arm scaffold, paint_overlays, by_prefix, floor_capacity, spot-anchors, wash_glass, coffee-carrier, SourceArgs, run_tui×2, agent_slot) | genuine single-domain duplication | CONFIRMED→FIXED | one impl each, behavior-preserving (1749 tests + golden/pixel-regression). Divergent on-arrival bodies + load-bearing WHY comments kept INLINE (wander-arm scaffold), not buried behind callbacks | B | chore/smell-audit-cleanup | 2026-06-17 |
| R0617-50-07 | `install/mod.rs` OpenClaw tests (897/946/985) `OPENCLAW_STATE_DIR` set | review LOW: set process-global env WITHOUT `TEST_ENV_LOCK` → flaky under plain `cargo test` (pre-existing, nextest-masked) | CONFIRMED→FIXED | added the `TEST_ENV_LOCK` guard (mirrors mod.rs:617) to all 3; `cargo test --lib install::` now 194/0 deterministic | A | chore/smell-audit-cleanup | 2026-06-17 |
| R0617-50-08 | `install/claude.rs::hook_command` | review-critic LOW: `hook_path_str` dedup left claude.rs hand-rolling the non-UTF-8 reject (different message) | CONFIRMED→FIXED | migrated both branches to `verify::hook_path_str` (unifies the message, completes the ×7 dedup) | A | chore/smell-audit-cleanup | 2026-06-17 |
| R0617-50-09 | `config.rs` tilde-expansion test deletion | review-critic LOW: the [05] consolidation dropped config's explicit `~user/p` + non-leading-`~` assertions | CONFIRMED→FIXED | restored both into `io.rs::expand_tilde_home_some_expands_leading_tilde_only` (behavior already correct; coverage restored) | A | chore/smell-audit-cleanup | 2026-06-17 |

## 2026-06-17 — dedup business-sense validation (the wrong-abstraction guard, #350)

User-requested second review of the 0.11.0 PR: validate that EVERY dedup is a RIGHT abstraction (call sites share ONE business reason-to-change), not a WRONG one ("duplication is cheaper than the wrong abstraction"). 30-agent run: one validator per dedup (adversarial toward REVERT) + a fresh 3-lens dup hunt + critic. **Result: all 26 dedups SOUND, 0 reverts/refines, 0 remaining/new duplication.** The pattern across every cross-domain-looking dedup: only the DOMAIN-NEUTRAL part was shared; the divergent part stayed inline. Two borderline cases cleared below (future reviews: demote, don't re-litigate). One defensive doc note added.

| ID | seam | claim | verdict | mechanism / anchor | tier | HEAD | date |
|---|---|---|---|---|---|---|---|
| R0617-DV-01 | `drawable.rs` spot-anchor helpers (pet + gateway mascot) | wrong abstraction? pet (office decoration) vs mascot (DaemonState-gated daemon indicator) are different domains — sharing `desk_visit_spot` could drag the pet's offset if the lobster's stand-beside is later tuned | REFUTED-sound-dedup | only the FURNITURE-GEOMETRY offset is shared (DESK_W/DESK_H-derived, not creature sprite); both then walk_between+snap so no pixel-precise pose; the divergent spot-SELECTION (pet=all+idle-bool+corridor / mascot=DaemonState-gated) correctly stays INLINE in pet_position/mascot_spots. Added a WHY-note documenting "share where-beside-furniture, not which-furniture-when" | A | chore/smell-audit-cleanup | 2026-06-17 |
| R0617-DV-02 | `id.rs::FNV_OFFSET_BASIS`/`FNV_PRIME` shared by `AgentId::from_parts` + `WalkableMask::signature` | wrong abstraction? identity-hash collision = correctness bug (sprite-merge) vs cache-key collision = perf bug (stale route) — asymmetric business consequences | REFUTED-sound-dedup | what's shared is ONLY the two standardized FNV-1a CONSTANTS — a mathematical fact, NOT a domain policy that can "diverge for a business reason"; the hashing LOGIC + inputs stay per-domain (string-id with 0xff separator vs sorted-rect geometry). The existing id.rs comment already documents "same primitive over different inputs, not a streaming hasher" | A | chore/smell-audit-cleanup | 2026-06-17 |

## 2026-06-17 — general-purpose whole-codebase review (#350)

User-requested general census (not just dedup): 12 finders (6 subsystem + 6 lens: correctness/security/silent-failure/concurrency/architecture/perf) → design-intent+repro adversarial verify → completeness critic. First run was server-rate-limited (11/12 finders throttled); re-run wave-split. **0 HIGH/critical; the 0.11.0 dedup/Grid work itself verified clean** (refutations: "paint_overlays a faithful refactor", "all 6 architecture invariants hold, post-0.11.0 crate split clean", "PixelCtx 21-field construction fine"). 7 confirmed findings, ALL pre-existing (floating window #346 / TUI modal mouse / render path), all FIXED in-PR per the no-deferral rule.

| ID | seam | claim | verdict | mechanism / anchor | tier | HEAD | date |
|---|---|---|---|---|---|---|---|
| R0617-GR-01 | `tui/mod.rs` mouse handler | MED: only help + version-popup install a mouse-swallow guard; left-clicks fall THROUGH the dashboard/Sources/theme-picker modals to the scene (top-left branding region uncovered by a centered modal) → `open::that(github)`/BMC or pin a hidden agent — the phantom-browser-launch class | CONFIRMED→FIXED | added `Event::Mouse(_) if theme_picker.is_some() \|\| dashboard_ui.open \|\| connection_ui.open => {}` before the general arm (inert; these have explicit close keys). Pre-existing on main, codecov-ignored event loop | A | chore/smell-audit-cleanup | 2026-06-17 |
| R0617-GR-02 | `floating/window.rs::about_to_wait` | MED: empty office (no agents) → `ControlFlow::Wait` (0fps) FREEZES the ambient layer (clock/weather/lightning/pet) — window looks dead | CONFIRMED→FIXED | both branches now `WaitUntil`+`request_redraw`; `IDLE_AMBIENT_FPS=1` (1000ms) when empty vs `ACTIVE_FPS=30` (33ms, exact original) — alive at minimal CPU | A | chore/smell-audit-cleanup | 2026-06-17 |
| R0617-GR-03 | `floating/window.rs::resumed` restore | LOW: persisted [floating] position restored with no on-screen validation → off-disconnected-monitor = unrecoverable (frameless/no-taskbar/always-on-top) | CONFIRMED→FIXED | `position_on_a_monitor` AABB-overlaps the saved rect vs `available_monitors()`; no overlap → skip `with_position` → OS-default placement; empty monitor list → keep saved (defensive) | A | chore/smell-audit-cleanup | 2026-06-17 |
| R0617-GR-04 | `pixel_painter/background/mod.rs` + `time_of_day.rs` + `ambient.rs` | LOW perf: per-window recompute of frame-invariant glass colors (lit_colors/building/sky_row, Lab+Vec); full-floor blend at strength 0; time_of_day_look computed twice/frame | CONFIRMED→FIXED | hoisted the invariants out of the window loop (`window_glass_invariants`, verified `h`/`look`/`theme` constant across windows); `dim_floor_overlay` early-returns at s<=0; ambient threads the already-computed `&look`. BYTE-IDENTICAL (golden+pixel-regression+gen-check green) | A | chore/smell-audit-cleanup | 2026-06-17 |
| R0617-GR-05 | `main.rs` Cmd::Floating | LOW docs: a positive `max-desks` config value is silently dropped in floating mode (capacity is window-pixel driven) | CONFIRMED→FIXED | documented in pixtuoid/CLAUDE.md (max-desks sharp edge): applies to `run` only; `max-desks=0` still warns via resolve_max_desks | A | chore/smell-audit-cleanup | 2026-06-17 |

## 2026-06-18 — sources-control-core refactor (Raycast / onboarding enabling)

Pre-implementation arc (consulted the `code-architect` agent + the architecture blueprint). Extracts a TUI-free `crate::sources` control core (detect/connect/disconnect/reconcile/status) consumed by THREE presenters: the in-TUI Sources panel (re-wired to delegate), a new scriptable CLI (`sources`/`connect`/`disconnect`), and (PR2) first-run onboarding. One adjudication: the CLI re-opens a surface #284 deliberately removed.

| ID | seam | claim | verdict | mechanism / anchor | tier | HEAD | date |
|---|---|---|---|---|---|---|---|
| R0618-01 | `cli.rs` `sources`/`connect`/`disconnect` subcommands | DELIBERATE-DESIGN REVERSAL: #284 deleted the `install-hooks`/`uninstall-hooks` CLI ("binding a source is the in-TUI Sources panel's job, `s`") — re-adding a connect/disconnect CLI re-opens that surface | ACCEPTED-as-second-presenter | The panel stays the INTERACTIVE path; the CLI is the SCRIPTABLE path (Raycast / automation / onboarding). Both write through the SAME `crate::sources::{connect,disconnect}` core (persist `[sources]` flag + `install_target`/`uninstall_target` + rollback) — a SECOND presenter over the structured core, NOT a re-litigation of the panel's role. #284 removed the CLI ORCHESTRATION (`plan_targets`/`interactive_pick`/confirm policy), which this does NOT restore (the CLI takes explicit ids; no interactive target-selection). New sharp edge added in `crates/pixtuoid/CLAUDE.md` (scriptable-vs-interactive surface). Anchors: `crates/pixtuoid/src/sources.rs`, `cli.rs` Cmd::{Sources,Connect,Disconnect}, `main.rs` run_{sources_list,sources_set,change} presenters | A | feat/sources-control-core | 2026-06-18 |

### PR #355 two-lens review (2026-06-18) — 3 confirmed, 0 refuted, all FIXED in-PR

| ID | seam | claim | verdict | mechanism / anchor | tier | HEAD | date |
|---|---|---|---|---|---|---|---|
| R0618-02 | `main.rs` Cmd::Disconnect / `reconcile_to` disconnect arm | MED: the CLI `.map(\|_\| ())`'d the disconnect outcome → `DisconnectOutcome::HookRemovalFailed` collapsed to a clean "disconnected" (exit 0) while stale hooks remained; the panel surfaces it but the scriptable surface (the point of PR1) hid it | CONFIRMED→FIXED | `run_change`'s `op` now returns `Result<String>` (the success token); Disconnect maps `HookRemovalFailed(e)` → `disconnected (hook removal failed: …)`; `reconcile_to` maps it → `ChangeOutcome::Failed("hooks not removed: …")`. Core test `disconnect_target_folds_a_hook_removal_failure_into_the_outcome` pins the fold + flag-false | A | feat/sources-control-core | 2026-06-18 |
| R0618-03 | `cli.rs` Sources/SourcesAction::Set `json` | LOW: `sources --json set <ids>` silently ignored `--json` (outer flag discarded via `..`, inner `Set.json` defaulted false) → plain table at exit 0, undercutting the pinned `--json` contract | CONFIRMED→FIXED | made `Sources.json` `#[arg(global = true)]` + dropped the duplicate `Set.json`; honored before OR after `set`. Pinned by `sources_json_is_global_across_the_set_subcommand` | A | feat/sources-control-core | 2026-06-18 |
| R0618-04 | `sources.rs` connect / plan_reconcile vs panel NoCli | NIT: CLI `connect`/`sources set` installs hooks for an ABSENT CLI (materializes its config dir), diverging from the panel's `NoCli` refusal — undocumented | CONFIRMED→FIXED(doc) | DELIBERATE: the CLI honors the explicit id (pre-provisioning for automation; `detect()` returns only present CLIs so onboarding offers only installed ones). Documented in `connect`'s doc comment + the `crates/pixtuoid/CLAUDE.md` "two surfaces, one core" sharp edge | A | feat/sources-control-core | 2026-06-18 |

| R0618-05 | `main.rs` run_change / run_sources_set exit code | MED (online review, carried-over): the CLI exited 0 even when a per-source op FAILED (connect install failure + flag rollback, persist abort, or a HookRemovalFailed partial) — only the pre-validation (unknown id) set a non-zero exit. A `$?`-checking shell/CI/onboarding caller got no signal on EXECUTION failure (the --json/Raycast path was unaffected — it parses per-source tokens) | CONFIRMED→FIXED | both presenters now track `any_failed` and `bail!` AFTER emitting all rows → non-zero exit on any op failure (connect Err, disconnect persist-abort, HookRemovalFailed mapped to Err / ChangeOutcome::Failed). Verified in a sandbox: a forced codex install failure exits 1; a clean connect exits 0 | A | feat/sources-control-core | 2026-06-18 |
