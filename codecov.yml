coverage:
  precision: 2
  round: down
  range: "40...100"

  status:
    project:
      default:
        target: auto
        threshold: 5%
        informational: true
    patch:
      default:
        target: 70%
        threshold: 5%
        informational: true

comment:
  layout: "condensed_header, diff, files"
  behavior: default
  require_changes: true
  require_base: false
  require_head: true

ignore:
  - "scripts/"
  - "docs/"
  - "*.md"
  # Not coverable headlessly — exercising these needs a real TTY / OS:
  - "crates/pixtuoid/src/main.rs"       # binary entry: terminal raw-mode, crash hook, signal handling
  - "crates/pixtuoid/src/tui/mod.rs"    # crossterm event loop: reads real keyboard/mouse from the TTY
  # Keeps the % honest: "of the code we CAN test headlessly, X% is tested",
  # rather than dragging the number down with paths no test can reach.
